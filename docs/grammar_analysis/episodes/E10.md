# The Hot Spot E10 日语文法解析

## 元信息

- 源文件：`2/The.Hot.Spot.S01E10.2025.V2.1080p.NF.WEB-DL.x264.AAC-ADWeb.jpn.srt`
- 字幕块数：965
- 当前状态：完成
- 已解析范围：0001-0965

## 第一批新文法总览（0001-0200）

本批新增或补登记 `G0407-G0411`。其中 `G0409 何から何まで` 为补登记，首见回填到 `E03-0619-2`；`G0410 Nさえ〜ば` 为补登记，首见回填到 `E09-0656-1`。同时扩展既有 `G0090` 覆盖口语请求 `Vてくんない`，扩展 `G0266` 覆盖 `Vさせてください`。

| 编号 | 文法 | 首次出现 | 本批次数 |
|---|---|---|---:|
| G0407 | Nに終わる：以……告终 | E10-0123-1 | 1 |
| G0408 | それどころではない：顾不上/不是……的时候 | E10-0128-1 | 2 |
| G0409 | 何から何まで：从头到尾/方方面面 | E03-0619-2 | 1 |
| G0410 | Nさえ〜ば：只要……就 | E09-0656-1 | 1 |
| G0411 | それはそうと：先不说这个/话说回来 | E10-0110-1 | 1 |

## 逐句解析（0001-0200）

### E10-0001 `00:00:04,003 --> 00:00:08,842`

- `（物音）`：音效，跳过统计。

### E10-0002 `00:00:09,884 --> 00:00:11,928`

- `（開けようとする音）`：动作音，跳过统计。

### E10-0003 `00:00:15,181 --> 00:00:16,141`

- `ハッ…`：惊吓/喘息，跳过统计。

### E10-0004 `00:00:27,652 --> 00:00:28,570`

- `（スイッチを押す音）`：动作音，跳过统计。

### E10-0005 `00:00:29,612 --> 00:00:30,822`

1. `あっ そっか`
   - `そっか`：`G0003`，理解/领会。

### E10-0006 `00:00:31,865 --> 00:00:33,033`

- `ハァ…`：叹息，跳过统计。

### E10-0007 `00:00:42,667 --> 00:00:44,085`

- `（鍵の開く音）`：音效，跳过统计。

### E10-0008 `00:00:44,169 --> 00:00:45,503`

- `（引き出しの開く音）`：音效，跳过统计。

### E10-0009 `00:00:45,587 --> 00:00:46,713`

- `ハァ…`：叹息，跳过统计。

### E10-0010 `00:00:48,256 --> 00:00:50,008`

- `（携帯電話の着信音） / （鍵の閉まる音）`：音效，跳过统计。

### E10-0011 `00:00:50,091 --> 00:00:52,052`

- `ハァ… / はいはい はいはい`：叹息和应答，跳过统计。

### E10-0012 `00:00:53,511 --> 00:00:54,971`

- `あっ もしもし`：电话开场寒暄，跳过统计。

### E10-0013 `00:00:56,431 --> 00:00:59,809`

1. `お世話になってます`
   - `お世話になってます`：`G0087` 加 `G0030` 加 `G0012`，固定电话寒暄，“承蒙关照”。

### E10-0014 `00:01:03,480 --> 00:01:07,442`

1. `あ～ 16日は…`
   - `16日は`：`G0013`，把日期作为话题；句子未完。

### E10-0015 `00:01:07,525 --> 00:01:09,277`

1. `ちょっと待ってくださいね`
   - `待ってください`：`G0053`，礼貌请求。
   - `ね`：`G0034`，缓和语气。

### E10-0016 `00:01:10,570 --> 00:01:13,239`

1. `夜ですよね？`
   - `です`：`G0012`，丁寧体。
   - `よね`：`G0083` 加 `G0034`，确认并求认同。

### E10-0017 `00:01:14,949 --> 00:01:17,243`

1. `でしたら大丈夫です`
   - `でしたら`：`G0054`，`だったら` 的礼貌形式，表示条件。
   - `です`：`G0012`，礼貌断定。

### E10-0018 `00:01:18,453 --> 00:01:20,997`

1. `では その時間に`
   - `では`：`G0056`，承接“那么”。
   - `その時間に`：`G0017`，指定时间点。

### E10-0019 `00:01:22,040 --> 00:01:24,125`

1. `失礼いたします`
   - `いたします`：`G0051` 加 `G0012`，`する` 的谦让丁寧形，电话结束寒暄。

### E10-0020 `00:01:34,719 --> 00:01:35,470`

- `（せんべいの砕ける音）`：音效，跳过统计。

### E10-0021 `00:01:54,030 --> 00:01:55,240`

1. `本社への連絡は済んだ？`
   - `本社への連絡`：`G0055`，`へ` 后接 `の` 修饰名词，表示联络对象。
   - `連絡は`：`G0013`，把“联络”作为确认话题。
   - `済んだ`：`G0001`，完成状态，“已经办完了吗”。

### E10-0022 `00:01:55,323 --> 00:01:57,033`

1. `はい 先ほど済みました`
   - `済みました`：`G0001` 加 `G0012`，礼貌过去/完成。

### E10-0023 `00:01:57,325 --> 00:01:59,619`

1. `あとは報告書を上げて 今日は終了になります`
   - `報告書を`：`G0031`，动作对象。
   - `上げて`：`G0035`，て形连接后续。
   - `今日は`：`G0013`，今日作为话题。
   - `終了になります`：`G0048` 加 `G0012`，礼貌说明“变为/算作结束”。

### E10-0024 `00:01:59,702 --> 00:02:00,537`

- `了解`：名词式应答，跳过统计。

### E10-0025 `00:02:01,079 --> 00:02:02,288`

- `（ため息）`：音效，跳过统计。

### E10-0026 `00:02:09,671 --> 00:02:10,797`

- `（ドアの閉まる音）`：音效，跳过统计。

### E10-0027 `00:02:24,018 --> 00:02:26,229`

1. `うん 分かった`
   - `分かった`：`G0001`，理解完成，“明白了”。

### E10-0028 `00:02:26,980 --> 00:02:29,691`

1. `ここにいたら うちらも怪しまれそうだから`
   - `ここに`：`G0017`，所在位置。
   - `いたら`：`G0045` 加 `G0054`，如果继续在这里。
   - `うちらも`：`G0011`，我们也。
   - `怪しまれそう`：`G0006` 加 `G0032`，看起来会被怀疑。
   - `だから`：`G0088`，说明理由；句子延续到下一块。

### E10-0029 `00:02:29,774 --> 00:02:30,650`

1. `ちょっと移動しま～す`
   - `移動します`：`G0029` 加 `G0012`，名词＋する的礼貌体。

### E10-0030 `00:02:30,733 --> 00:02:31,651`

- `はい`：应答，跳过统计。

### E10-0031 `00:02:37,282 --> 00:02:38,575`

1. `電力が復旧したら`
   - `電力が`：`G0010`，主语。
   - `復旧したら`：`G0029` 加 `G0054`，恢复供电之后/如果恢复供电。

### E10-0032 `00:02:38,658 --> 00:02:41,161`

1. `セキュリティーの信号も自動的に復旧しますので`
   - `セキュリティーの信号`：`G0004`，名词修饰。
   - `信号も`：`G0011`，信号也。
   - `自動的に`：`G0017`，副词化的状态。
   - `復旧しますので`：`G0029` 加 `G0012` 加 `G0052`，礼貌说明原因/依据。

### E10-0033 `00:02:41,244 --> 00:02:42,453`

1. `ご安心ください`
   - `ご安心ください`：`G0046` 加 `G0053`，敬语框架加请求，“请放心”。

### E10-0034 `00:02:42,537 --> 00:02:44,455`

1. `こちら 警備報告書になります`
   - `になります`：`G0048` 加 `G0012`，服务业礼貌提示。

### E10-0035 `00:02:44,539 --> 00:02:45,665`

- `ありがとう`：感谢语，跳过统计。

### E10-0036 `00:02:51,963 --> 00:02:53,089`

1. `また 車 来ましたよ`
   - `来ました`：`G0001` 加 `G0012`，车又来了。
   - `よ`：`G0083`，提醒告知。

### E10-0037 `00:02:53,173 --> 00:02:54,340`

- `えっ？`：反应语，跳过统计。

### E10-0038 `00:02:55,925 --> 00:02:57,719`

1. `電力会社の人かな？`
   - `電力会社の人`：`G0004`，名词修饰。
   - `かな`：`G0099`，自问式推测。

### E10-0039 `00:02:57,802 --> 00:03:00,054`

1. `電気 復旧しに来たんですかね？`
   - `復旧しに来た`：`G0214` 加 `G0001`，来做“恢复供电”这件事。
   - `んですかね`：`G0066` 加 `G0012` 加 `G0099`，说明式委婉推测。

### E10-0040 `00:03:00,305 --> 00:03:02,932`

1. `防犯カメラ 作動しちゃうよね？`
   - `作動しちゃう`：`G0029` 加 `G0133`，会启动掉/启动起来。
   - `よね`：`G0083` 加 `G0034`，提醒并求确认。

### E10-0041 `00:03:03,016 --> 00:03:04,350`

1. `しますね`
   - `します`：`G0012`，礼貌体。
   - `ね`：`G0034`，确认。

### E10-0042 `00:03:04,434 --> 00:03:05,560`

- `ヤバイ`：评价短句，跳过统计。

### E10-0043 `00:03:07,395 --> 00:03:08,605`

- `（携帯電話のバイブレーター）`：音效，跳过统计。

### E10-0044 `00:03:34,839 --> 00:03:36,341`

1. `まもなく復旧すると思われます`
   - `復旧する`：`G0029`，名词＋する。
   - `と思われます`：`G0023` 加 `G0006` 加 `G0012`，正式说明“认为会/预计会”。

### E10-0045 `00:03:36,424 --> 00:03:37,800`

1. `あっ 分かりました`
   - `分かりました`：`G0001` 加 `G0012`，礼貌接受。

### E10-0046 `00:03:43,890 --> 00:03:45,183`

1. `あっ つきました`
   - `つきました`：`G0001` 加 `G0012`，灯亮了/恢复了。

### E10-0047 `00:03:46,684 --> 00:03:48,937`

- `（足音）`：音效，跳过统计。

### E10-0048 `00:03:57,654 --> 00:03:58,947`

- `ハァ…`：叹息，跳过统计。

### E10-0049 `00:04:02,992 --> 00:04:04,118`

- `フゥ…`：叹息，跳过统计。

### E10-0050 `00:04:42,156 --> 00:04:44,117`

- `あっ… あ～っ！`：惊讶呼声，跳过统计。

### E10-0051 `00:04:44,200 --> 00:04:45,493`

- `高橋さ～ん！`：呼唤，跳过统计。

### E10-0052 `00:04:45,576 --> 00:04:46,911`

- `高橋さん！`：呼唤，跳过统计。

### E10-0053 `00:04:47,620 --> 00:04:49,205`

- `高橋さん 高橋さん`：呼唤，跳过统计。

### E10-0054 `00:04:49,288 --> 00:04:51,249`

1. `鍵 壊しちゃえば 取れたんだろうけど`
   - `壊しちゃえば`：`G0133` 加 `G0081`，如果把锁弄坏的话。
   - `取れた`：`G0069` 加 `G0001`，本来能拿到。
   - `んだろうけど`：`G0066` 加 `G0094` 加 `G0019`，推量并铺垫转折。

### E10-0055 `00:04:51,332 --> 00:04:54,961`

1. `さすがに 痕跡 残すのマズイと思って…`
   - `さすがに`：`G0213`，再怎么说。
   - `残すの`：`G0097`，把“留下痕迹”名词化。
   - `マズイと思って`：`G0023` 加 `G0035`，认为那样不妙并连接后文。

### E10-0056 `00:04:55,044 --> 00:04:57,005`

1. `確かに それじゃ しかたないですよね`
   - `それじゃ`：`G0056`，那么/那样的话。
   - `ない`：`G0021`，`しかたない` 中的否定形。
   - `ですよね`：`G0012` 加 `G0083` 加 `G0034`，礼貌确认。

### E10-0057 `00:04:57,088 --> 00:04:58,881`

1. `まあ 捕まらなかっただけでも`
   - `捕まらなかった`：`G0006` 加 `G0021` 加 `G0001`，没有被抓住。
   - `だけでも`：`G0039` 加 `G0217`，即便只是这样也……；句子延续。

### E10-0058 `00:04:58,965 --> 00:04:59,799`

1. `ねっ？`
   - `ね`：`G0034`，求认同。

### E10-0059 `00:04:59,882 --> 00:05:01,509`

1. `まあ そうだね…`
   - `そうだね`：`G0003` 加 `G0012` 加 `G0034`，认同。

### E10-0060 `00:05:01,884 --> 00:05:04,929`

1. `また作戦は練り直すとして`
   - `作戦は`：`G0013`，把“作战计划”作为话题。
   - `練り直すとして`：把“重新制定计划”暂作后续处理，复合动词 `直す` 本批按词汇处理。

### E10-0061 `00:05:05,013 --> 00:05:07,807`

1. `今日のところは解散しますかね`
   - `今日のところ`：`G0208`，现阶段/今天这一刻。
   - `ところは`：`G0013`，限定当前情况为话题。
   - `解散します`：`G0029` 加 `G0012`，名词＋する的礼貌体。
   - `かね`：`G0099`，委婉提议/确认。

### E10-0062 `00:05:07,890 --> 00:05:10,810`

1. `もう遅いし 帰ろっか ねっ？`
   - `し`：`G0110`，补充理由。
   - `帰ろっか`：`G0201` 加 `G0002`，意向形加确认，提议“回去吧”。
   - `ね`：`G0034`，征求认同。

### E10-0063 `00:05:10,893 --> 00:05:12,812`

1. `そうね`
   - `そうね`：`G0003` 加 `G0034`，认同。
2. `じゃ お疲れさまでした`
   - `じゃ`：`G0056`，承接结束。
   - `お疲れさまでした`：`G0057` 加 `G0001`，慰劳寒暄。

### E10-0064 `00:05:12,895 --> 00:05:14,981`

1. `お疲れさまでした`
   - `お疲れさまでした`：`G0057` 加 `G0001`，集体告别寒暄。

### E10-0065 `00:05:15,064 --> 00:05:17,275`

1. `気をつけてね`
   - `気をつけて`：`G0228`，句末て形提醒/叮嘱。
   - `ね`：`G0034`，柔和叮嘱。
2. `気をつけて`
   - `気をつけて`：`G0228`，轻请求/叮嘱。

### E10-0066 `00:05:17,817 --> 00:05:19,402`

- `お大事に / ああ…`：固定问候和回应，跳过统计。

### E10-0067 `00:05:20,403 --> 00:05:21,821`

1. `あっ そうだ 悪いんだけどさ`
   - `そうだ`：`G0003` 加 `G0012`，想起事情。
   - `んだけどさ`：`G0066` 加 `G0019` 加 `G0131`，铺垫请求并缓和。

### E10-0068 `00:05:21,904 --> 00:05:23,239`

1. `レイクホテル 寄ってもらってもいい？`
   - `寄ってもらって`：`G0141`，请对方顺路过去。
   - `てもいい`：`G0109`，许可/请求许可，“可以吗”。

### E10-0069 `00:05:23,322 --> 00:05:24,741`

1. `温泉ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌确认。

### E10-0070 `00:05:24,824 --> 00:05:25,658`

1. `分かりました`
   - `分かりました`：`G0001` 加 `G0012`，礼貌接受。

### E10-0071 `00:05:25,742 --> 00:05:27,702`

1. `じゃ 支配人に連絡しますね`
   - `じゃ`：`G0056`，承接。
   - `支配人に`：`G0017`，联络对象。
   - `連絡します`：`G0029` 加 `G0012`，名词＋する。
   - `ね`：`G0034`，说明将要做。

### E10-0072 `00:05:27,785 --> 00:05:29,829`

1. `あ～ 悪いね`
   - `ね`：`G0034`，缓和谢意。
2. `お願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0073 `00:05:29,912 --> 00:05:32,290`

1. `小野寺くんって どこの大学 出てんだっけ？`
   - `小野寺くんって`：`G0064`，口语提题。
   - `出てんだ`：`G0030` 加 `G0066`，`出ているんだ` 的口语缩约。
   - `だっけ`：`G0071`，回想确认。

### E10-0074 `00:05:32,373 --> 00:05:33,875`

1. `僕は甲斐文大です`
   - `僕は`：`G0013`，主题。
   - `です`：`G0012`，丁寧体。

### E10-0075 `00:05:33,958 --> 00:05:36,127`

1. `じゃ 清美ちゃんと一緒だ`
   - `じゃ`：`G0056`，那么/那就是。
   - `清美ちゃんと`：`G0016`，和清美。
   - `だ`：`G0012`，断定。

### E10-0076 `00:05:36,210 --> 00:05:37,253`

1. `えっ そうなんですか？`
   - `そうなんですか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，理解式确认。

### E10-0077 `00:05:37,336 --> 00:05:39,047`

1. `何学部？`
   - `何学部？`：`G0018`，省略 `ですか` 的名词疑问。

### E10-0078 `00:05:39,130 --> 00:05:40,256`

1. `経済学部でした`
   - `でした`：`G0012` 加 `G0001`，礼貌过去断定。

### E10-0079 `00:05:40,339 --> 00:05:41,883`

1. `どうしたの？`
   - `どう`：`G0093`，询问情况。
   - `した`：`G0001`，过去/发生。
   - `の`：`G0066`，说明式疑问。

### E10-0080 `00:05:45,386 --> 00:05:47,722`

1. `じゃ また`
   - `じゃ`：`G0056`，告别承接。

### E10-0081 `00:05:47,805 --> 00:05:49,265`

1. `連絡します`
   - `連絡します`：`G0029` 加 `G0012`，名词＋する的礼貌体。

### E10-0082 `00:05:49,348 --> 00:05:52,060`

1. `お疲れさまでした`
   - `お疲れさまでした`：`G0057` 加 `G0001`，慰劳寒暄。

### E10-0083 `00:05:55,521 --> 00:05:56,647`

- `アア…`：叹声，跳过统计。

### E10-0084 `00:05:57,273 --> 00:05:59,484`

- `高橋さん！ 高橋さん！`：呼唤，跳过统计。

### E10-0085 `00:05:59,567 --> 00:06:01,486`

- `支配人 ありがとうございます`：感谢语，跳过统计。

### E10-0086 `00:06:01,569 --> 00:06:02,862`

1. `今 フロント 誰もいないんで`
   - `誰もいない`：`G0165` 加 `G0045` 加 `G0021`，谁都不在。
   - `んで`：`G0052`，说明原因。

### E10-0087 `00:06:02,945 --> 00:06:03,905`

1. `そのまま入っちゃってください`
   - `そのまま`：`G0258`，保持现在状态直接进入。
   - `入っちゃって`：`G0133`，口语“就进去吧”。
   - `ください`：`G0053`，请求/指示。

### E10-0088 `00:06:03,988 --> 00:06:05,406`

- `あっ すいません`：道歉/客套，跳过统计。

### E10-0089 `00:06:05,490 --> 00:06:07,617`

1. `あした 朝番ですよね？`
   - `です`：`G0012`，丁寧体。
   - `よね`：`G0083` 加 `G0034`，提醒确认。

### E10-0090 `00:06:07,700 --> 00:06:09,243`

1. `あっ そうなんスよ`
   - `そうなんスよ`：`G0003` 加 `G0066` 加 `G0189` 加 `G0083`，口语丁寧缩约说明。

### E10-0091 `00:06:09,327 --> 00:06:11,913`

1. `あれだったら そのまま 空いた部屋で寝ていきます？`
   - `だったら`：`G0054`，如果那样的话。
   - `そのまま`：`G0258`，保持现状。
   - `空いた`：`G0001`，空出来的。
   - `部屋で`：`G0008`，动作场所。
   - `寝ていきます`：`G0085` 加 `G0012`，在这里睡下再离开。

### E10-0092 `00:06:11,996 --> 00:06:13,623`

1. `いいんですか？`
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌确认。

### E10-0093 `00:06:13,706 --> 00:06:15,249`

- `ありがとうございます`：感谢语，跳过统计。

### E10-0094 `00:06:15,333 --> 00:06:16,626`

1. `助かります`
   - `ます`：`G0012`，礼貌表达感谢。

### E10-0095 `00:06:16,709 --> 00:06:18,294`

- `あっ さあ / ああ…`：招呼和反应，跳过统计。

### E10-0096 `00:06:19,212 --> 00:06:20,379`

- `アア…`：叹声，跳过统计。

### E10-0097 `00:06:22,298 --> 00:06:23,299`

- `あっ…`：反应语，跳过统计。

### E10-0098 `00:06:23,800 --> 00:06:26,969`

1. `カゴ横に301号室のキー 置いといたんで`
   - `カゴ横に`：`G0017`，放置地点。
   - `301号室のキー`：`G0004`，名词修饰。
   - `置いといたんで`：`G0113` 加 `G0001` 加 `G0052`，`置いておいたので` 的口语缩约。

### E10-0099 `00:06:27,053 --> 00:06:28,262`

- `すいません`：道歉/客套，跳过统计。

### E10-0100 `00:06:28,346 --> 00:06:30,473`

1. `ホント 何から何まで ありがとうございます`
   - `何から何まで`：`G0409`，从头到尾/方方面面都承蒙照顾。

### E10-0101 `00:06:30,556 --> 00:06:31,974`

- `あっ いえいえ`：客套回应，跳过统计。

### E10-0102 `00:06:32,058 --> 00:06:35,728`

1. `ホテルを救ってもらうんですから これぐらいは させてください`
   - `ホテルを`：`G0031`，动作对象。
   - `救ってもらう`：`G0141`，获得对方帮助。
   - `んですから`：`G0066` 加 `G0012` 加 `G0088`，解释理由。
   - `これぐらい`：`G0027`，这种程度。
   - `これぐらいは`：`G0013`，强调“至少这点”。
   - `させてください`：`G0266` 加 `G0053`，请允许我做。

### E10-0103 `00:06:35,812 --> 00:06:37,188`

1. `助かります`
   - `ます`：`G0012`，礼貌体。

### E10-0104 `00:06:37,980 --> 00:06:41,234`

1. `ていうか 宇宙人の件も もっと早めに言ってもらえれば`
   - `ていうか`：`G0074`，话题转换。
   - `宇宙人の件`：`G0004`，名词修饰。
   - `件も`：`G0011`，也。
   - `もっと`：`G0044`，更加。
   - `早めに`：`G0086`，偏早一点。
   - `言ってもらえれば`：`G0141` 加 `G0081`，如果能让你早点告诉我。

### E10-0105 `00:06:41,359 --> 00:06:43,736`

1. `いつでも お風呂 入れるようにできたのに`
   - `いつでも`：`G0178`，任何时候都。
   - `入れる`：`G0069`，可能形。
   - `ように`：`G0164`，达到能入浴的状态。
   - `できた`：`G0069` 加 `G0001`，本来能够做到。
   - `のに`：`G0089`，遗憾/逆接，“明明可以”。

### E10-0106 `00:06:44,362 --> 00:06:45,780`

1. `いや ホント そうですよね`
   - `そうです`：`G0003` 加 `G0012`，认同。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0107 `00:06:45,863 --> 00:06:46,906`

- `ええ`：应答，跳过统计。

### E10-0108 `00:06:47,365 --> 00:06:51,828`

1. `確かに こんなに優しくて 信頼できる人なのだから`
   - `こんなに`：`G0167`，这种程度地。
   - `優しくて`：`G0035`，形容词て形连接。
   - `信頼できる`：`G0069`，能够信赖。
   - `人なのだから`：`G0066` 加 `G0088`，说明理由“因为是这样的人”。

### E10-0109 `00:06:51,911 --> 00:06:54,163`

1. `もっと早く言えばよかった`
   - `もっと`：`G0044`，更加。
   - `言えばよかった`：`G0257`，后悔“早点说就好了”。

### E10-0110 `00:06:55,248 --> 00:06:56,999`

1. `それはそうと`
   - `それはそうと`：`G0411`，先把前面话题放一边，转入新话题。

### E10-0111 `00:06:57,083 --> 00:07:01,254`

1. `さっきから支配人の首についている泡が気になる`
   - `さっきから`：`G0026`，从刚才开始。
   - `支配人の首`：`G0004`，名词修饰。
   - `ついている`：`G0030`，附着着的结果状态。
   - `泡が`：`G0010`，主语/焦点。
   - `気になる`：`G0172`，在意。

### E10-0112 `00:07:01,629 --> 00:07:06,384`

1. `言ってあげたいけど 今言うと 話の腰を折ってしまう`
   - `言ってあげたい`：`G0139` 加 `G0134`，想为对方指出来。
   - `けど`：`G0019`，转折铺垫。
   - `言うと`：`G0106`，一……就/如果说的话。
   - `話の腰`：`G0004`，名词修饰；`話の腰を折る` 为惯用搭配。
   - `折ってしまう`：`G0133`，不由得打断谈话。

### E10-0113 `00:07:06,926 --> 00:07:09,720`

1. `でも 気になる`
   - `気になる`：`G0172`，在意。

### E10-0114 `00:07:11,514 --> 00:07:12,890`

- `まあ でも…`：话语铺垫，跳过统计。

### E10-0115 `00:07:12,974 --> 00:07:17,103`

1. `広い意味で言えば 私たちも宇宙人ですけどね`
   - `意味で`：`G0008`，从某种意义上。
   - `言えば`：`G0081`，如果说/从……来说。
   - `私たちも`：`G0151` 加 `G0011`，我们也。
   - `ですけどね`：`G0012` 加 `G0019` 加 `G0034`，礼貌转折并缓和。

### E10-0116 `00:07:20,314 --> 00:07:22,525`

1. `そうですよね`
   - `そうです`：`G0003` 加 `G0012`，认同。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0117 `00:07:23,442 --> 00:07:26,946`

1. `もっと早く言えばよかった`
   - `もっと`：`G0044`，更加。
   - `言えばよかった`：`G0257`，后悔“早点说就好了”。

### E10-0118 `00:07:28,614 --> 00:07:29,824`

- `（エレベーターのベル）`：音效，跳过统计。

### E10-0119 `00:07:32,618 --> 00:07:34,162`

- `フゥ…`：叹息，跳过统计。

### E10-0120 `00:07:43,212 --> 00:07:44,672`

- `（ドアの開く音）`：音效，跳过统计。

### E10-0121 `00:07:49,218 --> 00:07:50,428`

- `（ドアの閉まる音）`：音效，跳过统计。

### E10-0122 `00:07:57,476 --> 00:07:58,603`

- `（ルームライトを消す音）`：音效，跳过统计。

### E10-0123 `00:08:01,355 --> 00:08:06,694`

1. `合意書の件は失敗に終わったが 諦めるのは まだ早い`
   - `合意書の件`：`G0004`，名词修饰。
   - `件は`：`G0013`，主题。
   - `失敗に終わった`：`G0407` 加 `G0001`，以失败告终。
   - `諦めるのは`：`G0097` 加 `G0013`，把“放弃”名词化作主题。
   - `まだ`：`G0224`，还/尚且。

### E10-0124 `00:08:06,903 --> 00:08:10,781`

1. `今の私には 心強い仲間が たくさんいる`
   - `今の私`：`G0004`，名词修饰。
   - `私には`：`G0017` 加 `G0013`，对现在的我来说/我这里。
   - `仲間が`：`G0010`，主语。
   - `いる`：`G0045`，有同伴存在。

### E10-0125 `00:08:11,532 --> 00:08:16,621`

1. `最初は 彼女たちの ずうずうしさと 口の軽さに あきれていたけど`
   - `最初は`：`G0013`，时间阶段作主题。
   - `彼女たち`：`G0151`，复数/群体后缀。
   - `彼女たちの`、`口の軽さ`：`G0004`，名词修饰。
   - `ずうずうしさと`：`G0016`，并列。
   - `口の軽さに`：`G0017`，感情对象。
   - `あきれていた`：`G0030` 加 `G0001`，过去持续的心理状态。
   - `けど`：`G0019`，转折。

### E10-0126 `00:08:16,704 --> 00:08:20,082`

1. `逆に それがあったから 私は助けら…`
   - `逆に`：`G0366`，反而。
   - `それが`：`G0010`，主语。
   - `あった`：`G0001`，存在的过去。
   - `から`：`G0088`，理由。
   - `私は`：`G0013`，主题。
   - `助けら…`：句子被打断，助けられた的未完成形式不计入统计。

### E10-0127 `00:08:32,845 --> 00:08:33,721`

- `いや…`：话语反应，跳过统计。

### E10-0128 `00:08:35,223 --> 00:08:37,475`

1. `今は それどころではない`
   - `今は`：`G0013`，当前时间作主题。
   - `それどころではない`：`G0408`，不是想那些事的时候/顾不上。

### E10-0129 `00:08:39,519 --> 00:08:41,646`

1. `怖がってるヒマはない`
   - `怖がってる`：`G0128` 加 `G0030`，表现出害怕的状态。
   - `ヒマは`：`G0013`，把“闲工夫”作为主题。
   - `ない`：`G0021`，没有。

### E10-0130 `00:08:46,776 --> 00:08:48,069`

- `（肩をたたく音）`：动作音，跳过统计。

### E10-0131 `00:08:55,451 --> 00:08:57,161`

- `（たたく音）`：动作音，跳过统计。

### E10-0132 `00:09:05,753 --> 00:09:07,255`

- `（たたく音）`：动作音，跳过统计。

### E10-0133 `00:09:07,338 --> 00:09:08,839`

1. `ちょ もう やめてくんないかな！`
   - `やめてくんない`：`G0090` 加 `G0021`，`やめてくれない` 的口语缩约，用否定疑问式请求对方停下。
   - `かな`：`G0099`，带烦躁感的自问/请求。

### E10-0134 `00:09:08,923 --> 00:09:11,342`

1. `今 それどこじゃないのよ！`
   - `それどこじゃない`：`G0408`，`それどころではない` 的口语缩约。
   - `のよ`：`G0066` 加 `G0083`，说明并强调。
2. `しかも あした 朝番だし！`
   - `しかも`：`G0312`，而且。
   - `だし`：`G0110`，补充理由。

### E10-0135 `00:09:11,968 --> 00:09:13,678`

1. `あなたは働いてないから いいかもしれないけど`
   - `あなたは`：`G0013`，主题。
   - `働いてない`：`G0030` 加 `G0021`，没有在工作。
   - `から`：`G0088`，理由。
   - `かもしれない`：`G0095`，也许。
   - `けど`：`G0019`，转折。

### E10-0136 `00:09:13,761 --> 00:09:15,471`

1. `こっちは生活 懸かってんのよ！`
   - `こっちは`：`G0013`，把自己这边作为对比主题。
   - `懸かってんのよ`：`G0030` 加 `G0066` 加 `G0083`，`懸かっているのよ` 的口语缩约，强调生活攸关。

### E10-0137 `00:09:15,554 --> 00:09:17,139`

1. `ていうか 俺 宇宙人だからね！`
   - `ていうか`：`G0074`，话题转换/吐槽。
   - `だから`：`G0088`，理由说明。
   - `ね`：`G0034`，提醒。

### E10-0138 `00:09:25,481 --> 00:09:28,693`

1. `そもそも 棚が違う`
   - `そもそも`：`G0215`，说到底/本来。
   - `棚が`：`G0010`，主语。

### E10-0139 `00:09:31,779 --> 00:09:36,284`

1. `市長選のときは ホントに ありがとうございました`
   - `市長選のとき`：`G0004` 加 `G0105`，市长选举的时候。
   - `ときは`：`G0013`，时间作话题。

### E10-0140 `00:09:37,243 --> 00:09:40,329`

1. `多目的施設の件`
   - `多目的施設の件`：`G0004`，名词修饰；句子延续。

### E10-0141 `00:09:40,413 --> 00:09:43,332`

1. `正式に浅ノ建設さんでお願いしたいと思ってますので`
   - `浅ノ建設さんで`：`G0008`，指定委托对象/承接方。
   - `お願いしたい`：`G0046` 加 `G0134`，礼貌表达希望委托。
   - `と思ってます`：`G0023` 加 `G0030` 加 `G0012`，礼貌说明自己的意向。
   - `ので`：`G0052`，原因/说明。

### E10-0142 `00:09:43,416 --> 00:09:46,043`

- `うん はいはい`：应答，跳过统计。

### E10-0143 `00:09:46,127 --> 00:09:49,422`

1. `今後も いい関係で やらせていただければと思ってます`
   - `今後も`：`G0011`，以后也。
   - `いい関係で`：`G0008`，以良好关系的状态。
   - `やらせていただければ`：`G0266` 加 `G0081`，请允许我方继续合作的话。
   - `と思ってます`：`G0023` 加 `G0030` 加 `G0012`，礼貌说明意向。

### E10-0144 `00:09:49,505 --> 00:09:51,924`

1. `もちろんです`
   - `です`：`G0012`，丁寧体。

### E10-0145 `00:09:52,008 --> 00:09:54,844`

1. `失礼しま～す`
   - `します`：`G0012`，礼貌结束电话。

### E10-0146 `00:09:57,638 --> 00:09:58,806`

- `（ため息）`：音效，跳过统计。

### E10-0147 `00:09:59,807 --> 00:10:00,891`

- `（ボタンを押す音）`：动作音，跳过统计。

### E10-0148 `00:10:03,936 --> 00:10:06,314`

1. `意見書なんだけどさ`
   - `なんだけどさ`：`G0066` 加 `G0019` 加 `G0131`，提出话题并缓和。

### E10-0149 `00:10:06,397 --> 00:10:08,774`

1. `これ ちょっと 目 通しといてもらえる？`
   - `通しといて`：`G0113`，`通しておいて` 的口语缩约。
   - `もらえる`：`G0141` 加 `G0069`，能请对方帮忙吗。

### E10-0150 `00:10:08,858 --> 00:10:11,944`

1. `それで 適当に返事 書いてさ`
   - `それで`：`G0067`，然后。
   - `書いて`：`G0035`，て形连接。
   - `さ`：`G0131`，口语缓和。

### E10-0151 `00:10:12,028 --> 00:10:14,238`

1. `市長名義で送っといてもらえる？`
   - `市長名義で`：`G0008`，以市长名义的方式/状态。
   - `送っといて`：`G0113`，`送っておいて` 的口语缩约。
   - `もらえる`：`G0141` 加 `G0069`，请对方帮忙。

### E10-0152 `00:10:14,447 --> 00:10:17,450`

1. `お願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0153 `00:10:18,326 --> 00:10:19,535`

- `（受話器を置く音）`：动作音，跳过统计。

### E10-0154 `00:10:20,077 --> 00:10:21,078`

- `（ため息）`：音效，跳过统计。

### E10-0155 `00:10:21,162 --> 00:10:22,997`

1. `あと 証拠になりそうなものって`
   - `証拠に`：`G0017`，变化结果。
   - `なりそう`：`G0048` 加 `G0032`，看起来会成为证据。
   - `ものって`：`G0064`，把“可能成为证据的东西”提题；句子延续。

### E10-0156 `00:10:23,080 --> 00:10:24,165`

1. `何があるかな？`
   - `何が`：`G0010`，疑问焦点。
   - `ある`：`G0075`，存在。
   - `かな`：`G0099`，自问。

### E10-0157 `00:10:24,248 --> 00:10:28,377`

1. `あとは もう 賄賂を渡す瞬間の写真とかかな`
   - `賄賂を`：`G0031`，动作对象。
   - `渡す瞬間`：`G0007`，连体修饰名词。
   - `瞬間の写真`：`G0004`，名词修饰。
   - `写真とか`：`G0063`，举例。
   - `かな`：`G0099`，推测。

### E10-0158 `00:10:28,461 --> 00:10:30,629`

1. `あ～ まあ そうなるよねえ`
   - `そうなる`：`G0003` 加 `G0048`，会变成那样/会那样。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0159 `00:10:30,713 --> 00:10:31,589`

- `うん`：应答，跳过统计。

### E10-0160 `00:10:31,672 --> 00:10:34,216`

1. `ただ それだけだと ちょっと弱いかもね`
   - `それだけ`：`G0039`，只凭那一点。
   - `だと`：`G0106`，如果是那样。
   - `かも`：`G0095`，也许。
   - `ね`：`G0034`，缓和判断。

### E10-0161 `00:10:34,300 --> 00:10:35,509`

1. `えっ そうなの？`
   - `そうなの`：`G0003` 加 `G0066`，说明式确认。

### E10-0162 `00:10:35,593 --> 00:10:37,511`

1. `うん だから そのときに`
   - `だから`：`G0076`，所以。
   - `そのときに`：`G0105` 加 `G0017`，在那个时候。

### E10-0163 `00:10:37,595 --> 00:10:39,722`

1. `市長が こういう不正を やってますみたいな`
   - `市長が`：`G0010`，主语。
   - `こういう`：`G0080`，这种。
   - `不正を`：`G0031`，动作对象。
   - `やってます`：`G0030` 加 `G0012`，正在做。
   - `みたいな`：`G0091`，像这样的；句子延续。

### E10-0164 `00:10:39,805 --> 00:10:41,891`

1. `関係者からの証言があるといいかも`
   - `関係者からの証言`：`G0026` 加 `G0004`，来自相关人士的证言。
   - `証言が`：`G0010`，主语。
   - `ある`：`G0075`，存在。
   - `あるといい`：`G0360`，要是有就好。
   - `かも`：`G0095`，也许。

### E10-0165 `00:10:41,974 --> 00:10:45,102`

1. `へえ～ なるほど 証言ね`
   - `ね`：`G0034`，确认/接受新信息。

### E10-0166 `00:10:45,186 --> 00:10:47,104`

1. `それは うちの旦那にやってもらおう`
   - `それは`：`G0013`，那件事作为话题。
   - `旦那に`：`G0017`，动作承担者/委托对象。
   - `やってもらおう`：`G0141` 加 `G0201`，打算让丈夫来做。

### E10-0167 `00:10:47,188 --> 00:10:49,482`

1. `そうだね`
   - `そうだね`：`G0003` 加 `G0012` 加 `G0034`，认同。
2. `お願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0168 `00:10:49,649 --> 00:10:50,900`

1. `じゃ あとは写真だね`
   - `じゃ`：`G0056`，承接。
   - `写真だね`：`G0012` 加 `G0034`，断定并确认。

### E10-0169 `00:10:50,983 --> 00:10:52,568`

1. `うん そうだね`
   - `そうだね`：`G0003` 加 `G0012` 加 `G0034`，认同。

### E10-0170 `00:10:52,652 --> 00:10:54,445`

- `ありがとう`：感谢语，跳过统计。

### E10-0171 `00:10:54,528 --> 00:10:55,988`

1. `また何かあったら連絡して`
   - `何か`：`G0096`，不定表达。
   - `あったら`：`G0001` 加 `G0054`，如果有什么事。
   - `連絡して`：`G0228`，句末て形轻请求。

### E10-0172 `00:10:56,072 --> 00:10:57,573`

1. `うん ありがとう　じゃあね`
   - `じゃあ`：`G0056`，告别承接。
   - `ね`：`G0034`，柔和收尾。

### E10-0173 `00:10:57,657 --> 00:11:00,159`

- `ありがとうございま～す`：感谢语，跳过统计。

### E10-0174 `00:11:00,242 --> 00:11:01,285`

- `はい`：应答，跳过统计。

### E10-0175 `00:11:02,078 --> 00:11:03,871`

1. `ハァ… 写真か`
   - `か`：`G0002`，自问/确认。

### E10-0176 `00:11:03,954 --> 00:11:05,706`

1. `いや それが いちばん難しいよね`
   - `それが`：`G0010`，焦点主语。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0177 `00:11:05,790 --> 00:11:07,917`

1. `そもそも いつ どこで渡すのかが 分かんないですからね`
   - `そもそも`：`G0215`，说到底。
   - `どこで`：`G0008`，地点疑问。
   - `渡すのか`：`G0072`，嵌入疑问。
   - `のかが`：`G0010`，把嵌入疑问作主语。
   - `分かんないです`：`G0021` 加 `G0012`，`分からないです` 的口语缩约。
   - `からね`：`G0088` 加 `G0034`，说明理由并求共感。

### E10-0178 `00:11:08,000 --> 00:11:09,418`

1. `そうなんだよね…`
   - `そうなんだ`：`G0003` 加 `G0066`，说明式认同。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0179 `00:11:09,502 --> 00:11:11,253`

1. `でも そういうのって 大体 料亭じゃない？`
   - `そういうの`：`G0080` 加 `G0097`，那种事。
   - `って`：`G0064`，口语提题。
   - `じゃない`：`G0098`，确认反问。

### E10-0180 `00:11:11,337 --> 00:11:12,922`

1. `そのイメージあるね`
   - `ある`：`G0075`，存在/拥有印象。
   - `ね`：`G0034`，共感。

### E10-0181 `00:11:13,005 --> 00:11:13,923`

1. `そこの“こまち”とか`
   - `とか`：`G0063`，举例。

### E10-0182 `00:11:14,006 --> 00:11:16,509`

1. `前 入っていきましたよね`
   - `前`：`G0058`，以前。
   - `入っていきました`：`G0085` 加 `G0001` 加 `G0012`，进入那边去了。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0183 `00:11:16,592 --> 00:11:18,344`

1. `絶対 こまちだよ`
   - `だよ`：`G0012` 加 `G0083`，断定并提醒。

### E10-0184 `00:11:18,427 --> 00:11:21,347`

1. `じゃ あとは 日時さえ分かれば`
   - `じゃ`：`G0056`，承接方案。
   - `日時さえ分かれば`：`G0410` 加 `G0081`，只要知道时间日期。

### E10-0185 `00:11:21,430 --> 00:11:23,599`

1. `その時間に お店に入って`
   - `その時間に`：`G0017`，指定时间。
   - `お店に`：`G0017`，进入的目标地点。
   - `入って`：`G0035`，て形连接。

### E10-0186 `00:11:23,682 --> 00:11:26,560`

1. `ふすまの隙間から こっそり 写真 撮る？`
   - `ふすまの隙間`：`G0004`，名词修饰。
   - `隙間から`：`G0026`，视线/拍摄起点。
   - `撮る？`：`G0002`，提议式疑问。

### E10-0187 `00:11:26,644 --> 00:11:28,312`

1. `まあ そうなりますね`
   - `そうなります`：`G0003` 加 `G0048` 加 `G0012`，礼貌说明“会变成那样”。
   - `ね`：`G0034`，确认。

### E10-0188 `00:11:28,396 --> 00:11:29,397`

1. `そうだよね…`
   - `そうだ`：`G0003` 加 `G0012`，认同。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0189 `00:11:29,480 --> 00:11:32,066`

1. `あそこ 高そうだよね`
   - `高そう`：`G0032`，看起来贵。
   - `だよね`：`G0012` 加 `G0083` 加 `G0034`，确认。
2. `高そう`
   - `高そう`：`G0032`，样态。

### E10-0190 `00:11:32,149 --> 00:11:33,859`

1. `こまち 高いよ`
   - `よ`：`G0083`，告知。
2. `やっぱり？`
   - 省略确认短句，跳过统计。

### E10-0191 `00:11:33,943 --> 00:11:35,277`

1. `飲み物も高い`
   - `飲み物も`：`G0011`，饮料也。

### E10-0192 `00:11:35,361 --> 00:11:37,863`

1. `コースだと １人１万くらいすると思うよ`
   - `コースだと`：`G0106`，如果是套餐。
   - `１万くらい`：`G0027`，大约一万。
   - `すると思う`：`G0023`，认为/估计要这么多。
   - `よ`：`G0083`，告知。

### E10-0193 `00:11:37,947 --> 00:11:39,657`

- `たかっ！ / でもね すっごいおいしい`：评价短句，跳过统计。

### E10-0194 `00:11:39,740 --> 00:11:40,991`

1. `えっ そうなんだ`
   - `そうなんだ`：`G0003` 加 `G0066`，理解/接受说明。

### E10-0195 `00:11:41,742 --> 00:11:43,077`

1. `ああいうお店ってさ`
   - `ああいう`：`G0080`，那种。
   - `って`：`G0064`，口语提题。
   - `さ`：`G0131`，口语提示。

### E10-0196 `00:11:43,160 --> 00:11:44,787`

1. `ソフトドリンクだけとかじゃ ダメかな？`
   - `だけ`：`G0039`，限定“只点软饮”。
   - `とか`：`G0063`，举例/模糊化方案。
   - `かな`：`G0099`，自问式征询。

### E10-0197 `00:11:44,870 --> 00:11:46,372`

1. `いや さすがに それは気まずくないですか？`
   - `さすがに`：`G0213`，再怎么说。
   - `それは`：`G0013`，把该做法作话题。
   - `気まずくないですか`：`G0098` 加 `G0012` 加 `G0002`，否定疑问式确认。

### E10-0198 `00:11:46,455 --> 00:11:47,373`

1. `ダメかぁ`
   - `か`：`G0002`，接受式确认。

### E10-0199 `00:11:47,456 --> 00:11:48,499`

- `あっ…`：反应语，跳过统计。

### E10-0200 `00:11:49,333 --> 00:11:50,501`

1. `何ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

## 第一批复盘（0001-0200）

- 本批从 E09 潜入失败后的撤退开始，推进到重新寻找贿赂证据的方案讨论。
- 新增或补登记 `G0407-G0411`；其中 `何から何まで` 和 `Nさえ〜ば` 分别回填到 E03、E09 的首见位置。
- 既有条目扩展：`G0090` 覆盖 `やめてくんない`，`G0266` 覆盖 `させてください/やらせていただければ`。
- 高频复习项包括 `ておく/とく`、`てもらう/もらえる`、`じゃない？`、`そうだよね`、`と思ってます`、`ば形`、`かな/かね` 和服务业礼貌表达。
- 下一批从 `E10-0201` 开始。

## 第二批新文法总览（0201-0400）

本批新增 `G0412 AかBか：是A还是B/二选一`。同时扩展既有 `G0079 たりする` 覆盖否定形式 `Vたりしない`，如 `糸 出たりしないんですか`。

| 编号 | 文法 | 首次出现 | 本批次数 |
|---|---|---|---:|
| G0412 | AかBか：是A还是B/二选一 | E10-0242-1 | 1 |

## 逐句解析（0201-0400）

### E10-0201 `00:11:50,584 --> 00:11:54,797`

1. `そういえば この前 オーナーの部屋で隠れてたとき`
   - `そういえば`：`G0059`，说起来，唤起相关记忆。
   - `この前`：`G0058`，以前/前些时候。
   - `オーナーの部屋`：`G0004`，名词修饰。
   - `部屋で`：`G0008`，动作发生地点。
   - `隠れてた`：`G0030` 加 `G0001`，`隠れていた` 的口语缩约，表示当时处在躲藏状态。
   - `とき`：`G0105`，……的时候。

### E10-0202 `00:11:54,922 --> 00:11:56,924`

1. `ちょうど オーナー 電話してたんだけどさ`
   - `電話してた`：`G0029` 加 `G0030` 加 `G0001`，`電話していた` 的口语缩约。
   - `んだけど`：`G0066` 加 `G0019`，说明背景并铺垫后文。
   - `さ`：`G0131`，口语提示。

### E10-0203 `00:11:57,007 --> 00:12:00,469`

1. `その相手って 今思えば 梅本さんだったかも`
   - `相手って`：`G0064`，口语提题。
   - `今思えば`：`G0081`，如果现在回想起来。
   - `梅本さんだった`：`G0001`，过去判断。
   - `かも`：`G0095`，也许/可能。

### E10-0204 `00:12:00,553 --> 00:12:01,846`

1. `そうなんですか？`
   - `そうなんですか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，礼貌地确认新信息。

### E10-0205 `00:12:01,929 --> 00:12:06,225`

1. `俺 下の警備員の動きを探るために 聴覚 上げてたから`
   - `下の警備員の動き`：`G0004`，名词连锁修饰。
   - `動きを`：`G0031`，动作对象。
   - `探るために`：`G0028`，为了探查。
   - `上げてた`：`G0030` 加 `G0001`，`上げていた` 的口语缩约。
   - `から`：`G0088`，说明理由。

### E10-0206 `00:12:06,308 --> 00:12:08,769`

1. `電話の声も耳に入ってたんだけど`
   - `電話の声`：`G0004`，电话里的声音。
   - `声も`：`G0011`，声音也。
   - `耳に`：`G0017`，进入的目标。
   - `入ってた`：`G0030` 加 `G0001`，处于听到/进入耳中的状态。
   - `んだけど`：`G0066` 加 `G0019`，说明背景并转折。

### E10-0207 `00:12:08,853 --> 00:12:11,021`

1. `確か 相手 女性だったんだよね`
   - `だった`：`G0001`，过去判断。
   - `んだ`：`G0066`，说明语气。
   - `よね`：`G0083` 加 `G0034`，带确认感的说明。

### E10-0208 `00:12:11,105 --> 00:12:12,356`

1. `なに話してたんですか？`
   - `なに`：`G0061`，疑问内容。
   - `話してた`：`G0030` 加 `G0001`，`話していた` 的口语缩约。
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌询问说明。

### E10-0209 `00:12:13,149 --> 00:12:15,901`

1. `意識は警備員にいってたから`
   - `意識は`：`G0013`，把注意力作为话题。
   - `警備員に`：`G0017`，意识指向的对象。
   - `いってた`：`G0030` 加 `G0001`，注意力当时转向警备员。
   - `から`：`G0088`，理由。

### E10-0210 `00:12:15,985 --> 00:12:17,820`

1. `ちょっと 内容まではっきり覚えてないんだけど`
   - `内容まで`：`G0014`，到内容这一层面。
   - `覚えてない`：`G0030` 加 `G0021`，`覚えていない` 的口语缩约。
   - `んだけど`：`G0066` 加 `G0019`，解释并铺垫。

### E10-0211 `00:12:18,904 --> 00:12:22,032`

1. `能力を使ったらいいんじゃないですか？`
   - `能力を`：`G0031`，动作对象。
   - `使ったら`：`G0001` 加 `G0054`，如果使用能力。
   - `たらいい`：`G0082`，建议“用一下不就好吗”。
   - `んじゃないですか`：`G0098` 加 `G0012` 加 `G0002`，委婉建议/确认。

### E10-0212 `00:12:22,116 --> 00:12:23,325`

- `能力？`：复述确认短句，跳过统计。

### E10-0213 `00:12:23,409 --> 00:12:25,202`

1. `それって 多分`
   - `それって`：`G0064`，口语提题；句子延续。

### E10-0214 `00:12:25,286 --> 00:12:29,290`

1. `記憶の片隅にはあるけど 思い出せないだけじゃないですか`
   - `記憶の片隅`：`G0004`，记忆的一角。
   - `片隅には`：`G0013`，带对比的主题。
   - `ある`：`G0075`，存在。
   - `けど`：`G0019`，转折。
   - `思い出せない`：`G0069` 加 `G0021`，可能否定，“想不起来”。
   - `だけ`：`G0039`，只是。
   - `じゃないですか`：`G0098` 加 `G0012` 加 `G0002`，确认式推测。

### E10-0215 `00:12:29,373 --> 00:12:30,583`

1. `まあ そうだと思うけど`
   - `そうだ`：`G0003` 加 `G0012`，那样。
   - `と思う`：`G0023`，认为。
   - `けど`：`G0019`，转折/保留。

### E10-0216 `00:12:30,666 --> 00:12:33,711`

1. `だったら 記憶力を上げたらいいんじゃないですか？`
   - `だったら`：`G0054`，既然那样的话。
   - `記憶力を`：`G0031`，动作对象。
   - `上げたらいい`：`G0001` 加 `G0082`，建议“提高记忆力就好”。
   - `んじゃないですか`：`G0098` 加 `G0012` 加 `G0002`，委婉建议。

### E10-0217 `00:12:33,794 --> 00:12:34,712`

- `記憶力？`：复述确认，跳过统计。

### E10-0218 `00:12:34,795 --> 00:12:36,797`

1. `耳では聞いてたんですもんね？`
   - `耳では`：`G0008` 加 `G0013`，用耳朵这一点作对比。
   - `聞いてた`：`G0030` 加 `G0001`，当时听到了。
   - `んです`：`G0066` 加 `G0012`，说明语气。
   - `もんね`：`G0150` 加 `G0034`，以“毕竟……”求认同。

### E10-0219 `00:12:36,881 --> 00:12:38,132`

1. `まあ 聞いてたけど`
   - `聞いてた`：`G0030` 加 `G0001`，过去持续/状态。
   - `けど`：`G0019`，转折保留。

### E10-0220 `00:12:38,215 --> 00:12:39,425`

1. `そんなこともできんの？`
   - `そんな`：`G0167`，那样的。
   - `ことも`：`G0097` 加 `G0011`，那种事也。
   - `できんの`：`G0069` 加 `G0066`，`できるの` 的口语音变，询问能力。

### E10-0221 `00:12:39,508 --> 00:12:41,594`

1. `それで山梨大 合格してるから`
   - `それで`：`G0067`，凭那个/因此。
   - `合格してる`：`G0029` 加 `G0030`，合格过/处于合格事实。
   - `から`：`G0088`，说明理由。

### E10-0222 `00:12:41,677 --> 00:12:43,387`

- `医学部…`：名词补充，跳过统计。

### E10-0223 `00:12:43,471 --> 00:12:45,556`

1. `何でもできるんですね`
   - `何でも`：`G0109`，任何事都可以/都行。
   - `できる`：`G0069`，可能表达。
   - `んです`：`G0066` 加 `G0012`，说明性理解。
   - `ね`：`G0034`，感叹确认。

### E10-0224 `00:12:45,639 --> 00:12:47,933`

1. `全然 そんな大した あれじゃないです`
   - `全然`：`G0162`，现代口语中加强否定评价。
   - `そんな`：`G0167`，那样的。
   - `じゃないです`：`G0043` 加 `G0012`，并不是。

### E10-0225 `00:12:48,309 --> 00:12:50,019`

1. `じゃ 能力のほう お願いします`
   - `じゃ`：`G0056`，承接。
   - `能力のほう`：`G0042`，服务/会话中柔化地指“能力这边”。
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0226 `00:12:50,102 --> 00:12:52,021`

1. `お願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0227 `00:12:52,938 --> 00:12:54,815`

- `いやぁ…`：犹豫/叹息，跳过统计。

### E10-0228 `00:12:55,483 --> 00:12:56,775`

1. `何ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0229 `00:12:57,651 --> 00:12:59,612`

1. `それって頭脳系の能力だよね？`
   - `それって`：`G0064`，口语提题。
   - `頭脳系の能力`：`G0004`，名词修饰。
   - `だよね`：`G0012` 加 `G0083` 加 `G0034`，确认。

### E10-0230 `00:12:59,695 --> 00:13:00,779`

- `はい`：应答，跳过统计。

### E10-0231 `00:13:02,865 --> 00:13:04,283`

1. `あっ そっか`
   - `そっか`：`G0003`，理解/想起来。

### E10-0232 `00:13:04,366 --> 00:13:05,743`

1. `そうなのよ`
   - `そうなの`：`G0003` 加 `G0066`，解释性认同。
   - `よ`：`G0083`，提示。
2. `えっ なに？`
   - `なに`：`G0061`，询问内容。

### E10-0233 `00:13:05,826 --> 00:13:07,620`

1. `ハゲるんだ？`
   - `んだ`：`G0066`，理解/确认说明。

### E10-0234 `00:13:07,703 --> 00:13:09,997`

1. `できれば ハゲは避けたいからさ`
   - `できれば`：`G0081`，如果可以的话。
   - `ハゲは`：`G0013`，把秃头作为回避对象话题。
   - `避けたい`：`G0134`，想避免。
   - `から`：`G0088`，理由。
   - `さ`：`G0131`，口语提示。

### E10-0235 `00:13:10,080 --> 00:13:10,915`

1. `どういうこと？`
   - `どういう`：`G0080`，什么样的/怎么回事。
   - `こと`：`G0097`，形式名词。

### E10-0236 `00:13:10,998 --> 00:13:14,543`

1. `頭脳系の能力 使うと 副作用でハゲんのよ`
   - `頭脳系の能力`：`G0004`，名词修饰。
   - `使うと`：`G0106`，一使用就/如果使用。
   - `副作用で`：`G0008`，原因。
   - `ハゲんの`：`G0066`，`ハゲるの` 的口语说明。
   - `よ`：`G0083`，告知。

### E10-0237 `00:13:14,835 --> 00:13:17,797`

1. `そうなんだ`
   - `そうなんだ`：`G0003` 加 `G0066`，理解说明。

### E10-0238 `00:13:17,880 --> 00:13:19,882`

1. `でも 今 そんなこと言ってられなくないですか？`
   - `でも`：`G0019`，转折。
   - `そんなこと`：`G0167` 加 `G0097`，那种事。
   - `言ってられなくないですか`：`G0397` 加 `G0098` 加 `G0012` 加 `G0002`，不能再说这种话了吧。

### E10-0239 `00:13:19,965 --> 00:13:21,550`

1. `ホテルがなくなったら`
   - `ホテルが`：`G0010`，主语。
   - `なくなったら`：`G0021` 加 `G0048` 加 `G0001` 加 `G0054`，如果酒店没了。

### E10-0240 `00:13:21,634 --> 00:13:23,511`

1. `高橋さん 死んじゃうかもしれないんですよね？`
   - `死んじゃう`：`G0133`，`死んでしまう` 的口语缩约，带严重后果感。
   - `かもしれない`：`G0095`，可能。
   - `んです`：`G0066` 加 `G0012`，说明语气。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0241 `00:13:23,594 --> 00:13:24,762`

1. `まあ そうなんだけどさ`
   - `そうなんだ`：`G0003` 加 `G0066`，承认说明。
   - `けど`：`G0019`，转折保留。
   - `さ`：`G0131`，口语提示。

### E10-0242 `00:13:24,845 --> 00:13:27,556`

1. `死ぬかハゲるかだったら ハゲるのほうが よくないですか？`
   - `死ぬかハゲるか`：`G0412`，二选一列举，“是死还是秃”。
   - `だったら`：`G0054`，如果是这种二选一条件。
   - `ハゲるの`：`G0097`，把动作名词化。
   - `ほうが`：`G0042`，比较中的一方。
   - `よくないですか`：`G0098` 加 `G0012` 加 `G0002`，反问式建议。

### E10-0243 `00:13:27,640 --> 00:13:29,058`

1. `そうよ それ 分かってんの`
   - `そう`：`G0003`，同意。
   - `分かってんの`：`G0030` 加 `G0066`，`分かっているの` 的口语缩约。

### E10-0244 `00:13:29,141 --> 00:13:30,851`

1. `分かってんだけど`
   - `分かってん`：`G0030` 加 `G0066`，`分かっているん` 的口语缩约。
   - `だけど`：`G0019`，转折。

### E10-0245 `00:13:31,227 --> 00:13:32,520`

- `ねえ…`：话语填充/犹豫，跳过统计。

### E10-0246 `00:13:33,687 --> 00:13:37,233`

1. `ちょっとだけ自力で思い出させてもらえる？`
   - `だけ`：`G0039`，限定一点点。
   - `自力で`：`G0008`，方式。
   - `思い出させてもらえる`：`G0266` 加 `G0141` 加 `G0069`，请求允许自己试着回忆。

### E10-0247 `00:13:37,316 --> 00:13:40,236`

1. `それができるなら いちばんですけど`
   - `それが`：`G0010`，主语。
   - `できるなら`：`G0069` 加 `G0108`，如果能做到。
   - `です`：`G0012`，礼貌断定。
   - `けど`：`G0019`，保留/转折。

### E10-0248 `00:13:40,319 --> 00:13:41,862`

1. `悪いけど ちょっと時間ちょうだい`
   - `けど`：`G0019`，前置铺垫。
   - `ちょうだい`：口语请求“给我”，本处作为轻请求短语不单列统计。

### E10-0249 `00:13:41,946 --> 00:13:42,780`

1. `ちょっと思い出すから`
   - `思い出すから`：`G0088`，说明理由/接下来要做的事。

### E10-0250 `00:13:42,863 --> 00:13:44,073`

1. `分かりました`
   - `分かりました`：`G0001` 加 `G0012`，礼貌过去形作应答，“明白了”。

### E10-0251 `00:13:44,156 --> 00:13:46,200`

- `え～っと…`：思考填充语，跳过统计。

### E10-0252 `00:13:52,665 --> 00:13:55,292`

1. `今さ ちょうど荷造りしててさ`
   - `さ`：`G0131`，口语提示。
   - `荷造りしてて`：`G0029` 加 `G0030` 加 `G0035`，正在打包并连接后文。

### E10-0253 `00:13:55,376 --> 00:13:56,335`

1. `来週 引っ越しなんだよね`
   - `引っ越しなんだ`：`G0066`，说明事实。
   - `よね`：`G0083` 加 `G0034`，柔和提示。

### E10-0254 `00:13:56,418 --> 00:13:57,962`

1. `そうなんだ 手伝いに行こっか？`
   - `そうなんだ`：`G0003` 加 `G0066`，理解。
   - `手伝いに`：`G0017`，目的。
   - `行こっか`：`G0201` 加 `G0002`，意向形提议。

### E10-0255 `00:13:58,045 --> 00:14:00,214`

1. `マジで？ 超助かる`
   - `で`：`G0008`，`マジで` 中的状态/方式副词化；口语强确认。

### E10-0256 `00:14:00,297 --> 00:14:01,257`

1. `じゃ 私も行くよ`
   - `じゃ`：`G0056`，承接。
   - `私も`：`G0011`，我也。
   - `よ`：`G0083`，告知。

### E10-0257 `00:14:01,340 --> 00:14:02,841`

- `ホント？ / うん`：确认和应答，跳过统计。

### E10-0258 `00:14:02,925 --> 00:14:03,801`

- `イエ～イ`：欢呼，跳过统计。

### E10-0259 `00:14:03,884 --> 00:14:06,178`

1. `運動会の打ち上げがあんの？`
   - `運動会の打ち上げ`：`G0004`，名词修饰。
   - `打ち上げが`：`G0010`，主语。
   - `あんの`：`G0075` 加 `G0066`，`あるの` 的口语音变。

### E10-0260 `00:14:06,262 --> 00:14:07,137`

1. `あります あります`
   - `あります`：`G0075` 加 `G0012`，存在的礼貌形。

### E10-0261 `00:14:07,221 --> 00:14:09,139`

1. `しかも カラオケで２次会とかありますよ`
   - `しかも`：`G0312`，而且。
   - `カラオケで`：`G0008`，地点/场合。
   - `とか`：`G0063`，举例。
   - `あります`：`G0075` 加 `G0012`，存在。
   - `よ`：`G0083`，告知。

### E10-0262 `00:14:09,223 --> 00:14:10,724`

1. `それ 当日でしょう？`
   - `それ`：`G0018`，省略谓语的名词句式话题。
   - `でしょう`：`G0094`，确认推量。

### E10-0263 `00:14:10,808 --> 00:14:11,892`

1. `タフだねえ`
   - `だ`：`G0012`，普通断定。
   - `ねえ`：`G0034`，感叹确认。

### E10-0264 `00:14:11,976 --> 00:14:14,144`

1. `児童が踊ったダンス曲を入れて`
   - `児童が`：`G0010`，主语。
   - `踊った`：`G0001`，过去/完成。
   - `踊ったダンス曲`：`G0007`，连体修饰。
   - `ダンス曲を`：`G0031`，动作对象。
   - `入れて`：`G0035`，て形连接。

### E10-0265 `00:14:14,228 --> 00:14:15,646`

1. `みんなで歌うんですけど`
   - `みんなで`：`G0008`，共同动作的主体范围。
   - `歌うんです`：`G0066` 加 `G0012`，说明活动内容。
   - `けど`：`G0019`，铺垫后文。

### E10-0266 `00:14:15,729 --> 00:14:17,856`

1. `担任は 前に出て そのダンス踊るんですよ`
   - `担任は`：`G0013`，主题。
   - `前に`：`G0017`，移动目标。
   - `出て`：`G0035`，て形连接。
   - `踊るんです`：`G0066` 加 `G0012`，说明规则。
   - `よ`：`G0083`，告知。

### E10-0267 `00:14:17,940 --> 00:14:20,317`

1. `はっちも踊んの？`
   - `はっちも`：`G0011`，也。
   - `踊んの`：`G0066`，`踊るの` 的口语音变，询问说明。

### E10-0268 `00:14:20,401 --> 00:14:23,237`

1. `踊ります 何なら運動会よりも全力で踊りますね`
   - `踊ります`：`G0012`，丁寧体。
   - `何なら`：`G0161`，甚至可以说。
   - `運動会よりも`：`G0152`，比较基准。
   - `全力で`：`G0008`，方式。
   - `ね`：`G0034`，说明中带共感。

### E10-0269 `00:14:23,320 --> 00:14:25,865`

1. `大変だね`
   - `だ`：`G0012`，断定。
   - `ね`：`G0034`，共感。

### E10-0270 `00:14:25,948 --> 00:14:28,659`

1. `そこに本番があんのね`
   - `そこに`：`G0017`，抽象位置。
   - `本番が`：`G0010`，主语。
   - `あんの`：`G0075` 加 `G0066`，`あるの` 的口语音变。
   - `ね`：`G0034`，确认。

### E10-0271 `00:14:29,869 --> 00:14:31,203`

- `高橋さん`：呼唤，跳过统计。

### E10-0272 `00:14:31,579 --> 00:14:32,413`

- `はい`：应答，跳过统计。

### E10-0273 `00:14:32,496 --> 00:14:35,416`

1. `一応 30分たちましたけど どうですか？`
   - `30分たちました`：`G0001` 加 `G0012`，时间已经过了 30 分钟。
   - `けど`：`G0019`，铺垫询问。
   - `どうですか`：`G0093` 加 `G0012` 加 `G0002`，询问状况。

### E10-0274 `00:14:36,542 --> 00:14:38,085`

1. `まだ ちょっと思い出せないね`
   - `まだ`：`G0104`，还没有。
   - `思い出せない`：`G0069` 加 `G0021`，可能否定。
   - `ね`：`G0034`，缓和说明。

### E10-0275 `00:14:38,586 --> 00:14:41,380`

1. `どうします？ もうちょっと自力でやります？`
   - `どうします`：`G0093` 加 `G0012`，询问打算。
   - `自力で`：`G0008`，方式。
   - `やります`：`G0012`，丁寧体。

### E10-0276 `00:14:43,299 --> 00:14:44,300`

1. `いや もういいよ`
   - `いいよ`：`G0083`，告知/决定。

### E10-0277 `00:14:45,426 --> 00:14:46,844`

1. `じゃ お願いします`
   - `じゃ`：`G0056`，承接。
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0278 `00:14:46,927 --> 00:14:48,721`

1. `お願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0279 `00:14:51,348 --> 00:14:52,600`

- `フゥ…`：呼气/准备动作，跳过统计。

### E10-0280 `00:14:57,521 --> 00:14:59,857`

1. `先ほど 本社への連絡は終了しました`
   - `本社への連絡`：`G0055`，联络对象。
   - `連絡は`：`G0013`，话题。
   - `終了しました`：`G0001` 加 `G0012`，礼貌过去/完成。

### E10-0281 `00:14:59,940 --> 00:15:01,567`

1. `電力が復旧したら セキュリティーの信号も…`
   - `電力が`：`G0010`，主语。
   - `復旧したら`：`G0001` 加 `G0054`，如果恢复供电。
   - `セキュリティーの信号`：`G0004`，名词修饰。
   - `信号も`：`G0011`，信号也；句子未完。

### E10-0282 `00:15:01,650 --> 00:15:03,777`

1. `引き続き よろしくお願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0283 `00:15:04,111 --> 00:15:07,156`

1. `もしもし？`
   - `もしもし`：电话开场寒暄，跳过统计。
2. `梅本です`
   - `です`：`G0012`，礼貌断定。

### E10-0284 `00:15:07,239 --> 00:15:08,657`

1. `お世話になっております`
   - `お世話になっております`：`G0087` 加 `G0030` 加 `G0012`，郑重电话寒暄。

### E10-0285 `00:15:08,741 --> 00:15:10,909`

1. `例の受け渡しの件なんですけど`
   - `例の`：双方都知道的“那件”，固定连体表达；本处不单列统计。
   - `受け渡しの件`：`G0004`，关于交接的事。
   - `なんですけど`：`G0066` 加 `G0012` 加 `G0019`，说明话题并铺垫。

### E10-0286 `00:15:10,993 --> 00:15:13,537`

1. `16日 いかがでしょうか？`
   - `いかがでしょうか`：`G0050` 加 `G0094` 加 `G0002`，礼貌询问是否合适。

### E10-0287 `00:15:13,621 --> 00:15:15,456`

1. `時間は 夜９時とか`
   - `時間は`：`G0013`，把时间作为话题。
   - `夜９時とか`：`G0063`，举例/提案中留余地。

### E10-0288 `00:15:15,539 --> 00:15:17,666`

1. `では その時間に`
   - `では`：`G0056`，承接“那么”。
   - `その時間に`：`G0017`，指定时间。

### E10-0289 `00:15:17,750 --> 00:15:21,003`

1. `じゃ 場所は セルフ富士浅田でいかがでしょう？`
   - `じゃ`：`G0056`，承接。
   - `場所は`：`G0013`，把地点作为话题。
   - `セルフ富士浅田で`：`G0008`，地点/场合。
   - `いかがでしょう`：`G0050` 加 `G0094`，礼貌提案。

### E10-0290 `00:15:22,254 --> 00:15:23,297`

1. `思い出した`
   - `思い出した`：`G0001`，想起来了。

### E10-0291 `00:15:24,340 --> 00:15:25,299`

1. `何ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0292 `00:15:25,382 --> 00:15:28,552`

1. `まず あの声は 梅本市長で間違いないよ`
   - `声は`：`G0013`，话题。
   - `梅本市長で`：`G0008`，判断内容/身份。
   - `間違いない`：`G0364`，肯定没错。
   - `よ`：`G0083`，告知。

### E10-0293 `00:15:28,636 --> 00:15:29,887`

- `やっぱり`：副词性反应，跳过统计。

### E10-0294 `00:15:29,970 --> 00:15:32,222`

1. `しかも“受け渡しの件”って はっきり言ってた`
   - `しかも`：`G0312`，而且。
   - `受け渡しの件`：`G0004`，名词修饰。
   - `って`：`G0064`，引用内容。
   - `言ってた`：`G0030` 加 `G0001`，`言っていた` 的口语缩约。

### E10-0295 `00:15:33,015 --> 00:15:34,391`

1. `で いつなんですか？`
   - `で`：`G0056`，承接“那么”。
   - `いつなんですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌询问时间。

### E10-0296 `00:15:34,475 --> 00:15:37,937`

1. `３月16日の午後９時 場所は セルフ富士浅田`
   - `３月16日の午後９時`：`G0004`，日期修饰时间。
   - `場所は`：`G0013`，地点作为话题；谓语省略。

### E10-0297 `00:15:39,605 --> 00:15:41,732`

- `セルフ富士浅田？`：复述确认，跳过统计。

### E10-0298 `00:15:41,815 --> 00:15:44,652`

1. `ガソリンスタンドですよね？`
   - `です`：`G0012`，礼貌断定。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0299 `00:15:46,195 --> 00:15:49,323`

1. `確かにガソリンスタンドで受け渡しをしたって事例はあるよ`
   - `ガソリンスタンドで`：`G0008`，地点。
   - `受け渡しを`：`G0031`，动作对象。
   - `した`：`G0001`，过去。
   - `って`：`G0064`，引用/说明内容。
   - `事例は`：`G0013`，话题。
   - `ある`：`G0075`，存在。
   - `よ`：`G0083`，告知。

### E10-0300 `00:15:49,406 --> 00:15:50,991`

1. `そうなんだ`
   - `そうなんだ`：`G0003` 加 `G0066`，理解说明。

### E10-0301 `00:15:51,075 --> 00:15:54,244`

1. `セルフのお店だと 店員も少ないし`
   - `セルフのお店`：`G0004`，名词修饰。
   - `だと`：`G0106`，如果是/说到。
   - `店員も`：`G0011`，店员也。
   - `し`：`G0110`，列举理由。

### E10-0302 `00:15:54,328 --> 00:15:56,997`

1. `車の中って どこのお店よりも個室だからさ`
   - `車の中`：`G0004`，车内。
   - `って`：`G0064`，口语提题。
   - `どこのお店よりも`：`G0152`，比较基准。
   - `だから`：`G0088`，说明理由。
   - `さ`：`G0131`，口语提示。

### E10-0303 `00:15:57,081 --> 00:15:59,083`

- `確かに`：应答性副词，跳过统计。

### E10-0304 `00:15:59,166 --> 00:16:01,710`

1. `場所と時間が分かってんなら`
   - `場所と時間`：`G0016`，并列。
   - `場所と時間が`：`G0010`，主语。
   - `分かってんなら`：`G0030` 加 `G0108`，`分かっているなら` 的口语缩约。

### E10-0305 `00:16:01,794 --> 00:16:03,712`

1. `警察に連絡すれば 捕まえてくれるんじゃないの？`
   - `警察に`：`G0017`，联络对象。
   - `連絡すれば`：`G0029` 加 `G0081`，如果联系。
   - `捕まえてくれる`：`G0090`，对我方有利地抓住。
   - `んじゃないの`：`G0098` 加 `G0066`，确认式推测。

### E10-0306 `00:16:03,796 --> 00:16:05,631`

1. `うちらが行くより`
   - `うちらが`：`G0010`，主语。
   - `行くより`：`G0152`，比较基准；句子延续。

### E10-0307 `00:16:05,714 --> 00:16:07,424`

1. `そっちのほうが早そうですよね`
   - `そっちのほう`：`G0042`，比较中的一方。
   - `早そう`：`G0032`，看起来更快。
   - `です`：`G0012`，礼貌断定。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0308 `00:16:07,508 --> 00:16:09,927`

1. `多分 タレコミだけでは`
   - `だけでは`：`G0039` 加 `G0013`，限定“仅靠举报”这一条件；句子延续。

### E10-0309 `00:16:10,010 --> 00:16:11,512`

1. `警察は動いてくれないと思うな`
   - `警察は`：`G0013`，话题。
   - `動いてくれない`：`G0090` 加 `G0021`，不会为我们采取行动。
   - `と思う`：`G0023`，认为。
   - `な`：`G0139`，自言/判断语气。

### E10-0310 `00:16:12,012 --> 00:16:14,264`

1. `そうなんだ`
   - `そうなんだ`：`G0003` 加 `G0066`，理解说明。

### E10-0311 `00:16:14,598 --> 00:16:16,558`

1. `ミノケン ありがとね`
   - `ね`：`G0034`，亲近柔和。

### E10-0312 `00:16:16,642 --> 00:16:18,185`

- `いえいえ / ありがとう`：寒暄应答，跳过统计。

### E10-0313 `00:16:18,268 --> 00:16:20,479`

1. `それ ミノケン？`
   - `それ`：`G0018`，省略“是……吗”的名词句。

### E10-0314 `00:16:20,980 --> 00:16:22,064`

- `ミノケ～ン！`：呼唤，跳过统计。

### E10-0315 `00:16:23,273 --> 00:16:26,193`

1. `武田直美 覚えてる？`
   - `覚えてる`：`G0030`，`覚えている` 的口语缩约。

### E10-0316 `00:16:26,276 --> 00:16:28,779`

1. `武田？ 久しぶり！`
   - `久しぶり`：`G0146`，隔了很久。

### E10-0317 `00:16:28,862 --> 00:16:31,990`

1. `今度さ みんなと飲もうよ`
   - `さ`：`G0131`，口语提示。
   - `みんなと`：`G0016`，共同对象。
   - `飲もう`：`G0201`，意向形提议。
   - `よ`：`G0083`，提议推动。

### E10-0318 `00:16:32,074 --> 00:16:33,450`

1. `飲もう 飲もう`
   - `飲もう`：`G0201`，意向形应和提议。

### E10-0319 `00:16:33,534 --> 00:16:35,244`

1. `じゃ またね`
   - `じゃ`：`G0056`，告别承接。
   - `ね`：`G0034`，柔和收尾。

### E10-0320 `00:16:35,327 --> 00:16:37,413`

1. `じゃ また連絡するね`
   - `じゃ`：`G0056`，承接。
   - `連絡する`：`G0029`，名词动作化。
   - `ね`：`G0034`，柔和告知。

### E10-0321 `00:16:37,496 --> 00:16:39,123`

- `おごってよ / バイバイ`：请求/告别短句，跳过统计。

### E10-0322 `00:16:39,206 --> 00:16:41,625`

- `バイバ～イ`：告别，跳过统计。

### E10-0323 `00:16:41,709 --> 00:16:43,419`

1. `じゃ やっぱり 写真は自分たちで`
   - `じゃ`：`G0056`，承接结论。
   - `写真は`：`G0013`，话题。
   - `自分たちで`：`G0008`，动作主体/方式；句子延续。

### E10-0324 `00:16:43,502 --> 00:16:44,878`

1. `撮らなきゃいけないってことですよね`
   - `撮らなきゃいけない`：`G0155`，必须拍。
   - `ってこと`：`G0190`，也就是说。
   - `です`：`G0012`，礼貌断定。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0325 `00:16:44,962 --> 00:16:46,797`

1. `そうだねえ`
   - `そうだ`：`G0003` 加 `G0012`，认同。
   - `ねえ`：`G0034`，共感。

### E10-0326 `00:16:46,880 --> 00:16:48,841`

1. `高橋さんって`
   - `って`：`G0064`，口语提题；句子延续。

### E10-0327 `00:16:48,924 --> 00:16:51,719`

1. `ここら辺から 糸 出たりしないんですか？`
   - `ここら辺から`：`G0026`，从这附近。
   - `出たりしない`：`G0079` 加 `G0021`，举例式试探“不会冒出丝之类的吗”。
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌询问。

### E10-0328 `00:16:51,802 --> 00:16:52,678`

- `糸？`：复述确认，跳过统计。

### E10-0329 `00:16:52,761 --> 00:16:53,762`

1. `スパイダーマンみたいに？`
   - `みたいに`：`G0091`，像蜘蛛侠那样。

### E10-0330 `00:16:53,846 --> 00:16:54,722`

- `そうそう`：应答，跳过统计。

### E10-0331 `00:16:54,805 --> 00:16:57,766`

1. `昨日 ちょうど テレビで「スパイダーマン」見てたら`
   - `テレビで`：`G0008`，媒介/场景。
   - `見てたら`：`G0030` 加 `G0001` 加 `G0054`，看着的时候/如果看着。

### E10-0332 `00:16:57,850 --> 00:16:59,476`

1. `りつが “高橋さん 高橋さん”っつって`
   - `りつが`：`G0010`，主语。
   - `っつって`：`G0060`，`と言って` 的粗口语缩约。

### E10-0333 `00:16:59,560 --> 00:17:01,186`

- `りっちゃん かわいい！`：评价短句，跳过统计。

### E10-0334 `00:17:01,270 --> 00:17:03,564`

1. `スパイダーマンも高速で移動するしね`
   - `スパイダーマンも`：`G0011`，也。
   - `高速で`：`G0008`，方式。
   - `移動する`：`G0029`，名词动作化。
   - `し`：`G0110`，补充理由。
   - `ね`：`G0034`，共感。

### E10-0335 `00:17:03,647 --> 00:17:05,274`

1. `だから 高橋さんも`
   - `だから`：`G0076`，所以。
   - `高橋さんも`：`G0011`，高桥也；句子延续。

### E10-0336 `00:17:05,357 --> 00:17:08,068`

1. `何か この辺から 出てこないかなぁと思って`
   - `何か`：`G0096`，某种东西。
   - `この辺から`：`G0026`，从这一带。
   - `出てこない`：`G0024` 加 `G0021`，不会冒出来。
   - `かな`：`G0099`，自问。
   - `と思って`：`G0023` 加 `G0035`，引用自己的想法并连接后文。

### E10-0337 `00:17:08,152 --> 00:17:09,611`

1. `そしたら 受け渡しのときさ`
   - `そしたら`：`G0054`，那样的话。
   - `受け渡しのとき`：`G0004` 加 `G0105`，交接的时候。
   - `さ`：`G0131`，口语提示。

### E10-0338 `00:17:09,695 --> 00:17:11,947`

1. `２人まとめて 糸でグルグル巻きにできるじゃん`
   - `糸で`：`G0008`，工具。
   - `できる`：`G0069`，可能表达。
   - `じゃん`：`G0092`，口语确认/提醒。

### E10-0339 `00:17:12,030 --> 00:17:13,490`

1. `確かに いけます？`
   - `いけます`：`G0069` 加 `G0012`，能行吗。

### E10-0340 `00:17:13,574 --> 00:17:15,367`

1. `ムリだよ`
   - `だよ`：`G0012` 加 `G0083`，断定告知。
2. `ですよね`
   - `ですよね`：`G0012` 加 `G0083` 加 `G0034`，确认。

### E10-0341 `00:17:15,451 --> 00:17:16,452`

1. `ムリか`
   - `か`：`G0002`，接受/确认。

### E10-0342 `00:17:16,535 --> 00:17:19,288`

1. `私 そろそろ仕事なんで行くね`
   - `そろそろ`：差不多该……，时间副词短注，不单列统计。
   - `仕事なんで`：`G0066` 加 `G0052`，因为要上班。
   - `行くね`：`G0034`，柔和告别。

### E10-0343 `00:17:19,371 --> 00:17:20,664`

- `またね`：告别，跳过统计。

### E10-0344 `00:17:20,748 --> 00:17:23,125`

- `またね～`：告别，跳过统计。

### E10-0345 `00:17:25,169 --> 00:17:26,962`

- `バイバイ`：告别，跳过统计。

### E10-0346 `00:17:27,629 --> 00:17:29,131`

- `（話し声）`：背景音，跳过统计。

### E10-0347 `00:17:29,214 --> 00:17:31,383`

1. `１コいい？`
   - `いい`：`G0109`，许可请求，“问一个可以吗”。

### E10-0348 `00:17:31,467 --> 00:17:32,551`

- `はい`：应答，跳过统计。

### E10-0349 `00:17:33,427 --> 00:17:36,055`

1. `あの人… 誰？`
   - `誰`：`G0061`，疑问词；省略 `ですか`。

### E10-0350 `00:17:36,138 --> 00:17:38,515`

1. `直美です`
   - `です`：`G0012`，礼貌断定。

### E10-0351 `00:17:38,599 --> 00:17:40,225`

1. `うちらの同級生です`
   - `うちらの同級生`：`G0004`，我们的同学。
   - `です`：`G0012`，礼貌断定。

### E10-0352 `00:17:40,309 --> 00:17:41,643`

1. `それは見てたら分かるけど`
   - `それは`：`G0013`，把“那点”作为话题。
   - `見てたら`：`G0030` 加 `G0054`，看着就/如果看。
   - `分かる`：`G0069`，能明白。
   - `けど`：`G0019`，转折铺垫。

### E10-0353 `00:17:41,727 --> 00:17:43,479`

1. `紹介しませんでしたっけ？`
   - `紹介しませんでした`：`G0029` 加 `G0021` 加 `G0001` 加 `G0012`，礼貌过去否定。
   - `っけ`：`G0071`，回想确认。

### E10-0354 `00:17:43,562 --> 00:17:45,939`

1. `されてないよ`
   - `されてない`：`G0006` 加 `G0030` 加 `G0021`，没有被介绍。
   - `よ`：`G0083`，告知。
2. `そうなんですか`
   - `そうなんですか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，理解/确认。

### E10-0355 `00:17:46,023 --> 00:17:48,025`

1. `誰かが紹介してると思ってた`
   - `誰かが`：`G0096` 加 `G0010`，某个人作主语。
   - `紹介してる`：`G0029` 加 `G0030`，正在/已经介绍。
   - `と思ってた`：`G0023` 加 `G0030` 加 `G0001`，一直以为。

### E10-0356 `00:17:48,108 --> 00:17:49,276`

1. `私も そう思ってた`
   - `私も`：`G0011`，我也。
   - `そう思ってた`：`G0023` 加 `G0030` 加 `G0001`，也那么以为。

### E10-0357 `00:17:49,359 --> 00:17:50,778`

1. `オーナーの事務所に行ったときから`
   - `オーナーの事務所`：`G0004`，名词修饰。
   - `事務所に`：`G0017`，目的地。
   - `行った`：`G0001`，过去。
   - `ときから`：`G0105` 加 `G0026`，从去事务所那时起。

### E10-0358 `00:17:50,861 --> 00:17:51,945`

1. `普通にいたよね？`
   - `いた`：`G0045` 加 `G0001`，当时在场。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0359 `00:17:52,029 --> 00:17:53,363`

1. `普通にいましたね`
   - `いました`：`G0045` 加 `G0001` 加 `G0012`，礼貌过去存在。
   - `ね`：`G0034`，确认。

### E10-0360 `00:17:53,447 --> 00:17:56,241`

1. `あのときって どういう流れで来たんだっけ？`
   - `あのときって`：`G0105` 加 `G0064`，把当时作为话题。
   - `どういう`：`G0080`，什么样的。
   - `流れで`：`G0008`，方式/过程。
   - `来たんだ`：`G0001` 加 `G0066`，说明性回想。
   - `っけ`：`G0071`，回想确认。

### E10-0361 `00:17:56,325 --> 00:17:57,326`

1. `あのときは`
   - `あのときは`：`G0105` 加 `G0013`，当时作为话题；句子延续。

### E10-0362 `00:17:57,409 --> 00:17:59,828`

1. `私が昼間に直美とランチ食べてて`
   - `私が`：`G0010`，主语。
   - `昼間に`：`G0017`，时间点。
   - `直美と`：`G0016`，共同对象。
   - `食べてて`：`G0030` 加 `G0035`，当时正在吃饭并连接后文。

### E10-0363 `00:17:59,912 --> 00:18:01,371`

1. `このあと みんなで集まるって話したら`
   - `このあと`：`G0058`，之后。
   - `みんなで`：`G0008`，共同主体。
   - `集まるって`：`G0064`，引用内容。
   - `話したら`：`G0001` 加 `G0054`，一说/如果说。

### E10-0364 `00:18:01,455 --> 00:18:02,748`

1. `行きたいって言うから連れてきた`
   - `行きたい`：`G0134`，想去。
   - `って`：`G0064`，引用内容。
   - `言うから`：`G0088`，理由。
   - `連れてきた`：`G0024` 加 `G0001`，带过来了。

### E10-0365 `00:18:02,831 --> 00:18:03,665`

1. `そうだ そうだ`
   - `そうだ`：`G0003` 加 `G0012`，想起来/认同。

### E10-0366 `00:18:03,749 --> 00:18:06,251`

1. `高橋さんいるけど 車 別だし いっかと思って`
   - `高橋さんいる`：`G0045`，高桥在场。
   - `けど`：`G0019`，转折铺垫。
   - `別だし`：`G0110`，补充理由。
   - `いっか`：`G0002`，`いいか` 的口语缩约，自问式许可。
   - `と思って`：`G0023` 加 `G0035`，引用自己的想法。

### E10-0367 `00:18:06,335 --> 00:18:07,961`

1. `じゃ 俺のこと話したの？`
   - `じゃ`：`G0056`，承接追问。
   - `俺のこと`：`G0097`，关于我这件事/我的情况。
   - `話した`：`G0001`，过去。
   - `の`：`G0066`，说明疑问。

### E10-0368 `00:18:08,045 --> 00:18:10,214`

1. `話したけど 宇宙人のことは言ってないですよ`
   - `話した`：`G0001`，过去。
   - `けど`：`G0019`，转折。
   - `宇宙人のことは`：`G0097` 加 `G0013`，关于宇宙人的事作为对比话题。
   - `言ってない`：`G0030` 加 `G0021`，没有说。
   - `です`：`G0012`，礼貌断定。
   - `よ`：`G0083`，告知。

### E10-0369 `00:18:10,297 --> 00:18:11,215`

1. `そうなの？`
   - `そうなの`：`G0003` 加 `G0066`，确认说明。

### E10-0370 `00:18:11,298 --> 00:18:12,966`

1. `口堅いんで`
   - `んで`：`G0052`，口语原因，“因为嘴严”。

### E10-0371 `00:18:13,050 --> 00:18:13,884`

1. `何て言ったの？`
   - `何て`：`G0064`，引用疑问“怎么说的”。
   - `言った`：`G0001`，过去。
   - `の`：`G0066`，说明疑问。

### E10-0372 `00:18:13,967 --> 00:18:16,762`

1. `そのまま “きーちゃんの職場の先輩”って`
   - `そのまま`：`G0258`，照原样/就那样。
   - `きーちゃんの職場の先輩`：`G0004`，名词连锁修饰。
   - `って`：`G0064`，引用；句子延续。

### E10-0373 `00:18:16,845 --> 00:18:17,679`

1. `それだけ？`
   - `だけ`：`G0039`，限定“就这些？”。

### E10-0374 `00:18:17,763 --> 00:18:19,348`

1. `それだけですよ`
   - `だけ`：`G0039`，限定。
   - `です`：`G0012`，礼貌断定。
   - `よ`：`G0083`，告知。

### E10-0375 `00:18:19,431 --> 00:18:21,266`

1. `オーナーの事務所に入ることは？`
   - `オーナーの事務所`：`G0004`，名词修饰。
   - `事務所に`：`G0017`，进入目标。
   - `入ること`：`G0097`，把动作名词化。
   - `ことは`：`G0013`，作为对比话题；谓语省略。

### E10-0376 `00:18:21,350 --> 00:18:22,768`

1. `それは言いました`
   - `それは`：`G0013`，把该事项作为对比话题。
   - `言いました`：`G0001` 加 `G0012`，礼貌过去。

### E10-0377 `00:18:22,851 --> 00:18:24,728`

1. `オーナーが市長とグルになってて`
   - `オーナーが`：`G0010`，主语。
   - `市長と`：`G0016`，共同/结伙对象。
   - `グルになってて`：`G0048` 加 `G0030` 加 `G0035`，已经结成一伙并连接后文。

### E10-0378 `00:18:24,812 --> 00:18:27,815`

1. `悪いことしてるから それを暴こうとしてるって`
   - `悪いこと`：`G0097`，事情/行为。
   - `してる`：`G0030`，正在做。
   - `から`：`G0088`，理由。
   - `それを`：`G0031`，动作对象。
   - `暴こうとしてる`：`G0201` 加 `G0136` 加 `G0030`，正打算/试图揭发。
   - `って`：`G0064`，引用说明内容。

### E10-0379 `00:18:28,607 --> 00:18:30,400`

1. `それに対しては何て言ったの？`
   - `それに対して`：`G0278`，针对那件事。
   - `は`：`G0013`，对比话题。
   - `何て`：`G0064`，引用疑问。
   - `言った`：`G0001`，过去。
   - `の`：`G0066`，说明疑问。

### E10-0380 `00:18:31,026 --> 00:18:33,570`

1. `別に “大変だね”くらいですね`
   - `別に`：`G0104`，并没有特别。
   - `くらい`：`G0027`，大约/程度。
   - `です`：`G0012`，礼貌断定。
   - `ね`：`G0034`，确认。

### E10-0381 `00:18:34,238 --> 00:18:35,531`

1. `そう`
   - `そう`：`G0003`，接受/理解。

### E10-0382 `00:18:35,614 --> 00:18:37,407`

1. `今度 ちゃんと紹介しますね`
   - `紹介します`：`G0029` 加 `G0012`，礼貌动作。
   - `ね`：`G0034`，柔和告知。

### E10-0383 `00:18:37,491 --> 00:18:38,534`

- `すいません`：道歉寒暄，跳过统计。

### E10-0384 `00:18:38,659 --> 00:18:40,410`

1. `宇宙人だってことは絶対 言わないんで`
   - `宇宙人だってこと`：`G0190`，宇宙人这件事。
   - `ことは`：`G0013`，对比话题。
   - `言わない`：`G0021`，否定。
   - `んで`：`G0052`，说明理由/保证。

### E10-0385 `00:18:40,494 --> 00:18:43,497`

1. `もう言って 逆にモヤモヤするから`
   - `言って`：`G0228`，句末て形轻命令/请求。
   - `逆に`：`G0366`，反而。
   - `するから`：`G0088`，说明原因。

### E10-0386 `00:18:43,580 --> 00:18:44,540`

1. `いいんですか？`
   - `いい`：`G0109`，许可。
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌确认。

### E10-0387 `00:18:44,623 --> 00:18:46,291`

1. `ていうか もう 言わないで普通にいられるほうが`
   - `ていうか`：`G0074`，话说/不如说。
   - `言わないで`：`G0036`，不说而保持。
   - `いられる`：`G0069`，可能表达。
   - `ほうが`：`G0042`，比较的一方；句子延续。

### E10-0388 `00:18:46,375 --> 00:18:47,793`

1. `気になっちゃうから`
   - `気になっちゃう`：`G0172` 加 `G0133`，会变得在意，`てしまう` 口语缩约带不由自主感。
   - `から`：`G0088`，理由。

### E10-0389 `00:18:48,335 --> 00:18:50,295`

1. `分かりました`
   - `分かりました`：`G0001` 加 `G0012`，礼貌应答。

### E10-0390 `00:18:53,465 --> 00:18:54,883`

1. `今そんなことになってんの？`
   - `そんなこと`：`G0167` 加 `G0097`，那种情况。
   - `になってん`：`G0048` 加 `G0030` 加 `G0066`，`になっているの` 的口语缩约。

### E10-0391 `00:18:54,967 --> 00:18:56,802`

1. `はい だから`
   - `だから`：`G0076`，所以；句子延续。

### E10-0392 `00:18:56,885 --> 00:18:59,137`

1. `私たちで不正を暴いて`
   - `私たちで`：`G0008`，动作主体范围。
   - `不正を`：`G0031`，动作对象。
   - `暴いて`：`G0035`，て形连接。

### E10-0393 `00:18:59,221 --> 00:19:01,557`

1. `売却を阻止しようと思って`
   - `売却を`：`G0031`，动作对象。
   - `阻止しよう`：`G0029` 加 `G0201`，意向形。
   - `と思って`：`G0023` 加 `G0035`，表示打算并连接后文。

### E10-0394 `00:19:01,640 --> 00:19:03,225`

1. `そっか`
   - `そっか`：`G0003`，理解。

### E10-0395 `00:19:03,308 --> 00:19:04,309`

- `いや…`：话语填充，跳过统计。

### E10-0396 `00:19:04,393 --> 00:19:06,645`

1. `今まで言わなかったけどさ`
   - `今まで`：`G0014`，到现在为止。
   - `言わなかった`：`G0021` 加 `G0001`，过去否定。
   - `けど`：`G0019`，转折铺垫。
   - `さ`：`G0131`，口语提示。

### E10-0397 `00:19:07,312 --> 00:19:13,110`

1. `実は 50年後の浅田は ホントに ひどいことになっててさ`
   - `50年後の浅田`：`G0004`，名词修饰。
   - `浅田は`：`G0013`，话题。
   - `ひどいこと`：`G0097`，形式名词化的“糟糕情况”。
   - `になってて`：`G0048` 加 `G0030` 加 `G0035`，已经变成那种状态并连接后文。
   - `さ`：`G0131`，口语提示。

### E10-0398 `00:19:13,193 --> 00:19:14,695`

1. `そうなんですか？`
   - `そうなんですか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，礼貌确认。

### E10-0399 `00:19:14,778 --> 00:19:15,946`

- `うん`：应答，跳过统计。

### E10-0400 `00:19:16,655 --> 00:19:21,076`

1. `環境は破壊されて 町も荒廃しちゃってさ`
   - `環境は`：`G0013`，话题。
   - `破壊されて`：`G0006` 加 `G0035`，被破坏并连接。
   - `町も`：`G0011`，城镇也。
   - `荒廃しちゃって`：`G0029` 加 `G0133` 加 `G0035`，`してしまって` 的口语缩约，表示糟糕结果。
   - `さ`：`G0131`，口语提示。

## 第二批复盘（0201-0400）

- 本批从高桥回忆电话内容推进到确认受贿时间地点，并切到村上说明 50 年后的浅田。
- 新增 `G0412 AかBか`，用于 `死ぬかハゲるか` 这种明确二选一。
- 扩展 `G0079` 覆盖 `出たりしない` 这类否定试探；后续 `Vたりしない？` 也按列举/例示处理。
- 高频复习项包括 `ている/てた`、`んですか`、`じゃないですか`、`たらいい`、`と思う`、`ってこと`、`なきゃいけない`。
- 下一批从 `E10-0401` 开始。

## 第三批新文法总览（0401-0600）

本批无新增编号，主要复用 `G0133 てしまう・ちゃう`、`G0155 なきゃいけない`、`G0190 ってことは`、`G0258 このまま/そのまま`、`G0366 逆に`、`G0374 なおさら` 等既有条目。

## 逐句解析（0401-0600）

### E10-0401 `00:19:21,160 --> 00:19:23,036`

1. `しかも その影響で`
   - `しかも`：`G0312`，而且。
   - `その影響で`：`G0008`，原因。

### E10-0402 `00:19:23,579 --> 00:19:26,123`

1. `2040年ごろかな？`
   - `ごろ`：`G0111`，大约某个时间。
   - `かな`：`G0099`，不确定的自问。

### E10-0403 `00:19:27,165 --> 00:19:31,420`

1. `富士山が世界遺産の登録を抹消されちゃったのよ`
   - `富士山が`：`G0010`，主语。
   - `世界遺産の登録`：`G0004`，名词修饰。
   - `登録を`：`G0031`，动作对象。
   - `抹消されちゃった`：`G0006` 加 `G0133` 加 `G0001`，`されてしまった` 的口语缩约，表示遗憾结果。
   - `の`：`G0066`，说明语气。
   - `よ`：`G0083`，告知。

### E10-0404 `00:19:31,503 --> 00:19:34,006`

1. `抹消ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌确认。

### E10-0405 `00:19:34,089 --> 00:19:35,257`

- `うん`：应答，跳过统计。

### E10-0406 `00:19:35,716 --> 00:19:39,553`

1. `あのとき 梅本雅子を当選させちゃったところから`
   - `あのとき`：`G0105`，那个时候。
   - `梅本雅子を`：`G0031`，使役动作对象。
   - `当選させちゃった`：`G0047` 加 `G0133` 加 `G0001`，让其当选了，带后悔感。
   - `ところから`：`G0301` 加 `G0026`，从那个节点/场面开始。

### E10-0407 `00:19:39,636 --> 00:19:41,930`

1. `いろいろとおかしくなり始めたんだよね`
   - `おかしくなり始めた`：`G0048` 加 `G0222` 加 `G0001`，开始变得不对劲。
   - `んだ`：`G0066`，说明。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0408 `00:19:43,891 --> 00:19:48,145`

1. `じゃ なおさら 不正を暴かなきゃですね`
   - `じゃ`：`G0056`，承接结论。
   - `なおさら`：`G0374`，更加/更何况。
   - `不正を`：`G0031`，动作对象。
   - `暴かなきゃ`：`G0155`，`暴かなければいけない` 的口语省略，必须揭发。
   - `です`：`G0012`，礼貌断定。
   - `ね`：`G0034`，确认。

### E10-0409 `00:19:48,687 --> 00:19:49,771`

- `うん`：应答，跳过统计。

### E10-0410 `00:19:50,522 --> 00:19:53,775`

1. `是非 暴いて 未来を変えてほしいな`
   - `暴いて`：`G0035`，て形连接。
   - `未来を`：`G0031`，动作对象。
   - `変えてほしい`：`G0073`，希望对方改变。
   - `な`：`G0139`，愿望/感慨语气。

### E10-0411 `00:19:58,530 --> 00:19:59,865`

1. `頑張ります`
   - `ます`：`G0012`，丁寧体。

### E10-0412 `00:20:10,626 --> 00:20:11,877`

1. `そして`
   - `そして`：叙述推进“然后”，接续词短句不单列统计。

### E10-0413 `00:20:14,004 --> 00:20:17,841`

1. `いよいよ 決戦当日`
   - `いよいよ`：`G0297`，终于/终于到了关键时刻。

### E10-0414 `00:20:34,775 --> 00:20:38,237`

1. `８時40分 まだ時間あるね`
   - `まだ`：`G0104`，还。
   - `時間ある`：`G0075`，有时间。
   - `ね`：`G0034`，确认。

### E10-0415 `00:20:38,320 --> 00:20:39,988`

1. `あと20分か`
   - `あと`：`G0058`，还剩/之后的时间量。
   - `か`：`G0002`，自言确认。

### E10-0416 `00:20:40,405 --> 00:20:41,281`

1. `てか 寒いね`
   - `てか`：`G0074`，话说。
   - `ね`：`G0034`，共感。

### E10-0417 `00:20:41,365 --> 00:20:43,784`

1. `寒いッスね 着込んできて良かった`
   - `ッス`：`G0012`，口语丁寧体。
   - `ね`：`G0034`，共感。
   - `着込んできて`：`G0024` 加 `G0035`，穿厚衣服来到这里。
   - `良かった`：`G0001`，过去评价“幸好”。

### E10-0418 `00:20:43,867 --> 00:20:45,327`

- `あの…`：话语填充，跳过统计。

### E10-0419 `00:20:45,744 --> 00:20:48,080`

1. `良かったら あの… これ`
   - `良かったら`：`G0054`，如果愿意/方便的话；后文省略“请收下”。

### E10-0420 `00:20:48,205 --> 00:20:49,331`

- `えっ？`：反应语，跳过统计。

### E10-0421 `00:20:49,414 --> 00:20:51,208`

1. `どうしたんですか？ 珍しい`
   - `どうしたんですか`：`G0093` 加 `G0066` 加 `G0012` 加 `G0002`，询问缘由。

### E10-0422 `00:20:51,291 --> 00:20:53,669`

1. `俺のために ここまで やってもらってるからさ`
   - `俺のために`：`G0028`，为了我。
   - `ここまで`：`G0014`，到这种程度。
   - `やってもらってる`：`G0141` 加 `G0030`，一直让大家帮忙。
   - `から`：`G0088`，理由。
   - `さ`：`G0131`，口语提示。

### E10-0423 `00:20:53,752 --> 00:20:54,753`

- `あら？`：反应语，跳过统计。

### E10-0424 `00:20:54,836 --> 00:20:56,255`

1. `せめてもの気持ちっていうか`
   - `せめて`：`G0276`，至少/最低限。
   - `気持ちっていうか`：`G0060` 加 `G0074`，说是心意吧/怎么说呢。

### E10-0425 `00:20:56,338 --> 00:20:58,298`

- `ありがとうございます`：感谢语，跳过统计。

### E10-0426 `00:20:58,382 --> 00:21:00,050`

- `温か / ホントありがとう`：评价和感谢，跳过统计。

### E10-0427 `00:21:00,133 --> 00:21:01,969`

- `いえいえ`：应答寒暄，跳过统计。

### E10-0428 `00:21:02,052 --> 00:21:05,347`

1. `ていうか もう 高橋さんのためだけじゃないですよ`
   - `ていうか`：`G0074`，话说/不如说。
   - `高橋さんのため`：`G0028`，为了高桥。
   - `だけ`：`G0039`，限定。
   - `じゃないです`：`G0043` 加 `G0012`，并不是。
   - `よ`：`G0083`，告知。

### E10-0429 `00:21:05,973 --> 00:21:07,015`

1. `どういうこと？`
   - `どういう`：`G0080`，什么样的/怎么回事。
   - `こと`：`G0097`，形式名词。

### E10-0430 `00:21:07,099 --> 00:21:09,851`

1. `この間 村上さんから聞いたんですけど`
   - `この間`：`G0058`，前些时候。
   - `村上さんから`：`G0026`，消息来源。
   - `聞いた`：`G0001`，过去。
   - `んですけど`：`G0066` 加 `G0012` 加 `G0019`，说明并铺垫。

### E10-0431 `00:21:09,935 --> 00:21:11,645`

1. `50年後の浅田って`
   - `50年後の浅田`：`G0004`，名词修饰。
   - `って`：`G0064`，口语提题。

### E10-0432 `00:21:11,728 --> 00:21:13,981`

1. `結構 ひどいことになってるらしくって`
   - `ひどいこと`：`G0097`，形式名词化的状态。
   - `になってる`：`G0048` 加 `G0030`，变成那种状态。
   - `らしくって`：`G0065` 加 `G0035`，听说/似乎并连接后文。

### E10-0433 `00:21:14,064 --> 00:21:15,565`

1. `そうなの？`
   - `そうなの`：`G0003` 加 `G0066`，确认说明。

### E10-0434 `00:21:15,649 --> 00:21:18,986`

1. `富士山も 世界遺産から登録抹消されるんですって`
   - `富士山も`：`G0011`，富士山也。
   - `世界遺産から`：`G0026`，从世界遗产名录中。
   - `登録抹消される`：`G0006`，被取消登记。
   - `んですって`：`G0066` 加 `G0156`，听说/转述语气。

### E10-0435 `00:21:19,069 --> 00:21:20,320`

- `抹消？`：复述确认，跳过统计。

### E10-0436 `00:21:20,404 --> 00:21:21,738`

- `うん / 最悪じゃん`：应答和评价，跳过统计。

### E10-0437 `00:21:21,822 --> 00:21:22,739`

1. `ねっ？`
   - `ね`：`G0034`，求认同。

### E10-0438 `00:21:22,823 --> 00:21:25,659`

1. `町がおかしくなり始めたのって`
   - `町が`：`G0010`，主语。
   - `おかしくなり始めた`：`G0048` 加 `G0222` 加 `G0001`，开始变得不对劲。
   - `のって`：`G0097` 加 `G0064`，把“开始变糟这件事”提为话题。

### E10-0439 `00:21:25,742 --> 00:21:28,662`

1. `やっぱり 梅本さんが市長になってからなんだって`
   - `梅本さんが`：`G0010`，主语。
   - `市長になってから`：`G0048` 加 `G0026`，从成为市长之后。
   - `なんだって`：`G0066` 加 `G0156`，转述听说。

### E10-0440 `00:21:28,745 --> 00:21:30,288`

1. `そうなんだ`
   - `そうなんだ`：`G0003` 加 `G0066`，理解说明。

### E10-0441 `00:21:30,372 --> 00:21:32,791`

1. `あのクソ市長 マジで何してくれてんの！`
   - `何してくれてんの`：`G0090` 加 `G0030` 加 `G0066`，反讽式“给我们干了什么好事”。

### E10-0442 `00:21:32,874 --> 00:21:34,793`

1. `これ 絶対 引きずり下ろさなきゃですね`
   - `引きずり下ろさなきゃ`：`G0155`，必须拉下台。
   - `です`：`G0012`，礼貌断定。
   - `ね`：`G0034`，确认。

### E10-0443 `00:21:34,876 --> 00:21:36,128`

1. `ねっ！`
   - `ね`：`G0034`，强烈认同。

### E10-0444 `00:21:36,211 --> 00:21:38,297`

1. `だから こうなったら`
   - `だから`：`G0076`，所以。
   - `こうなったら`：`G0048` 加 `G0054`，既然事情变成这样。

### E10-0445 `00:21:38,380 --> 00:21:42,259`

1. `高橋さんだけの問題じゃなくて うちら みんなの問題なんで`
   - `高橋さんだけ`：`G0039`，只限高桥。
   - `問題じゃなくて`：`G0043` 加 `G0035`，并不是……而是……
   - `みんなの問題`：`G0004`，大家的问题。
   - `なんで`：`G0066` 加 `G0052`，因为是……。

### E10-0446 `00:21:42,342 --> 00:21:44,720`

1. `もう そんなに気ぃ遣わなくても大丈夫ですよ`
   - `そんなに`：`G0020`，那么地。
   - `気ぃ遣わなくても`：`G0021` 加 `G0217`，即使不那么顾虑也。
   - `です`：`G0012`，礼貌断定。
   - `よ`：`G0083`，告知。

### E10-0447 `00:21:45,429 --> 00:21:47,055`

1. `そっか`
   - `そっか`：`G0003`，理解。

### E10-0448 `00:21:47,139 --> 00:21:47,973`

1. `分かった`
   - `分かった`：`G0001`，理解完成。

### E10-0449 `00:21:48,056 --> 00:21:50,058`

1. `絶対 写真撮って`
   - `撮って`：`G0035`，て形连接后文。

### E10-0450 `00:21:50,142 --> 00:21:52,019`

1. `あのクソ市長 警察に突き出そうよ`
   - `警察に`：`G0017`，交给/扭送的对象。
   - `突き出そう`：`G0201`，意向形提议。
   - `よ`：`G0083`，推动提议。

### E10-0451 `00:21:52,102 --> 00:21:54,104`

1. `やっちゃいましょう`
   - `やっちゃいましょう`：`G0133` 加 `G0201` 加 `G0012`，`やってしまいましょう` 的口语缩约，表示干脆做掉。

### E10-0452 `00:21:54,354 --> 00:21:58,066`

1. `ちなみになんだけどさ`
   - `ちなみに`：`G0107`，顺便说。
   - `なんだけど`：`G0066` 加 `G0019`，说明并转入话题。
   - `さ`：`G0131`，口语提示。

### E10-0453 `00:21:58,859 --> 00:22:02,529`

1. `コーヒー代 130円ずつ いいかな？`
   - `130円ずつ`：`G0114`，每人 130 日元。
   - `いい`：`G0109`，许可/可以。
   - `かな`：`G0099`，试探性征询。

### E10-0454 `00:22:04,698 --> 00:22:06,533`

1. `もともと これ`
   - `もともと`：`G0320`，原本；句子延续。

### E10-0455 `00:22:06,616 --> 00:22:09,828`

1. `俺のためにってことへのお礼だったからさ`
   - `俺のために`：`G0028`，为了我。
   - `ってこと`：`G0190`，这件事。
   - `ことへの`：`G0055`，指向/对象。
   - `お礼だった`：`G0001`，过去判断。
   - `から`：`G0088`，理由。
   - `さ`：`G0131`，口语提示。

### E10-0456 `00:22:10,120 --> 00:22:12,164`

1. `みんなのためってことなら`
   - `みんなのため`：`G0028`，为了大家。
   - `ってこと`：`G0190`，这回事。
   - `なら`：`G0108`，如果是那样的话。

### E10-0457 `00:22:15,459 --> 00:22:18,462`

1. `別に ケチって言ってるわけじゃなくてさ`
   - `別に`：`G0104`，并不是特别/并非。
   - `ケチって`：`G0064`，引用称呼/评价。
   - `言ってる`：`G0030`，正在说/意思是。
   - `わけじゃなくて`：`G0157` 加 `G0035`，并不是说……。
   - `さ`：`G0131`，口语提示。

### E10-0458 `00:22:18,545 --> 00:22:20,839`

1. `気ぃ遣うなって言ってたから`
   - `気ぃ遣うな`：`G0148`，禁止/不要顾虑。
   - `って`：`G0064`，引用内容。
   - `言ってた`：`G0030` 加 `G0001`，刚才说过。
   - `から`：`G0088`，理由。

### E10-0459 `00:22:20,922 --> 00:22:23,592`

1. `一応 対等にしといたほうがいいかなと思って`
   - `対等にしといた`：`G0113` 加 `G0001`，`しておいた` 的口语缩约，先弄成对等。
   - `ほうがいい`：`G0170`，最好。
   - `かな`：`G0099`，自问。
   - `と思って`：`G0023` 加 `G0035`，引用想法。

### E10-0460 `00:22:24,134 --> 00:22:27,429`

1. `そのほうが お互い 気ぃ遣わないで済むじゃん`
   - `そのほうが`：`G0042`，那种做法更。
   - `お互い`：`G0403`，彼此。
   - `気ぃ遣わないで`：`G0036`，不顾虑而。
   - `済む`：`G0199`，可以解决/不必再。
   - `じゃん`：`G0092`，口语确认。

### E10-0461 `00:22:27,637 --> 00:22:31,308`

1. `たった今 私たちのモチベーションは`
   - `私たちのモチベーション`：`G0004`，名词修饰。
   - `モチベーションは`：`G0013`，话题；句子延续。

### E10-0462 `00:22:31,391 --> 00:22:35,062`

1. `100パーセント 地元のためだけになった`
   - `地元のため`：`G0028`，为了本地。
   - `だけ`：`G0039`，限定。
   - `になった`：`G0048` 加 `G0001`，变成只为了本地。

### E10-0463 `00:22:36,021 --> 00:22:37,898`

1. `対等にしとこう`
   - `しとこう`：`G0113` 加 `G0201`，`しておこう` 的口语缩约，先保持对等吧。

### E10-0464 `00:22:38,648 --> 00:22:42,361`

1. `そういえば 高橋さん この前の副作用 大丈夫でした？`
   - `そういえば`：`G0059`，说起来。
   - `この前の副作用`：`G0004`，前些时候的副作用。
   - `でした`：`G0001` 加 `G0012`，礼貌过去询问。

### E10-0465 `00:22:42,444 --> 00:22:43,320`

1. `どれの？`
   - `どれの`：`G0004`，疑问词后接 `の`，问“哪个的”。

### E10-0466 `00:22:43,403 --> 00:22:45,197`

1. `記憶力 上げたときの`
   - `上げた`：`G0001`，过去。
   - `ときの`：`G0105` 加 `G0004`，……时候的。

### E10-0467 `00:22:45,280 --> 00:22:48,867`

1. `それがさ なぜか 全然 平気だったのよ`
   - `それが`：`G0010`，主语/话题转入意外结果。
   - `さ`：`G0131`，口语提示。
   - `なぜか`：`G0356`，不知为何。
   - `全然`：`G0162`，现代口语肯定用法，加强“完全没事”。
   - `だった`：`G0001`，过去判断。
   - `の`：`G0066`，说明语气。
   - `よ`：`G0083`，告知。

### E10-0468 `00:22:48,950 --> 00:22:49,993`

1. `そうなんですか？`
   - `そうなんですか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，礼貌确认。

### E10-0469 `00:22:50,077 --> 00:22:52,454`

1. `おでこ 広がってないでしょう？`
   - `広がってない`：`G0030` 加 `G0021`，没有变宽。
   - `でしょう`：`G0094`，确认推量。

### E10-0470 `00:22:52,537 --> 00:22:54,998`

1. `確かに / ホントだ`
   - `だ`：`G0012`，断定。

### E10-0471 `00:22:55,082 --> 00:22:56,625`

1. `そうなのよ / そうですね`
   - `そうなの`：`G0003` 加 `G0066`，说明。
   - `よ`：`G0083`，告知。
   - `ですね`：`G0012` 加 `G0034`，礼貌确认。

### E10-0472 `00:22:56,708 --> 00:22:57,834`

1. `こんなこともあるんだと思って`
   - `こんなこと`：`G0167` 加 `G0097`，这种事。
   - `ことも`：`G0011`，这种事也。
   - `あるんだ`：`G0075` 加 `G0066`，说明“也会有这种情况”。
   - `と思って`：`G0023` 加 `G0035`，引用想法。

### E10-0473 `00:22:57,918 --> 00:23:00,003`

1. `来ました？`
   - `来ました`：`G0001` 加 `G0012`，礼貌过去形确认“来了？”

### E10-0474 `00:23:00,462 --> 00:23:02,881`

1. `早くないッスか？`
   - `早くない`：`G0098`，否定疑问式确认。
   - `ッスか`：`G0012` 加 `G0002`，口语丁寧疑问。

### E10-0475 `00:23:04,132 --> 00:23:05,842`

1. `手袋 外した`
   - `外した`：`G0001`，过去/完成。

### E10-0476 `00:23:05,926 --> 00:23:06,760`

- `うん？`：反应语，跳过统计。

### E10-0477 `00:23:07,427 --> 00:23:08,845`

1. `瑞稀ちゃんじゃない？`
   - `じゃない`：`G0098`，确认反问。

### E10-0478 `00:23:08,929 --> 00:23:10,514`

1. `ホントだ`
   - `だ`：`G0012`，断定确认。

### E10-0479 `00:23:11,598 --> 00:23:14,643`

- `瑞稀ちゃん！`：呼唤，跳过统计。

### E10-0480 `00:23:14,726 --> 00:23:16,311`

1. `清美さんだ`
   - `だ`：`G0012`，断定发现。

### E10-0481 `00:23:17,145 --> 00:23:19,189`

1. `清美さんたちいるよ`
   - `清美さんたちいる`：`G0045`，存在。
   - `よ`：`G0083`，告知。

### E10-0482 `00:23:19,272 --> 00:23:20,690`

1. `どこ？`
   - `どこ`：`G0061`，疑问地点。

### E10-0483 `00:23:20,774 --> 00:23:21,608`

1. `由美ちゃんだ`
   - `だ`：`G0012`，断定发现。

### E10-0484 `00:23:21,691 --> 00:23:24,277`

1. `こっちおいで`
   - `おいで`：`G0046`，口语化的来这边/过来。

### E10-0485 `00:23:24,361 --> 00:23:27,489`

1. `こっちこっち こっちおいで`
   - `おいで`：`G0046`，口语招呼“过来”。

### E10-0486 `00:23:27,572 --> 00:23:29,324`

1. `おいで おいで！ おいでよ！`
   - `おいで`：`G0046`，招呼对方过来。
   - `よ`：`G0083`，推动语气。

### E10-0487 `00:23:30,033 --> 00:23:32,244`

1. `何ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0488 `00:23:32,327 --> 00:23:35,330`

1. `隠れて 隠れて 隠れて！`
   - `隠れて`：`G0228`，句末て形轻命令/请求。

### E10-0489 `00:23:35,413 --> 00:23:37,916`

1. `こんばんは`
   - 寒暄语，跳过统计。
2. `何してるんですか？`
   - `何してる`：`G0030`，正在做什么。
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌询问说明。

### E10-0490 `00:23:37,999 --> 00:23:39,126`

1. `今からね ここで`
   - `今から`：`G0026`，从现在开始。
   - `ね`：`G0034`，提示。
   - `ここで`：`G0008`，地点；句子延续。

### E10-0491 `00:23:39,209 --> 00:23:41,211`

1. `オーナーと市長が賄賂の受け渡しすんのよ`
   - `オーナーと市長が`：`G0016` 加 `G0010`，并列主语。
   - `賄賂の受け渡し`：`G0004`，名词修饰。
   - `すんの`：`G0029` 加 `G0066`，`するの` 的口语缩约。
   - `よ`：`G0083`，告知。

### E10-0492 `00:23:41,294 --> 00:23:42,546`

1. `ここでですか？`
   - `ここで`：`G0008`，地点。
   - `ですか`：`G0012` 加 `G0002`，礼貌确认。

### E10-0493 `00:23:42,629 --> 00:23:43,463`

1. `だから みんなで`
   - `だから`：`G0076`，所以。
   - `みんなで`：`G0008`，共同主体；句子延续。

### E10-0494 `00:23:43,547 --> 00:23:45,048`

1. `写真 撮ろうと思って ここで待機してんの`
   - `撮ろう`：`G0201`，意向形。
   - `と思って`：`G0023` 加 `G0035`，表示打算。
   - `ここで`：`G0008`，地点。
   - `待機してんの`：`G0030` 加 `G0066`，`待機しているの` 的口语缩约。

### E10-0495 `00:23:45,132 --> 00:23:46,299`

- `すご～い`：评价，跳过统计。

### E10-0496 `00:23:46,383 --> 00:23:48,135`

1. `それって うちらも参加してもいいですか？`
   - `それって`：`G0064`，口语提题。
   - `うちらも`：`G0011`，我们也。
   - `参加してもいい`：`G0109`，可以参加。
   - `ですか`：`G0012` 加 `G0002`，礼貌询问。

### E10-0497 `00:23:48,218 --> 00:23:50,512`

1. `いいよ 大丈夫だよね？`
   - `いいよ`：`G0109` 加 `G0083`，许可。
   - `だよね`：`G0012` 加 `G0083` 加 `G0034`，向同伴确认。

### E10-0498 `00:23:50,595 --> 00:23:52,222`

1. `じゃ ちょっと車だけ移動してきます`
   - `じゃ`：`G0056`，承接。
   - `車だけ`：`G0039`，只把车。
   - `移動してきます`：`G0024` 加 `G0012`，移动后回来。

### E10-0499 `00:23:52,305 --> 00:23:54,141`

1. `気をつけてね`
   - `気をつけて`：`G0228`，句末て形提醒/请求。
   - `ね`：`G0034`，柔和叮嘱。

### E10-0500 `00:23:56,476 --> 00:23:58,311`

- `おかえり / ただいま`：寒暄，跳过统计。

### E10-0501 `00:23:58,395 --> 00:24:00,939`

- `おかえりなさい / ただいまで～す`：寒暄，跳过统计。

### E10-0502 `00:24:01,022 --> 00:24:02,607`

1. `何時ぐらいに来そうですか？`
   - `何時ぐらい`：`G0027`，大约几点。
   - `に`：`G0017`，时间点。
   - `来そう`：`G0032`，看起来/预计会来。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0503 `00:24:02,691 --> 00:24:03,942`

1. `予定では９時だね`
   - `予定では`：`G0013`，按预定来说。
   - `だね`：`G0012` 加 `G0034`，断定并确认。

### E10-0504 `00:24:04,025 --> 00:24:06,153`

1. `じゃ もうちょっとだ カメラ準備しとこう`
   - `じゃ`：`G0056`，承接。
   - `だ`：`G0012`，断定。
   - `準備しとこう`：`G0029` 加 `G0113` 加 `G0201`，`準備しておこう` 的口语缩约，先准备好。

### E10-0505 `00:24:06,236 --> 00:24:07,737`

1. `そうだね`
   - `そうだね`：`G0003` 加 `G0012` 加 `G0034`，认同。

### E10-0506 `00:24:11,741 --> 00:24:14,161`

1. `髪 薄くなりました？`
   - `薄くなりました`：`G0022` 加 `G0001` 加 `G0012`，变薄了。

### E10-0507 `00:24:14,244 --> 00:24:16,246`

1. `それが 今回 大丈夫だったのよ`
   - `それが`：`G0010`，主语/意外转折。
   - `だった`：`G0001`，过去判断。
   - `の`：`G0066`，说明语气。
   - `よ`：`G0083`，告知。

### E10-0508 `00:24:16,329 --> 00:24:18,039`

1. `そうなんですか？`
   - `そうなんですか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，礼貌确认。

### E10-0509 `00:24:22,711 --> 00:24:24,629`

1. `直美さん？`
   - 人名复述确认，跳过统计。

### E10-0510 `00:24:25,213 --> 00:24:26,756`

1. `ホントだ 直美`
   - `だ`：`G0012`，断定发现。

### E10-0511 `00:24:26,840 --> 00:24:28,717`

1. `何してんの？`
   - `何してんの`：`G0030` 加 `G0066`，`何しているの` 的口语缩约。

### E10-0512 `00:24:28,800 --> 00:24:30,218`

1. `これから ここで`
   - `これから`：`G0026`，从现在起。
   - `ここで`：`G0008`，地点；句子延续。

### E10-0513 `00:24:30,302 --> 00:24:32,762`

1. `オーナーと市長が賄賂の受け渡しするからさ`
   - `オーナーと市長が`：`G0016` 加 `G0010`，并列主语。
   - `賄賂の受け渡し`：`G0004`，名词修饰。
   - `するから`：`G0088`，理由/背景。
   - `さ`：`G0131`，口语提示。

### E10-0514 `00:24:32,846 --> 00:24:34,598`

1. `写真 撮ろうと思って みんなで待機してんの`
   - `撮ろう`：`G0201`，意向形。
   - `と思って`：`G0023` 加 `G0035`，表示打算。
   - `みんなで`：`G0008`，共同主体。
   - `待機してんの`：`G0030` 加 `G0066`，`待機しているの` 的口语缩约。

### E10-0515 `00:24:34,681 --> 00:24:36,516`

1. `この前 話してたやつだ`
   - `この前`：`G0058`，前些时候。
   - `話してた`：`G0030` 加 `G0001`，之前说过。
   - `やつ`：`G0192`，口语指代“那件事/那个东西”。
   - `だ`：`G0012`，断定。

### E10-0516 `00:24:36,600 --> 00:24:38,185`

1. `そうそうそう`
   - `そう`：`G0003`，认同。
2. `てか 直美は何してんの？`
   - `てか`：`G0074`，话说。
   - `直美は`：`G0013`，话题。
   - `何してんの`：`G0030` 加 `G0066`，询问正在做什么。

### E10-0517 `00:24:38,768 --> 00:24:41,271`

1. `今 引っ越しのための荷造りしててさ`
   - `引っ越しのための`：`G0028` 加 `G0004`，为了搬家的。
   - `荷造りしてて`：`G0029` 加 `G0030` 加 `G0035`，正在打包并连接后文。
   - `さ`：`G0131`，口语提示。

### E10-0518 `00:24:41,354 --> 00:24:43,148`

1. `コンビニで 必要な物 買ってきたの`
   - `コンビニで`：`G0008`，地点。
   - `必要な物`：`G0007`，连体修饰。
   - `買ってきた`：`G0024` 加 `G0001`，买了带回来。
   - `の`：`G0066`，说明语气。

### E10-0519 `00:24:43,231 --> 00:24:45,609`

1. `家 建てたんだって？`
   - `建てた`：`G0001`，过去/完成。
   - `んだって`：`G0066` 加 `G0156`，听说/确认传闻。

### E10-0520 `00:24:45,692 --> 00:24:47,694`

1. `引っ越したら遊びに来てよ`
   - `引っ越したら`：`G0001` 加 `G0054`，搬家之后/如果搬了家。
   - `遊びに`：`G0017`，目的。
   - `来て`：`G0228`，句末て形请求。
   - `よ`：`G0083`，推动语气。

### E10-0521 `00:24:47,777 --> 00:24:48,737`

1. `行く行く`
   - 口语应答，“会去会去”，不单列统计。

### E10-0522 `00:24:48,820 --> 00:24:50,864`

1. `高橋さんは もう知ってるよね？`
   - `高橋さんは`：`G0013`，话题。
   - `知ってる`：`G0030`，知道。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0523 `00:24:50,947 --> 00:24:53,408`

1. `何度か お会いしてますよね`
   - `何度か`：`G0096`，几次。
   - `お会いしてます`：`G0046` 加 `G0030` 加 `G0012`，谦让礼貌表达“见过”。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0524 `00:24:53,491 --> 00:24:54,492`

1. `そうですよね`
   - `そうです`：`G0003` 加 `G0012`，认同。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0525 `00:24:54,576 --> 00:24:56,203`

1. `高橋さん 宇宙人だよ`
   - `だよ`：`G0012` 加 `G0083`，断定告知。

### E10-0526 `00:24:56,286 --> 00:24:57,204`

- `えっ？`：反应语，跳过统计。

### E10-0527 `00:24:57,287 --> 00:24:59,623`

1. `今言ったら ややこしいでしょう`
   - `今言ったら`：`G0001` 加 `G0054`，如果现在说。
   - `でしょう`：`G0094`，确认推量。

### E10-0528 `00:24:59,706 --> 00:25:00,832`

1. `マズかったッスか？`
   - `マズかった`：`G0001`，过去评价。
   - `ッスか`：`G0012` 加 `G0002`，口语丁寧疑问。

### E10-0529 `00:25:00,916 --> 00:25:02,876`

1. `落ち着いてるときにさ`
   - `落ち着いてる`：`G0030`，处于冷静状态。
   - `ときに`：`G0105` 加 `G0017`，在……时候。
   - `さ`：`G0131`，口语提示。

### E10-0530 `00:25:02,959 --> 00:25:04,961`

1. `気にしないでください`
   - `気にしないでください`：`G0036` 加 `G0053`，请不要在意。

### E10-0531 `00:25:05,045 --> 00:25:06,838`

1. `来たかも`
   - `来た`：`G0001`，来了。
   - `かも`：`G0095`，也许。

### E10-0532 `00:25:06,922 --> 00:25:08,632`

1. `あれ そうですよね？`
   - `そうです`：`G0003` 加 `G0012`，那是吧。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0533 `00:25:09,132 --> 00:25:11,343`

1. `写真だよ`
   - `だよ`：`G0012` 加 `G0083`，提醒/告知。

### E10-0534 `00:25:11,426 --> 00:25:12,552`

1. `梅本さん 乗ってますね`
   - `乗ってます`：`G0030` 加 `G0012`，正在车上/已经上车。
   - `ね`：`G0034`，确认。

### E10-0535 `00:25:12,636 --> 00:25:13,929`

1. `乗ってる？`
   - `乗ってる`：`G0030`，正在/已经坐在车上。

### E10-0536 `00:25:14,012 --> 00:25:16,097`

1. `緊張してきた`
   - `緊張してきた`：`G0024` 加 `G0001`，紧张感开始涌上来。

### E10-0537 `00:25:16,181 --> 00:25:17,807`

1. `オーナーの車じゃないですか？`
   - `オーナーの車`：`G0004`，名词修饰。
   - `じゃないですか`：`G0098` 加 `G0012` 加 `G0002`，确认反问。

### E10-0538 `00:25:17,891 --> 00:25:19,059`

- `えっ？`：反应语，跳过统计。

### E10-0539 `00:25:19,142 --> 00:25:21,728`

1. `ホントだ`
   - `だ`：`G0012`，断定发现。

### E10-0540 `00:25:24,397 --> 00:25:26,441`

1. `梅本さん 降りました`
   - `降りました`：`G0001` 加 `G0012`，礼貌过去/完成。

### E10-0541 `00:25:26,525 --> 00:25:27,609`

1. `降りた`
   - `降りた`：`G0001`，下车了。

### E10-0542 `00:25:27,692 --> 00:25:28,568`

- `えっ？`：反应语，跳过统计。

### E10-0543 `00:25:29,110 --> 00:25:31,321`

1. `なんか…`
   - `なんか`：`G0062`，话语填充/总觉得。

### E10-0544 `00:25:32,864 --> 00:25:34,741`

1. `乗りましたね`
   - `乗りました`：`G0001` 加 `G0012`，礼貌过去/完成。
   - `ね`：`G0034`，确认。
2. `乗った`
   - `乗った`：`G0001`，上车了。

### E10-0545 `00:25:35,325 --> 00:25:36,409`

- `こんばんは`：寒暄，跳过统计。

### E10-0546 `00:25:36,493 --> 00:25:38,620`

1. `今日 寒いですね`
   - `今日`：时间名词作话题省略。
   - `です`：`G0012`，礼貌断定。
   - `ね`：`G0034`，寒暄式共感。

### E10-0547 `00:25:38,703 --> 00:25:40,330`

1. `寒いですねえ`
   - `です`：`G0012`，礼貌断定。
   - `ねえ`：`G0034`，共感。

### E10-0548 `00:25:40,413 --> 00:25:42,123`

1. `ちょっと後ろ行きますね`
   - `後ろ行きます`：`G0012`，礼貌告知。
   - `ね`：`G0034`，柔和提示。

### E10-0549 `00:25:42,207 --> 00:25:43,250`

- `は～い`：应答，跳过统计。

### E10-0550 `00:25:43,708 --> 00:25:47,879`

1. `オーナー？ 降りましたね`
   - `降りました`：`G0001` 加 `G0012`，礼貌过去/完成。
   - `ね`：`G0034`，确认。

### E10-0551 `00:25:49,381 --> 00:25:51,174`

- `えっ？ えっ？`：反应语，跳过统计。

### E10-0552 `00:25:51,258 --> 00:25:54,427`

1. `後ろ乗った 後ろ見えない`
   - `乗った`：`G0001`，上到后座。
   - `見えない`：`G0069` 加 `G0021`，看不见。

### E10-0553 `00:25:54,511 --> 00:25:56,972`

1. `行こう 近く行こう`
   - `行こう`：`G0201`，意向形提议。
2. `行きます？`
   - `行きます`：`G0012`，礼貌疑问。

### E10-0554 `00:25:57,055 --> 00:25:59,432`

1. `行こう 近く行こう`
   - `行こう`：`G0201`，意向形提议。

### E10-0555 `00:26:00,016 --> 00:26:01,685`

1. `壁んとこ`
   - `とこ`：`G0301`，`ところ` 的口语缩约，表示地点。

### E10-0556 `00:26:02,560 --> 00:26:04,104`

1. `頭下げて`
   - `下げて`：`G0228`，句末て形轻命令。

### E10-0557 `00:26:04,187 --> 00:26:05,814`

1. `壁んとこ`
   - `とこ`：`G0301`，`ところ` 的口语缩约。

### E10-0558 `00:26:06,731 --> 00:26:08,441`

- `よしよし`：反应语，跳过统计。

### E10-0559 `00:26:11,403 --> 00:26:14,072`

1. `ダメだ 全然 見えない`
   - `だ`：`G0012`，断定。
   - `全然`：`G0162`，和否定搭配。
   - `見えない`：`G0069` 加 `G0021`，看不见。

### E10-0560 `00:26:14,155 --> 00:26:15,365`

1. `スモークかかってますしね`
   - `かかってます`：`G0030` 加 `G0012`，贴着/带着烟膜。
   - `し`：`G0110`，补充理由。
   - `ね`：`G0034`，确认。

### E10-0561 `00:26:15,448 --> 00:26:17,367`

1. `これって写真に写るの？`
   - `これって`：`G0064`，口语提题。
   - `写真に`：`G0017`，成像目标/结果。
   - `写るの`：`G0066`，说明疑问。

### E10-0562 `00:26:17,450 --> 00:26:19,035`

1. `これは写んないよ`
   - `これは`：`G0013`，话题。
   - `写んない`：`G0021`，`写らない` 的口语音变。
   - `よ`：`G0083`，告知。

### E10-0563 `00:26:19,119 --> 00:26:21,288`

1. `これ 高橋さんの能力でスモーク薄くできないんですか？`
   - `高橋さんの能力`：`G0004`，名词修饰。
   - `能力で`：`G0008`，手段。
   - `薄くできない`：`G0069` 加 `G0021`，不能弄薄。
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，礼貌询问。

### E10-0564 `00:26:21,371 --> 00:26:23,206`

1. `それはムリだよ`
   - `それは`：`G0013`，话题。
   - `だよ`：`G0012` 加 `G0083`，断定告知。

### E10-0565 `00:26:23,290 --> 00:26:26,585`

1. `こちらに菓子折 ２つ入ってますので`
   - `こちらに`：`G0017`，所在位置。
   - `２つ`：数量表达。
   - `入ってます`：`G0030` 加 `G0012`，已经放在里面。
   - `ので`：`G0052`，说明理由/前提。

### E10-0566 `00:26:26,668 --> 00:26:27,794`

- `ありがとうございます`：感谢语，跳过统计。

### E10-0567 `00:26:27,877 --> 00:26:29,504`

- `いえいえ こちらこそ`：寒暄应答，跳过统计。

### E10-0568 `00:26:29,963 --> 00:26:31,131`

1. `一応 ご確認を`
   - `ご確認`：`G0046`，敬语名词化；后面省略 `お願いします`。

### E10-0569 `00:26:31,214 --> 00:26:33,758`

1. `じゃ ちょっと失礼します`
   - `じゃ`：`G0056`，承接。
   - `失礼します`：`G0012`，礼貌告知动作。

### E10-0570 `00:26:34,718 --> 00:26:37,679`

1. `これ もう 正面から撮るしかないんじゃない？`
   - `正面から`：`G0026`，从正面。
   - `撮るしかない`：`G0181`，只能拍。
   - `んじゃない`：`G0098`，确认式提议。

### E10-0571 `00:26:37,762 --> 00:26:39,514`

1. `そうしよっか`
   - `そうしよっか`：`G0201` 加 `G0002`，意向形提议。

### E10-0572 `00:26:39,597 --> 00:26:41,599`

1. `別に 顔見られても関係ないもんね`
   - `別に`：`G0104`，并不特别。
   - `見られても`：`G0006` 加 `G0217`，即使被看到。
   - `関係ない`：`G0021`，没关系/无影响。
   - `もんね`：`G0150` 加 `G0034`，说明理由并求共感。

### E10-0573 `00:26:41,683 --> 00:26:42,809`

1. `どうせ すぐ捕まるし`
   - `どうせ`：反正，副词短注，不单列统计。
   - `捕まる`：`G0006`，被抓/落网。
   - `し`：`G0110`，补充理由。

### E10-0574 `00:26:42,892 --> 00:26:44,978`

1. `見つかったら そのまま車出されて`
   - `見つかったら`：`G0001` 加 `G0054`，如果被发现。
   - `そのまま`：`G0258`，保持那样/直接。
   - `車出されて`：`G0006` 加 `G0035`，车被开走并连接。

### E10-0575 `00:26:45,061 --> 00:26:46,104`

1. `逃げられちゃいません？`
   - `逃げられちゃいません`：`G0069` 加 `G0133` 加 `G0012` 加 `G0002`，会不会被他们逃掉，带不妙结果感。

### E10-0576 `00:26:46,187 --> 00:26:47,314`

1. `そっか`
   - `そっか`：`G0003`，理解。

### E10-0577 `00:26:47,397 --> 00:26:49,357`

1. `どうしよう`
   - `どうしよう`：`G0201`，自问“怎么办”。

### E10-0578 `00:26:49,607 --> 00:26:52,694`

1. `高橋さん 車押さえて 動けなくできないッスか？`
   - `車押さえて`：`G0035`，按住车并连接。
   - `動けなくできない`：`G0069` 加 `G0021`，不能让它动不了吗。
   - `ッスか`：`G0012` 加 `G0002`，口语丁寧疑问。

### E10-0579 `00:26:52,777 --> 00:26:54,696`

1. `ちょっとならできるかもしれないけど`
   - `なら`：`G0108`，如果只是一会儿。
   - `できる`：`G0069`，可能。
   - `かもしれない`：`G0095`，可能。
   - `けど`：`G0019`，转折保留。

### E10-0580 `00:26:54,779 --> 00:26:56,698`

1. `押さえ続けられるかな？`
   - `押さえ続けられる`：`G0205` 加 `G0069`，能不能持续按住。
   - `かな`：`G0099`，自问。

### E10-0581 `00:26:57,115 --> 00:26:58,325`

1. `やってみるわ`
   - `やってみる`：`G0168`，试试看。
   - `わ`：`G0268`，句末柔和/自言式决意。

### E10-0582 `00:26:58,950 --> 00:27:00,744`

- `瑞稀ちゃん！`：呼唤，跳过统计。

### E10-0583 `00:27:00,827 --> 00:27:03,663`

- `瑞稀ちゃん！ 瑞稀ちゃん！`：呼唤，跳过统计。

### E10-0584 `00:27:03,747 --> 00:27:04,914`

- `瑞稀ちゃん？`：呼唤/确认，跳过统计。

### E10-0585 `00:27:11,588 --> 00:27:12,672`

- `おお～…`：反应语，跳过统计。

### E10-0586 `00:27:14,132 --> 00:27:15,508`

- `えっ？ えっ？`：反应语，跳过统计。

### E10-0587 `00:27:19,262 --> 00:27:21,848`

1. `今 電源 落としたんで もう逃げられないです`
   - `電源 落とした`：`G0001`，切断了电源。
   - `んで`：`G0052`，原因。
   - `逃げられない`：`G0069` 加 `G0021`，逃不了。
   - `です`：`G0012`，礼貌断定。

### E10-0588 `00:27:21,931 --> 00:27:24,684`

1. `じゃ 正面から堂々と撮れるね`
   - `じゃ`：`G0056`，承接结论。
   - `正面から`：`G0026`，从正面。
   - `撮れる`：`G0069`，能拍。
   - `ね`：`G0034`，确认。

### E10-0589 `00:27:24,768 --> 00:27:26,978`

1. `あの車って オートスライドだから`
   - `車って`：`G0064`，口语提题。
   - `だから`：`G0088`，说明理由；句子延续。

### E10-0590 `00:27:27,062 --> 00:27:28,355`

1. `中からドア開けられないですよね`
   - `中から`：`G0026`，从里面。
   - `開けられない`：`G0069` 加 `G0021`，打不开。
   - `です`：`G0012`，礼貌断定。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0591 `00:27:28,438 --> 00:27:29,439`

1. `そうだね`
   - `そうだね`：`G0003` 加 `G0012` 加 `G0034`，认同。

### E10-0592 `00:27:29,522 --> 00:27:31,900`

1. `てことは これ このまま確保できてるってことですよね？`
   - `てことは`：`G0190`，也就是说/这么一来。
   - `このまま`：`G0258`，保持现状。
   - `確保できてる`：`G0069` 加 `G0030`，已经能确保控制住。
   - `ってこと`：`G0190`，这就是说。
   - `です`：`G0012`，礼貌断定。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0593 `00:27:31,983 --> 00:27:33,568`

1. `じゃ…`
   - `じゃ`：`G0056`，承接后续行动。

### E10-0594 `00:27:33,652 --> 00:27:36,446`

1. `このまま通報して 警察に引き渡せばいいのか`
   - `このまま`：`G0258`，保持现状直接。
   - `通報して`：`G0035`，报警并连接后文。
   - `警察に`：`G0017`，交给的对象。
   - `引き渡せばいい`：`G0082`，这样交给警方就行。
   - `のか`：`G0072`，嵌入/自问式确认。

### E10-0595 `00:27:36,529 --> 00:27:38,198`

1. `中に証拠もありますしね`
   - `中に`：`G0017`，存在位置。
   - `証拠も`：`G0011`，证据也。
   - `あります`：`G0075` 加 `G0012`，礼貌存在。
   - `し`：`G0110`，补充理由。
   - `ね`：`G0034`，确认。

### E10-0596 `00:27:38,281 --> 00:27:41,326`

1. `瑞稀ちゃん 超ファインプレー！`
   - 评价短句，跳过统计。

### E10-0597 `00:27:41,409 --> 00:27:43,453`

- `フゥ～！`：欢呼，跳过统计。

### E10-0598 `00:27:43,536 --> 00:27:45,163`

- `イエ～イ！`：欢呼，跳过统计。

### E10-0599 `00:27:45,246 --> 00:27:47,582`

1. `いたいた みんないる`
   - `いた`：`G0045` 加 `G0001`，找到了/人在那儿。
   - `みんないる`：`G0045`，大家都在。

### E10-0600 `00:27:47,665 --> 00:27:48,958`

1. `やってるわ`
   - `やってる`：`G0030`，正在做。
   - `わ`：`G0268`，句末感叹/确认。

## 第三批复盘（0401-0600）

- 本批推进到决战现场，瑞稀切断车辆电源，现场暂时成功控制住受贿车辆。
- 无新增编号；主要复用并强化 `てしまう/ちゃう` 的遗憾结果、`なきゃ` 的义务、`ってことは` 的推论、`このまま` 的状态保持。
- 高频复习项包括 `んですか`、`よね`、`じゃない？`、`から` 起点/理由、`ておく/とく`、`できる/できない`、`このまま`。
- 下一批从 `E10-0601` 开始。

## 第四批新文法总览（0601-0800）

本批新增 `G0413 とはいえ`。其他尾声旁白和日常对话主要复用既有 `ことになる`、`とのこと`、`をきっかけに`、`らしい`、`すんなり`、`させていただく` 等条目。

| 编号 | 文法 | 首次出现 | 本批次数 |
|---|---|---|---:|
| G0413 | とはいえ：话虽如此/尽管如此 | E10-0664-1 | 1 |

## 逐句解析（0601-0800）

### E10-0601 `00:27:49,209 --> 00:27:50,377`

1. `降りる？`
   - 省略 `の/か` 的升调疑问，按 `G0018` 处理。

### E10-0602 `00:27:50,460 --> 00:27:53,588`

1. `今 降りたらマズそうだから こっから見てよう`
   - `降りたら`：`G0054`，假设“现在下去的话”。
   - `マズそう`：`G0032`，样态，“看起来不妙”。
   - `だから`：`G0088`，说明理由。
   - `こっから`：`G0026`，`ここから` 的口语缩约，表示起点/位置。
   - `見てよう`：`G0201`，`見ていよう` 的口语形，表示“先继续看着吧”的意向。

### E10-0603 `00:27:53,672 --> 00:27:54,839`

- `うん`：应答，跳过统计。

### E10-0604 `00:27:58,927 --> 00:28:00,095`

- `確かに`：同意回应，跳过统计。

### E10-0605 `00:28:00,178 --> 00:28:02,472`

1. `では よろしくお願いいたします`
   - `では`：`G0056`，承接话题，“那么”。
   - `お願いいたします`：`G0046` 加 `G0051` 加 `G0012`，郑重请求/寒暄。

### E10-0606 `00:28:02,555 --> 00:28:03,807`

- `こちらこそ`：寒暄回应，跳过统计。

### E10-0607 `00:28:03,890 --> 00:28:05,433`

1. `落ち着いたら ゆっくりご飯でも`
   - `落ち着いたら`：`G0054`，等事情平稳后。
   - `ご飯でも`：`でも` 表示轻松提议，“吃个饭什么的”，按普通副助词短注，不新增编号。

### E10-0608 `00:28:05,517 --> 00:28:06,768`

- `是非是非`：应答/接受邀请，跳过统计。

### E10-0609 `00:28:06,851 --> 00:28:08,478`

1. `是非　じゃ 失礼しますね`
   - `じゃ`：`G0056`，承接并结束对话。
   - `失礼します`：`G0012`，礼貌告辞。
   - `ね`：`G0034`，柔和语气。

### E10-0610 `00:28:08,561 --> 00:28:09,938`

1. `お気をつけて`
   - `お〜て`：`G0046`，敬语框架；后面省略 `ください`，表示礼貌提醒。

### E10-0611 `00:28:10,772 --> 00:28:12,107`

- `（ドアの開閉ボタンを押す音）`：音效，跳过统计。

### E10-0612 `00:28:13,024 --> 00:28:13,983`

- `あれ？`：反应语，跳过统计。

### E10-0613 `00:28:14,943 --> 00:28:17,195`

1. `開かないですか？`
   - `開かない`：`G0021`，否定。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0614 `00:28:17,278 --> 00:28:18,113`

- `（ドアの開閉ボタンを押す音）`：音效，跳过统计。

### E10-0615 `00:28:18,196 --> 00:28:19,656`

1. `これって ここ押すだけですよね？`
   - `これって`：`G0064`，口语提题。
   - `押すだけ`：`G0039`，限定“只要按这里”。
   - `ですよね`：`G0012` 加 `G0083` 加 `G0034`，礼貌确认。

### E10-0616 `00:28:19,739 --> 00:28:21,574`

1. `そうなんです`
   - `そうなんです`：`G0003` 加 `G0066` 加 `G0012`，确认并说明“是这样”。

### E10-0617 `00:28:21,658 --> 00:28:24,494`

1. `どうしちゃったのかしらね？`
   - `どう`：`G0093`，询问状态。
   - `しちゃった`：`G0133` 加 `G0001`，`してしまった` 的口语缩约，带意外/不妙感。
   - `のか`：`G0072`，嵌入疑问。
   - `かしらね`：`G0099` 加 `G0034`，女性化/柔和的自问。

### E10-0618 `00:28:24,577 --> 00:28:25,829`

1. `開かないですね`
   - `開かない`：`G0021`，打不开。
   - `ですね`：`G0012` 加 `G0034`，礼貌确认。
2. `あららら…`
   - 反应语，跳过统计。

### E10-0619 `00:28:25,912 --> 00:28:27,122`

1. `ごめんなさい そっち開きますか？`
   - `ごめんなさい`：道歉固定语，跳过统计。
   - `開きますか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0620 `00:28:27,205 --> 00:28:28,456`

- `（ドアの開閉ボタンを押す音）`：音效，跳过统计。

### E10-0621 `00:28:28,540 --> 00:28:30,583`

1. `開かないですか？`
   - `開かない`：`G0021`，否定。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。
2. `開かないですね`
   - `開かない`：`G0021`，重复确认。
   - `ですね`：`G0012` 加 `G0034`，礼貌确认。

### E10-0622 `00:28:30,667 --> 00:28:31,876`

- `（はしゃぎ声）`：声音描写，跳过统计。

### E10-0623 `00:28:31,960 --> 00:28:34,421`

1. `でも 前のドア 手動で開きますよ`
   - `前のドア`：`G0004`，名词修饰。
   - `手動で`：`G0008`，手段。
   - `開きますよ`：`G0012` 加 `G0083`，礼貌告知。

### E10-0624 `00:28:34,504 --> 00:28:35,672`

- `えっ？`：反应语，跳过统计。

### E10-0625 `00:28:36,881 --> 00:28:39,175`

1. `じゃ 前から逃げられちゃうんだ`
   - `じゃ`：`G0056`，承接推论。
   - `前から`：`G0026`，从前门。
   - `逃げられちゃう`：`G0069` 加 `G0133`，可能逃走并带不妙结果感。
   - `んだ`：`G0066`，领会式说明。

### E10-0626 `00:28:39,259 --> 00:28:41,052`

1. `はい`
   - 应答，跳过统计。
2. `どうしよう`
   - `どうしよう`：`G0201`，自问“怎么办”。

### E10-0627 `00:28:41,136 --> 00:28:42,470`

1. `じゃ やっぱり 写真 撮りに行く？`
   - `じゃ`：`G0056`，承接。
   - `撮りに行く`：`G0214`，`Vます形词干＋に行く`，表示去拍照。
   - 句末升调疑问，按 `G0018` 处理。

### E10-0628 `00:28:42,554 --> 00:28:44,764`

1. `それしかないよね`
   - `しかない`：`G0181`，只有这个办法。
   - `よね`：`G0083` 加 `G0034`，确认共识。

### E10-0629 `00:28:45,306 --> 00:28:46,516`

1. `あっ 待って`
   - `待って`：`G0228`，句末て形轻请求/制止。

### E10-0630 `00:28:47,892 --> 00:28:48,935`

1. `俺が行くわ`
   - `俺が`：`G0010`，焦点主语。
   - `行くわ`：`G0268`，句末 `わ` 表示自我决定。

### E10-0631 `00:28:50,478 --> 00:28:51,521`

1. `ちょっと これ借りますね`
   - `借ります`：`G0012`，礼貌告知。
   - `ね`：`G0034`，缓和。

### E10-0632 `00:28:51,604 --> 00:28:52,772`

- `あっ はい`：应答，跳过统计。

### E10-0633 `00:29:05,368 --> 00:29:07,078`

1. `ちょっと失礼します`
   - `失礼します`：`G0012`，礼貌告知动作。

### E10-0634 `00:29:08,121 --> 00:29:09,456`

- `あ～…`：反应/犹豫声，跳过统计。

### E10-0635 `00:29:13,710 --> 00:29:14,544`

- `ンッ…`：声音描写，跳过统计。

### E10-0636 `00:29:14,627 --> 00:29:16,421`

1. `大丈夫ですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌询问。
2. `あっ…`
   - 反应语，跳过统计。

### E10-0637 `00:29:16,504 --> 00:29:19,382`

1. `すいません なんか 電気 落ちちゃったみたいで`
   - `なんか`：`G0062`，话语填充。
   - `落ちちゃった`：`G0133` 加 `G0001`，`落ちてしまった` 的口语缩约。
   - `みたいで`：`G0091` 加 `G0052`，似乎是电断了，作为原因说明。

### E10-0638 `00:29:20,842 --> 00:29:24,512`

- `（鼓動音）`：音效，跳过统计。

### E10-0639 `00:29:32,020 --> 00:29:33,229`

- `えっ？`：反应语，跳过统计。

### E10-0640 `00:29:37,734 --> 00:29:39,110`

- `すごっ！ / ヤバッ…`：感叹，跳过统计。

### E10-0641 `00:29:39,194 --> 00:29:40,236`

- `なに これ…`：反应语，跳过统计。

### E10-0642 `00:29:40,320 --> 00:29:41,988`

- `すっご…`：感叹，跳过统计。

### E10-0643 `00:29:42,071 --> 00:29:44,574`

1. `スパイダーマンだ！`
   - `だ`：`G0012`，普通体断定。

### E10-0644 `00:30:23,071 --> 00:30:25,949`

- `アア… ハァハァ…`：喘息，跳过统计。

### E10-0645 `00:30:26,032 --> 00:30:27,200`

- `（ドアを開けようとする音）`：动作音，跳过统计。

### E10-0646 `00:30:27,784 --> 00:30:30,703`

1. `こっちも開かない`
   - `も`：`G0011`，这边也。
   - `開かない`：`G0021`，否定。

### E10-0647 `00:30:31,120 --> 00:30:33,665`

1. `手動でもですか？`
   - `でも`：表示“即使用手动也”，按普通副助词短注。
   - `ですか`：`G0012` 加 `G0002`，礼貌惊讶确认。

### E10-0648 `00:30:33,748 --> 00:30:35,583`

1. `いや なんでだろう？`
   - `なんで`：`G0061`，询问原因。
   - `だろう`：`G0094`，推量/自问。

### E10-0649 `00:30:35,667 --> 00:30:38,628`

- `（パトカーのサイレン）`：音效，跳过统计。

### E10-0650 `00:30:41,381 --> 00:30:43,883`

1. `警察です 話があるから出てきなさい`
   - `です`：`G0012`，礼貌断定。
   - `話がある`：`G0010` 加 `G0075`，有话要说。
   - `から`：`G0088`，理由。
   - `出てきなさい`：`G0024` 加 `G0221`，要求对方出来。

### E10-0651 `00:30:46,344 --> 00:30:48,680`

1. `これ 何だ？ ガムテープか？`
   - `何だ`：`G0096` 加 `G0012`，询问这是什么。
   - `か`：`G0002`，疑问。

### E10-0652 `00:30:48,763 --> 00:30:51,808`

1. `こうして オーナーと市長は`
   - `オーナーと市長`：`G0016`，并列。
   - `は`：`G0013`，话题；句子延续到下一块。

### E10-0653 `00:30:51,891 --> 00:30:54,686`

1. `警察に任意同行を求められ…`
   - `警察に`：`G0017`，动作关联对象。
   - `任意同行を`：`G0031`，宾语。
   - `求められ`：`G0006` 加 `G0117`，被要求并作叙述中止。

### E10-0654 `00:30:54,769 --> 00:30:56,938`

1. `個人的に借りたお金ですって`
   - `個人的に`：`G0017`，副词化。
   - `借りたお金`：`G0001` 加 `G0007`，过去动作修饰名词。
   - `ですって`：`G0012` 加 `G0156`，转述“说是私人借款”。

### E10-0655 `00:30:57,021 --> 00:31:00,817`

1. `２人とも 最初は否定していたものの`
   - `２人とも`：`G0011`，两人都。
   - `最初は`：`G0013`，话题。
   - `否定していた`：`G0030` 加 `G0001`，过去持续状态。
   - `ものの`：`G0335`，虽然……但是。

### E10-0656 `00:31:01,985 --> 00:31:06,155`

1. `職員の証言と 持っていた2000万円`
   - `職員の証言`：`G0004`，名词修饰。
   - `証言と`：`G0016`，并列。
   - `持っていた`：`G0030` 加 `G0001`，过去拥有状态。

### E10-0657 `00:31:07,532 --> 00:31:11,786`

1. `更に 家宅捜索で出てきた合意書によって`
   - `更に`：`G0355`，递进补充。
   - `家宅捜索で`：`G0008`，场景/手段。
   - `出てきた`：`G0024` 加 `G0001`，出现出来。
   - `合意書によって`：`G0005`，依据/原因。

### E10-0658 `00:31:11,869 --> 00:31:14,372`

1. `２人は容疑を認めた`
   - `２人は`：`G0013`，话题。
   - `容疑を`：`G0031`，宾语。
   - `認めた`：`G0001`，过去。

### E10-0659 `00:31:14,455 --> 00:31:15,665`

- `はい`：应答，跳过统计。

### E10-0660 `00:31:16,124 --> 00:31:17,834`

1. `間違いありません`
   - `間違いありません`：`G0364` 加 `G0012`，郑重断定“没有错”。

### E10-0661 `00:31:20,837 --> 00:31:22,547`

- `その結果`：叙述承接，跳过统计。

### E10-0662 `00:31:22,964 --> 00:31:26,050`

1. `ホテルの売却は白紙となった`
   - `ホテルの売却`：`G0004`，名词修饰。
   - `売却は`：`G0013`，话题。
   - `白紙となった`：`G0048` 加 `G0001`，变成作废/归零的状态。

### E10-0663 `00:31:26,134 --> 00:31:27,635`

- `すみませんでした`：道歉固定语，跳过统计。

### E10-0664 `00:31:27,969 --> 00:31:31,931`

1. `とはいえ 所有権はまだオーナーにあるため`
   - `とはいえ`：`G0413`，话虽如此，承认前文后提出保留。
   - `所有権は`：`G0013`，话题。
   - `オーナーにある`：`G0017` 加 `G0075`，所有权还在老板手里。
   - `ため`：`G0028`，原因。

### E10-0665 `00:31:32,015 --> 00:31:34,142`

1. `どうなるかは分からないけど`
   - `どうなる`：`G0093` 加 `G0048`，会变成什么样。
   - `かは`：`G0072` 加 `G0013`，把嵌入疑问作话题。
   - `分からない`：`G0021`，否定。
   - `けど`：`G0019`，转折铺垫。

### E10-0666 `00:31:35,184 --> 00:31:37,103`

1. `支配人の話では`
   - `支配人の話`：`G0004`，名词修饰。
   - `では`：话题/依据提示，按普通承接短注。

### E10-0667 `00:31:37,186 --> 00:31:40,523`

1. `ホテルがなくなることはないとのこと`
   - `ホテルが`：`G0010`，主语。
   - `なくなる`：`G0022`，变得不存在。
   - `ことはない`：`G0097` 加 `G0021`，把“会消失”名词化后否定。
   - `とのこと`：`G0384`，报告称/据说。

### E10-0668 `00:31:43,776 --> 00:31:45,445`

1. `とりあえず`
   - `とりあえず`：`G0185`，暂且/总之先。

### E10-0669 `00:31:45,528 --> 00:31:49,616`

1. `高橋さんは これからも温泉に入れそうだし…`
   - `高橋さんは`：`G0013`，话题。
   - `も`：`G0011`，今后也。
   - `温泉に`：`G0017`，进入的对象/地点。
   - `入れそう`：`G0069` 加 `G0032`，看起来还能泡温泉。
   - `し`：`G0110`，补充理由。

### E10-0670 `00:31:50,742 --> 00:31:51,784`

- `（車の開錠音）`：音效，跳过统计。

### E10-0671 `00:32:12,013 --> 00:32:16,768`

1. `この町も きっと救えたはず`
   - `町も`：`G0011`，这座城镇也。
   - `救えた`：`G0069` 加 `G0001`，过去可能/成功救下。
   - `はず`：`G0273`，按理应该。

### E10-0672 `00:32:28,613 --> 00:32:30,448`

- `こんにちは`：寒暄，跳过统计。

### E10-0673 `00:32:30,531 --> 00:32:31,699`

- `こんにちは`：寒暄，跳过统计。

### E10-0674 `00:32:33,201 --> 00:32:37,747`

1. `今日は イオンで 若葉の新しいバッシュを買ったあと`
   - `今日は`：`G0013`，话题。
   - `イオンで`：`G0008`，场所。
   - `若葉の新しいバッシュ`：`G0004`，名词修饰。
   - `買ったあと`：`G0001` 加 `G0058`，买完之后。

### E10-0675 `00:32:37,830 --> 00:32:39,791`

- `ジョナさまで ご飯`：名词省略句，按场景说明跳过统计。

### E10-0676 `00:32:39,874 --> 00:32:41,334`

1. `それで逮捕ができたの`
   - `それで`：`G0067`，于是/所以。
   - `逮捕ができた`：`G0010` 加 `G0069` 加 `G0001`，能够逮捕。
   - `の`：`G0097`，说明性名词化。

### E10-0677 `00:32:41,417 --> 00:32:45,797`

1. `この数か月 さすがにいろいろありすぎたので`
   - `ありすぎた`：`G0179` 加 `G0001`，发生太多了。
   - `ので`：`G0052`，原因。

### E10-0678 `00:32:45,880 --> 00:32:48,800`

1. `私は 高橋さんに許可を取り`
   - `私は`：`G0013`，话题。
   - `高橋さんに`：`G0017`，取得许可的对象。
   - `許可を`：`G0031`，宾语。
   - `取り`：`G0117`，连用形中止。

### E10-0679 `00:32:48,883 --> 00:32:52,887`

1. `これまでの全てを若葉に話すことにした`
   - `これまでの全て`：`G0004`，到目前为止的一切。
   - `全てを`：`G0031`，宾语。
   - `若葉に`：`G0017`，告知对象。
   - `話すことにした`：`G0274` 加 `G0001`，决定告诉若叶。

### E10-0680 `00:32:54,013 --> 00:32:56,808`

1. `静岡のおじいちゃんと一緒だ`
   - `静岡のおじいちゃん`：`G0004`，名词修饰。
   - `と`：`G0016`，比较/并列对象。
   - `だ`：`G0012`，断定。

### E10-0681 `00:32:58,142 --> 00:32:59,185`

- `うん？`：反应语，跳过统计。

### E10-0682 `00:32:59,727 --> 00:33:00,895`

1. `えっ どういうこと？`
   - `どういうこと`：`G0080` 加 `G0097`，询问“怎么回事”。

### E10-0683 `00:33:00,979 --> 00:33:03,147`

1. `静岡のおじいちゃん 宇宙人じゃん`
   - `静岡のおじいちゃん`：`G0004`，名词修饰。
   - `じゃん`：`G0092`，提醒式确认。

### E10-0684 `00:33:04,357 --> 00:33:05,191`

1. `聞いてないの？`
   - `聞いてない`：`G0030` 加 `G0021`，没有听说/不知道。
   - `の`：`G0097`，说明性疑问。

### E10-0685 `00:33:05,274 --> 00:33:09,487`

1. `静岡のおじいちゃんとは 元夫の父`
   - `静岡のおじいちゃんとは`：`G0060`，用 `とは` 提出定义对象。
   - `元夫の父`：`G0004`，名词修饰。

### E10-0686 `00:33:09,570 --> 00:33:10,488`

- `聞いてない`：单句否定回应，已在前块说明，这里简注跳过。

### E10-0687 `00:33:10,571 --> 00:33:14,242`

1. `ということは 元夫と若葉も`
   - `ということは`：`G0190`，由前项推出结论。
   - `元夫と若葉`：`G0016`，并列。
   - `も`：`G0011`，也。

### E10-0688 `00:33:14,325 --> 00:33:17,620`

1. `宇宙人の血を引いているということ`
   - `宇宙人の血`：`G0004`，名词修饰。
   - `血を`：`G0031`，宾语。
   - `引いている`：`G0030`，持续状态。
   - `ということ`：`G0190`，也就是说/这回事。

### E10-0689 `00:33:17,704 --> 00:33:19,122`

1. `クオーターとかだから`
   - `とか`：`G0063`，模糊列举/举例。
   - `だから`：`G0088`，说明原因。

### E10-0690 `00:33:19,205 --> 00:33:21,541`

1. `こっちの人と あんま変わんないみたいだけどね`
   - `こっちの人`：`G0004`，名词修饰。
   - `人と`：`G0016`，比较对象。
   - `あんま変わんない`：`G0037` 加 `G0021`，`あまり変わらない` 的口语。
   - `みたい`：`G0091`，似乎。
   - `だけどね`：`G0019` 加 `G0034`，转折缓和。

### E10-0691 `00:33:22,625 --> 00:33:24,419`

- `クオーター？ / うん`：复述确认和应答，跳过统计。

### E10-0692 `00:33:24,502 --> 00:33:27,630`

1. `お母さんさ よく化粧水 持って帰ってくれるじゃん`
   - `さ`：`G0131`，口语提示。
   - `持って帰ってくれる`：`G0090`，别人为我方带回来。
   - `じゃん`：`G0092`，提醒对方共有信息。

### E10-0693 `00:33:27,714 --> 00:33:28,548`

- `化粧水？`：复述确认，跳过统计。

### E10-0694 `00:33:28,631 --> 00:33:31,009`

1. `温泉の成分が入ったやつ`
   - `温泉の成分`：`G0004`，名词修饰。
   - `成分が`：`G0010`，主语。
   - `入ったやつ`：`G0001` 加 `G0007`，过去动作修饰名词。

### E10-0695 `00:33:31,092 --> 00:33:32,552`

1. `だから 普通に知ってると思ってた`
   - `だから`：`G0088`，所以。
   - `知ってる`：`G0030`，知道的状态。
   - `と思ってた`：`G0023` 加 `G0030` 加 `G0001`，过去一直以为。

### E10-0696 `00:33:32,635 --> 00:33:33,761`

- `ああ…`：反应语，跳过统计。

### E10-0697 `00:33:34,303 --> 00:33:36,097`

1. `えっ いや あれは`
   - `あれは`：`G0013`，话题；句子延续。

### E10-0698 `00:33:36,180 --> 00:33:39,058`

1. `もらえるから 持って帰ってただけで`
   - `もらえる`：`G0141`，能够得到。
   - `から`：`G0088`，理由。
   - `持って帰ってた`：`G0030` 加 `G0001`，过去习惯/状态。
   - `だけ`：`G0039`，限定“只是”。
   - `で`：`G0008`，以未完结的状态接续后文。

### E10-0699 `00:33:39,142 --> 00:33:40,852`

1. `そうなんだ`
   - `そうなんだ`：`G0003` 加 `G0066`，接受新信息。
2. `うん`
   - 应答，跳过统计。

### E10-0700 `00:33:41,936 --> 00:33:43,980`

1. `えっ… じゃ 若葉って`
   - `じゃ`：`G0056`，承接推论。
   - `若葉って`：`G0064`，口语提题；句子延续。

### E10-0701 `00:33:44,063 --> 00:33:45,857`

1. `あれがないと免疫が落ちちゃうの？`
   - `あれが`：`G0010`，主语。
   - `ないと`：`G0166`，如果没有。
   - `免疫が`：`G0010`，主语。
   - `落ちちゃう`：`G0133`，不由得/不妙地下降。
   - `の`：`G0097`，说明性疑问。

### E10-0702 `00:33:45,940 --> 00:33:47,900`

1. `いや そこまでじゃなくて`
   - `そこまでじゃなくて`：`G0043` 加 `G0035`，不是到那种程度，并连接后文。

### E10-0703 `00:33:47,984 --> 00:33:51,362`

1. `まあ たまにつけてたら 風邪ひかないぐらいかな`
   - `つけてたら`：`G0030` 加 `G0054`，如果偶尔涂着。
   - `ひかない`：`G0021`，不会感冒。
   - `ぐらい`：`G0027`，程度。
   - `かな`：`G0099`，不确定推测。

### E10-0704 `00:33:52,697 --> 00:33:54,449`

- `そうなんだ？ / うん`：理解确认和应答，跳过统计。

### E10-0705 `00:33:55,241 --> 00:33:57,618`

1. `お父さんは？`
   - `は`：`G0013`，把“父亲”作为新话题。

### E10-0706 `00:33:57,702 --> 00:34:01,372`

1. `お父さんは温泉につかったほうがいいみたい`
   - `お父さんは`：`G0013`，话题。
   - `温泉に`：`G0017`，进入的对象。
   - `つかったほうがいい`：`G0170`，最好泡温泉。
   - `みたい`：`G0091`，听起来/似乎。

### E10-0707 `00:34:01,456 --> 00:34:04,000`

- `へえ～ そうなんだ / うん`：反应和应答，跳过统计。

### E10-0708 `00:34:04,083 --> 00:34:05,376`

- `レイクホテル？`：复述确认，跳过统计。

### E10-0709 `00:34:05,460 --> 00:34:08,379`

1. `静岡にいい温泉があるみたいで`
   - `静岡に`：`G0017`，地点。
   - `温泉がある`：`G0010` 加 `G0075`，存在。
   - `みたいで`：`G0091` 加 `G0052`，似乎有，并连接后文。

### E10-0710 `00:34:08,463 --> 00:34:10,590`

1. `月１くらいでそこに行ってるらしい`
   - `月１くらい`：`G0027`，大约每月一次。
   - `そこに`：`G0017`，目的地。
   - `行ってる`：`G0030`，习惯性动作。
   - `らしい`：`G0065`，传闻。

### E10-0711 `00:34:10,673 --> 00:34:12,216`

- `へえ～`：反应语，跳过统计。

### E10-0712 `00:34:12,300 --> 00:34:14,927`

1. `ここは どこでしょうか？`
   - `ここは`：`G0013`，话题。
   - `どこ`：`G0096`，疑问词。
   - `でしょうか`：`G0094` 加 `G0002`，礼貌推量疑问。

### E10-0713 `00:34:15,011 --> 00:34:16,387`

- `そう`：回应，跳过统计。

### E10-0714 `00:34:16,471 --> 00:34:19,807`

1. `さっき 若葉から聞いてびっくりしたんだけど`
   - `若葉から`：`G0026`，消息来源。
   - `聞いて`：`G0035`，て形连接。
   - `びっくりした`：`G0001`，过去。
   - `んだけど`：`G0066` 加 `G0019`，说明并铺垫。

### E10-0715 `00:34:19,891 --> 00:34:21,601`

1. `俺 言ったよ`
   - `言った`：`G0001`，过去。
   - `よ`：`G0083`，告知。

### E10-0716 `00:34:23,394 --> 00:34:24,395`

- `うん`：应答，跳过统计。

### E10-0717 `00:34:24,937 --> 00:34:28,107`

1. `俺が運動神経いいって話になって`
   - `俺が`：`G0010`，主语。
   - `いいって`：`G0064`，引用内容。
   - `話になって`：`G0048` 加 `G0035`，话题变成“运动神经好”。

### E10-0718 `00:34:28,399 --> 00:34:31,277`

1. `宇宙人の血 引いてるからね”っつったら…`
   - `宇宙人の血`：`G0004`，名词修饰。
   - `引いてる`：`G0030`，持续状态。
   - `からね`：`G0088` 加 `G0034`，说明理由。
   - `っつったら`：`G0060` 加 `G0054`，`って言ったら` 的粗口语缩约。

### E10-0719 `00:34:33,279 --> 00:34:35,072`

1. `いや 普通に笑ってたけど`
   - `笑ってた`：`G0030` 加 `G0001`，过去状态。
   - `けど`：`G0019`，转折。

### E10-0720 `00:34:35,156 --> 00:34:36,991`

- `あ～…`：反应语，跳过统计。

### E10-0721 `00:34:37,325 --> 00:34:41,037`

1. `そうなんだ？ まあ ８分の１だもんね`
   - `そうなんだ`：`G0003` 加 `G0066`，理解确认。
   - `８分の１`：`G0004`，分数表达中的 `の`。
   - `だもんね`：`G0150` 加 `G0034`，说明理由并求共感。

### E10-0722 `00:34:41,120 --> 00:34:44,707`

1. `驚いたけど もう ゆるゆるなので`
   - `驚いた`：`G0001`，过去。
   - `けど`：`G0019`，转折。
   - `なので`：`G0052`，原因。

### E10-0723 `00:34:44,791 --> 00:34:47,001`

1. `すんなり受け入れられた`
   - `すんなり`：`G0378`，顺利/轻松地。
   - `受け入れられた`：`G0069` 加 `G0001`，能够接受。

### E10-0724 `00:34:48,085 --> 00:34:50,463`

1. `若葉 何か話すことある？`
   - `何か`：`G0096`，不定。
   - `話すこと`：`G0097`，要说的事。
   - `ある`：`G0075`，存在。

### E10-0725 `00:34:50,546 --> 00:34:52,131`

- `大丈夫 / ふ～ん…`：应答和反应，跳过统计。

### E10-0726 `00:34:53,007 --> 00:34:54,258`

1. `じゃ…`
   - `じゃ`：`G0056`，承接告别。

### E10-0727 `00:34:54,342 --> 00:34:56,719`

- `うん は～い`：应答，跳过统计。

### E10-0728 `00:35:04,811 --> 00:35:07,105`

1. `校外学習で行った…`
   - `校外学習で`：`G0008`，场景。
   - `行った`：`G0001`，过去。

### E10-0729 `00:35:07,188 --> 00:35:10,066`

- `信玄餅工場`：名词短句，跳过统计。

### E10-0730 `00:35:10,483 --> 00:35:14,529`

1. `自分で包んだ信玄餅を食べました`
   - `自分で`：`G0008`，动作主体/手段。
   - `包んだ信玄餅`：`G0001` 加 `G0007`，过去动作修饰名词。
   - `信玄餅を`：`G0031`，宾语。
   - `食べました`：`G0012` 加 `G0001`，礼貌过去。

### E10-0731 `00:35:14,612 --> 00:35:17,156`

1. `先生が挟んだ…`
   - `先生が`：`G0010`，主语。
   - `挟んだ`：`G0001`，过去；句子延续。

### E10-0732 `00:35:17,615 --> 00:35:20,409`

- `バレーボール`：名词短句，跳过统计。

### E10-0733 `00:35:20,952 --> 00:35:24,747`

1. `いつの間にかなくなっていました`
   - `いつの間にか`：`G0345`，不知不觉间。
   - `なくなっていました`：`G0022` 加 `G0030` 加 `G0012` 加 `G0001`，已经不见了。

### E10-0734 `00:35:24,831 --> 00:35:26,749`

- `フフフフッ…`：笑声，跳过统计。

### E10-0735 `00:35:26,833 --> 00:35:30,586`

1. `この６年間でたくさんのことを学び…`
   - `６年間で`：`G0008`，时间范围。
   - `たくさんのこと`：`G0004` 加 `G0097`，许多事情。
   - `ことを`：`G0031`，宾语。
   - `学び`：`G0117`，连用形中止。

### E10-0736 `00:35:31,045 --> 00:35:34,340`

1. `そして たくさんの思い出を胸に…`
   - `たくさんの思い出`：`G0004`，名词修饰。
   - `思い出を`：`G0031`，宾语。
   - `胸に`：`G0017`，抽象承载位置。

### E10-0737 `00:35:35,091 --> 00:35:37,135`

- `僕たち…`：句子未完，跳过统计。

### E10-0738 `00:35:37,802 --> 00:35:39,929`

- `私たちは…`：句子未完，跳过统计。

### E10-0739 `00:35:40,513 --> 00:35:43,099`

1. `卒業します`
   - `卒業します`：`G0029` 加 `G0012`，名词＋する的礼貌体。

### E10-0740 `00:35:45,143 --> 00:35:46,853`

1. `まあ 慣れですよね`
   - `ですよね`：`G0012` 加 `G0083` 加 `G0034`，礼貌确认。

### E10-0741 `00:35:46,936 --> 00:35:48,187`

- `慣れ？ / ええ`：复述确认和应答，跳过统计。

### E10-0742 `00:35:48,271 --> 00:35:49,647`

1. `それって単純に`
   - `それって`：`G0064`，口语提题；后续判断延续到下一块。

### E10-0743 `00:35:49,730 --> 00:35:52,150`

1. `遅く開ければいいだけなんじゃないですか？`
   - `遅く`：`G0022`，形容词副词化。
   - `開ければいい`：`G0081` 加 `G0082`，只要慢点打开就行。
   - `だけ`：`G0039`，限定。
   - `なんじゃないですか`：`G0098` 加 `G0066` 加 `G0012` 加 `G0002`，确认式反问。

### E10-0744 `00:35:52,233 --> 00:35:53,943`

1. `まあ 素人は そう思いますよね`
   - `素人は`：`G0013`，话题。
   - `そう思います`：`G0023` 加 `G0012`，那样想。
   - `よね`：`G0083` 加 `G0034`，共感确认。

### E10-0745 `00:35:54,026 --> 00:35:55,027`

1. `あっ 違うんですか？`
   - `んですか`：`G0066` 加 `G0012` 加 `G0002`，带说明期待的疑问。

### E10-0746 `00:35:55,111 --> 00:35:57,321`

1. `違いますよ あの… 年齢とか雰囲気から`
   - `違いますよ`：`G0012` 加 `G0083`，礼貌告知。
   - `年齢とか雰囲気`：`G0063`，列举。
   - `から`：`G0026`，判断来源。

### E10-0747 `00:35:57,405 --> 00:36:00,157`

1. `この人は早そう”とか “この人は時間かかるな”とか`
   - `この人は`：`G0013`，话题。
   - `早そう`：`G0032`，样态。
   - `とか`：`G0063`，列举判断内容。
   - `な`：`G0131`，口语感叹/判断语气。

### E10-0748 `00:36:00,241 --> 00:36:01,159`

1. `読み取るんですよ`
   - `んです`：`G0066` 加 `G0012`，说明原理。
   - `よ`：`G0083`，告知。

### E10-0749 `00:36:01,242 --> 00:36:03,286`

1. `それは すごいですね`
   - `それは`：`G0013`，话题。
   - `ですね`：`G0012` 加 `G0034`，礼貌评价。

### E10-0750 `00:36:03,369 --> 00:36:04,662`

- `“ててて！”ですね`：拟声/节目用语，跳过统计。

### E10-0751 `00:36:04,745 --> 00:36:06,956`

1. `私 検尿カップ回収の大会とかあったら`
   - `回収の大会`：`G0004`，名词修饰。
   - `大会とか`：`G0063`，举例。
   - `あったら`：`G0075` 加 `G0054`，如果有；句子延续到下一块。

### E10-0752 `00:36:07,039 --> 00:36:09,250`

1. `結構いいとこ いくと思いますよ`
   - `いいとこ`：`G0148`，`ところ` 的口语缩约。
   - `と思います`：`G0023` 加 `G0012`，表达想法。
   - `よ`：`G0083`，告知。

### E10-0753 `00:36:09,625 --> 00:36:11,460`

1. `てか これ 絶対 使ってくださいね`
   - `てか`：`G0074`，话题转换/补充。
   - `使ってください`：`G0053`，请求。
   - `ね`：`G0034`，强调提醒。

### E10-0754 `00:36:19,510 --> 00:36:21,637`

1. `またリバウンド取った！`
   - `取った`：`G0001`，过去/完成。

### E10-0755 `00:36:21,721 --> 00:36:23,055`

1. `若葉ちゃん ずばぬけてません？`
   - `てません`：`G0030` 加 `G0012` 加 `G0002`，礼貌否定疑问，实际为确认“是不是很突出”。

### E10-0756 `00:36:23,139 --> 00:36:24,807`

1. `なんか もう既に 何校からか`
   - `なんか`：`G0062`，话语填充。
   - `何校からか`：`G0096` 加 `G0026`，从好几所学校；句子延续到下一块。

### E10-0757 `00:36:24,891 --> 00:36:26,225`

1. `特待の誘いが来てるらしい`
   - `特待の誘い`：`G0004`，名词修饰。
   - `誘いが`：`G0010`，主语。
   - `来てる`：`G0030`，已经来了的结果状态。
   - `らしい`：`G0065`，传闻。

### E10-0758 `00:36:26,309 --> 00:36:27,351`

1. `そりゃ来るな`
   - `な`：`G0131`，口语感叹/判断语气。

### E10-0759 `00:36:27,435 --> 00:36:28,728`

- `リバウンド！`：呼喊，跳过统计。

### E10-0760 `00:36:28,811 --> 00:36:30,479`

1. `またリバウンド取った`
   - `取った`：`G0001`，过去/完成。

### E10-0761 `00:36:30,563 --> 00:36:31,939`

1. `跳躍力 すごくない？`
   - `すごくない`：`G0098`，イ形容词否定形表确认反问。

### E10-0762 `00:36:32,023 --> 00:36:34,567`

1. `さすが南中バレー部エースの血ですね`
   - `エースの血`：`G0004`，名词修饰。
   - `ですね`：`G0012` 加 `G0034`，礼貌评价。

### E10-0763 `00:36:34,650 --> 00:36:35,568`

- `ハハハハッ…`：笑声，跳过统计。

### E10-0764 `00:36:35,651 --> 00:36:36,652`

- `いや…`：话语填充，跳过统计。

### E10-0765 `00:36:36,736 --> 00:36:39,488`

1. `多分 宇宙人の血`
   - `宇宙人の血`：`G0004`，名词修饰。

### E10-0766 `00:36:42,700 --> 00:36:45,202`

1. `あの事件から半年後`
   - `事件から`：`G0026`，起点。
   - `半年後`：`G0058`，时间后。

### E10-0767 `00:36:45,661 --> 00:36:48,331`

1. `新しいオーナーが誕生した`
   - `オーナーが`：`G0010`，主语。
   - `誕生した`：`G0029` 加 `G0001`，名词＋する的过去形。

### E10-0768 `00:36:49,290 --> 00:36:51,626`

1. `この度 レイクホテル浅ノ湖の`
   - `レイクホテル浅ノ湖の`：`G0004`，名词修饰；句子延续。

### E10-0769 `00:36:51,709 --> 00:36:54,879`

1. `オーナーに 就任させていただくことになりました`
   - `オーナーに`：`G0017`，就任目标身份。
   - `就任させていただく`：`G0266`，请允许/承蒙让我就任。
   - `ことになりました`：`G0142` 加 `G0012` 加 `G0001`，形成安排/结果。

### E10-0770 `00:36:54,962 --> 00:36:56,422`

1. `小野寺充です`
   - `です`：`G0012`，礼貌断定。

### E10-0771 `00:36:56,505 --> 00:36:58,591`

- `（拍手）`：音效，跳过统计。

### E10-0772 `00:36:58,674 --> 00:37:01,135`

1. `なぜ こうなったかというと`
   - `こうなった`：`G0048` 加 `G0001`，变成这样。
   - `か`：`G0072`，嵌入疑问。
   - `かというと`：`G0177`，引出解释“要说为什么”。

### E10-0773 `00:37:03,137 --> 00:37:06,724`

1. `今回 市長たちが捕まったことによって`
   - `市長たちが`：`G0010`，主语。
   - `捕まったこと`：`G0001` 加 `G0097`，把被捕事件名词化。
   - `によって`：`G0005`，因……而。

### E10-0774 `00:37:06,807 --> 00:37:09,644`

1. `村上さんが帰った50年後の浅田は`
   - `村上さんが`：`G0010`，主语。
   - `帰った`：`G0001`，过去。
   - `50年後の浅田`：`G0058` 加 `G0004`，50年后的浅田。
   - `浅田は`：`G0013`，话题。

### E10-0775 `00:37:09,727 --> 00:37:12,355`

1. `自然豊かな町に戻り`
   - `町に`：`G0017`，变化后的状态/目标。
   - `戻り`：`G0117`，连用形中止。

### E10-0776 `00:37:12,438 --> 00:37:14,148`

1. `富士山も世界遺産に`
   - `富士山も`：`G0011`，富士山也。
   - `世界遺産に`：`G0017`，登记目标；句子延续到下一块。

### E10-0777 `00:37:14,231 --> 00:37:16,984`

1. `登録されたままになっていたらしく`
   - `登録された`：`G0006` 加 `G0001`，被登记。
   - `ままになっていた`：`G0258` 加 `G0048` 加 `G0030` 加 `G0001`，一直保持登记状态。
   - `らしく`：`G0065`，传闻/推测的连用形。

### E10-0778 `00:37:18,527 --> 00:37:21,030`

1. `村上さんが その恩返しとして`
   - `村上さんが`：`G0010`，主语。
   - `恩返しとして`：`G0265`，作为报恩。

### E10-0779 `00:37:21,113 --> 00:37:25,159`

1. `未来で蓄えていたお金の一部を金に換えて`
   - `未来で`：`G0008`，场景。
   - `蓄えていた`：`G0030` 加 `G0001`，过去持续储蓄状态。
   - `お金の一部`：`G0004`，名词修饰。
   - `一部を`：`G0031`，宾语。
   - `金に換えて`：`G0017` 加 `G0035`，换成黄金并连接后文。

### E10-0780 `00:37:25,242 --> 00:37:27,328`

1. `こっちの世界に持ち込み…`
   - `こっちの世界`：`G0004`，名词修饰。
   - `世界に`：`G0017`，带入的目的地。
   - `持ち込み`：`G0117`，连用形中止。

### E10-0781 `00:37:27,536 --> 00:37:28,996`

1. `僕は オーナーになれないから…`
   - `僕は`：`G0013`，话题。
   - `オーナーになれない`：`G0048` 加 `G0069` 加 `G0021`，不能成为老板。
   - `から`：`G0088`，理由。

### E10-0782 `00:37:29,080 --> 00:37:31,540`

1. `このホテルでいちばんお世話になったという`
   - `ホテルで`：`G0008`，范围/场所。
   - `お世話になった`：`G0087` 加 `G0001`，承蒙照顾过。
   - `という`：`G0060`，引用说明。

### E10-0783 `00:37:31,624 --> 00:37:33,501`

1. `小野寺くんに譲り`
   - `小野寺くんに`：`G0017`，转让对象。
   - `譲り`：`G0117`，连用形中止。

### E10-0784 `00:37:33,834 --> 00:37:38,756`

1. `小野寺くんが それを換金し ホテルを買うことになった`
   - `小野寺くんが`：`G0010`，主语。
   - `それを`：`G0031`，宾语。
   - `換金し`：`G0029` 加 `G0117`，名词＋する的连用中止。
   - `ホテルを`：`G0031`，宾语。
   - `買うことになった`：`G0142` 加 `G0001`，结果变成由他买下酒店。

### E10-0785 `00:37:38,839 --> 00:37:39,674`

1. `…らしい`
   - `らしい`：`G0065`，传闻。

### E10-0786 `00:37:40,132 --> 00:37:42,176`

1. `よろしくお願いします`
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求/寒暄。

### E10-0787 `00:37:42,593 --> 00:37:46,514`

1. `ホテルつぶれなくて良かった`
   - `つぶれなくて`：`G0021` 加 `G0035`，没有倒闭而。
   - `良かった`：`G0001`，固定评价“太好了”。

### E10-0788 `00:37:46,597 --> 00:37:48,140`

1. `ウチの救世主だよね`
   - `ウチの救世主`：`G0004`，名词修饰。
   - `だよね`：`G0012` 加 `G0083` 加 `G0034`，断定并求共感。

### E10-0789 `00:37:48,224 --> 00:37:49,183`

1. `そうですね`
   - `そうですね`：`G0003` 加 `G0012` 加 `G0034`，礼貌认同。

### E10-0790 `00:37:49,266 --> 00:37:52,061`

1. `遺産相続レースには負けたけど`
   - `には`：`G0017` 加 `G0013`，对象话题化。
   - `負けた`：`G0001`，过去。
   - `けど`：`G0019`，转折。

### E10-0791 `00:37:52,144 --> 00:37:53,980`

1. `これで良かったと思う`
   - `これで`：`G0008`，状态/方式。
   - `良かった`：`G0001`，结果评价。
   - `と思う`：`G0023`，表达想法。

### E10-0792 `00:37:54,063 --> 00:37:55,523`

- `ハハハッ…`：笑声，跳过统计。

### E10-0793 `00:37:56,232 --> 00:37:58,776`

1. `小野寺くんって前言ってた若い男の子？`
   - `小野寺くんって`：`G0064`，口语提题。
   - `前言ってた`：`G0058` 加 `G0030` 加 `G0001`，之前说过的。
   - `若い男の子`：`G0004`，名词修饰。

### E10-0794 `00:37:58,859 --> 00:38:00,069`

- `うん そうそう / ふ～ん…`：应答和反应，跳过统计。

### E10-0795 `00:38:00,152 --> 00:38:01,362`

1. `なんか 聞いたら`
   - `なんか`：`G0062`，话语填充。
   - `聞いたら`：`G0054`，听了之后/一问才知道。

### E10-0796 `00:38:01,445 --> 00:38:04,865`

1. `もともと ホテルを経営するのが夢だったみたいで`
   - `もともと`：`G0320`，原本。
   - `ホテルを`：`G0031`，宾语。
   - `経営するの`：`G0029` 加 `G0097`，经营酒店这件事。
   - `のが`：`G0010`，名词化小句作主语。
   - `夢だった`：`G0001`，过去断定。
   - `みたいで`：`G0091` 加 `G0052`，似乎是，并说明原因。

### E10-0797 `00:38:04,949 --> 00:38:07,159`

1. `その勉強のために現場で働いてたんだって`
   - `その勉強のために`：`G0004` 加 `G0028`，为了学习经营。
   - `現場で`：`G0008`，场所。
   - `働いてた`：`G0030` 加 `G0001`，过去持续状态。
   - `んだって`：`G0066` 加 `G0156`，说明式转述。

### E10-0798 `00:38:07,243 --> 00:38:09,745`

1. `じゃ 小野寺くん 夢が かなったんですね`
   - `じゃ`：`G0056`，承接推论。
   - `夢が`：`G0010`，主语。
   - `かなった`：`G0001`，过去/完成。
   - `んですね`：`G0066` 加 `G0012` 加 `G0034`，理解确认。

### E10-0799 `00:38:09,829 --> 00:38:13,749`

1. `てか 高橋さん 町も救って 若者の夢もかなえちゃいましたね`
   - `てか`：`G0074`，话题推进。
   - `町も`：`G0011`，城镇也。
   - `救って`：`G0035`，て形连接。
   - `若者の夢`：`G0004`，名词修饰。
   - `夢も`：`G0011`，梦想也。
   - `かなえちゃいました`：`G0133` 加 `G0012` 加 `G0001`，带“顺手做到”的口语感。
   - `ね`：`G0034`，共感。

### E10-0800 `00:38:13,833 --> 00:38:17,003`

1. `別に 俺は自分のために やっただけだからさ`
   - `別に`：`G0104`，和后续否定/限制感搭配，表示“倒也不是特别”。
   - `俺は`：`G0013`，话题。
   - `自分のために`：`G0004` 加 `G0028`，为了自己。
   - `やった`：`G0001`，过去。
   - `だけ`：`G0039`，限定。
   - `だからさ`：`G0088` 加 `G0131`，说明理由并口语缓和。

## 第四批复盘（0601-0800）

- 本批完成警方介入、酒店保全、若叶外星血统揭示以及新老板就任的尾声铺陈。
- 新增 `G0413 とはいえ`，用于旁白中“虽然酒店出售取消，但所有权仍在老板手里”的保留说明。
- 高频复习项包括 `たら`、`そうだ` 样态、`じゃないですか`、`とのこと`、`ことにした/ことになった`、`らしい`、`ていただく`、`ために`。
- `ご飯でも`、`白紙となる`、`金に換えて` 偏词汇/搭配，本批不新增编号。


## 第五批新文法总览（0801-0965）

本批新增 `G0414-G0416`，并完成 E10 收尾。`Nがあってこそ` 和 `Nと化す` 出现在尾声对话与时间线说明中，`と言っても過言ではない` 用于全剧最后的总结旁白。

| 编号 | 文法 | 首次出现 | 本批次数 |
|---|---|---|---:|
| G0414 | Nがあってこそ：正因为有……才 | E10-0816-1 | 1 |
| G0415 | Nと化す：化为/变成…… | E10-0839-1 | 1 |
| G0416 | と言っても過言ではない：说……也不为过 | E10-0961-1 | 1 |

## 逐句解析（0801-0965）

### E10-0801 `00:38:17,086 --> 00:38:18,170`

1. `ですよね`
   - `ですよね`：`G0012` 加 `G0083` 加 `G0034`，顺着对方确认。
2. `うん？`
   - 反应语，跳过统计。

### E10-0802 `00:38:18,254 --> 00:38:20,506`

1. `まあ 結果論ですからね`
   - `です`：`G0012`，礼貌断定。
   - `から`：`G0088`，理由。
   - `ね`：`G0034`，缓和。
2. `はぁ？`
   - 反应语，跳过统计。

### E10-0803 `00:38:22,216 --> 00:38:26,554`

1. `俺の謙遜を そんな即 丸飲みしないでもらえる？`
   - `俺の謙遜`：`G0004`，名词修饰。
   - `謙遜を`：`G0031`，宾语。
   - `しないで`：`G0036`，不要照单全收。
   - `もらえる`：`G0141`，请求对方不要这样做。

### E10-0804 `00:38:26,637 --> 00:38:27,513`

- `丸飲み？`：复述确认，跳过统计。

### E10-0805 `00:38:27,596 --> 00:38:30,057`

1. `丸飲みじゃん 謙遜を`
   - `じゃん`：`G0092`，口语确认/吐槽。
   - `謙遜を`：`G0031`，宾语倒装。

### E10-0806 `00:38:30,141 --> 00:38:32,393`

1. `いや 謙遜っていうか事実だし`
   - `っていうか`：`G0074`，修正说法，“不如说”。
   - `し`：`G0110`，补充理由。

### E10-0807 `00:38:32,476 --> 00:38:36,063`

1. `いや そうだけどさ もっと なんか こう… あるじゃん`
   - `だけど`：`G0019`，转折。
   - `さ`：`G0131`，口语提示。
   - `なんか`：`G0062`，话语填充。
   - `ある`：`G0075`，应该有点什么。
   - `じゃん`：`G0092`，提醒/吐槽。

### E10-0808 `00:38:36,147 --> 00:38:37,481`

1. `何がですか？`
   - `何が`：`G0096` 加 `G0010`，疑问词作主语。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0809 `00:38:37,565 --> 00:38:38,399`

- `いや…`：话语填充，跳过统计。

### E10-0810 `00:38:38,482 --> 00:38:40,651`

1. `実は みんなのためにやったんじゃないんですか？`
   - `みんなのために`：`G0004` 加 `G0028`，为了大家。
   - `やった`：`G0001`，过去。
   - `じゃないんですか`：`G0098` 加 `G0066` 加 `G0012` 加 `G0002`，确认式反问。

### E10-0811 `00:38:40,735 --> 00:38:41,569`

1. `…みたいなさ`
   - `みたいな`：`G0091`，举出类似说法。
   - `さ`：`G0131`，口语提示。

### E10-0812 `00:38:41,652 --> 00:38:42,820`

1. `でも 違いますよね？`
   - `違います`：`G0012`，礼貌断定。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0813 `00:38:42,903 --> 00:38:44,196`

1. `うん 違うけど`
   - `けど`：`G0019`，转折保留。

### E10-0814 `00:38:44,280 --> 00:38:46,407`

1. `でも ２ターンぐらいあってもいいじゃない`
   - `ぐらい`：`G0027`，大约/程度。
   - `あってもいい`：`G0217` 加 `G0109`，有一两个回合也可以。
   - `じゃない`：`G0098`，确认反问。

### E10-0815 `00:38:46,490 --> 00:38:49,535`

1. `だって 実際 みんなのためにはなったわけだからさ`
   - `だって`：`G0156`，提出理由。
   - `みんなのためには`：`G0004` 加 `G0028` 加 `G0013`，为了大家这一点。
   - `なった`：`G0048` 加 `G0001`，结果变成对大家有利。
   - `わけ`：`G0157`，说明逻辑。
   - `だからさ`：`G0088` 加 `G0131`，理由说明并缓和。

### E10-0816 `00:38:49,618 --> 00:38:52,747`

1. `それは もちろんね みんなの協力があってこそだけど…`
   - `それは`：`G0013`，话题。
   - `ね`：`G0034`，插入缓和。
   - `みんなの協力`：`G0004`，名词修饰。
   - `協力があってこそ`：`G0414`，正因为有大家的合作才成立。
   - `だけど`：`G0019`，转折铺垫。

### E10-0817 `00:38:52,830 --> 00:38:58,711`

1. `温泉は ハゲだけじゃなく器の小ささも適用外らしい`
   - `温泉は`：`G0013`，话题。
   - `だけじゃなく`：`G0354`，不仅……。
   - `器の小ささ`：`G0004`，名词修饰。
   - `も`：`G0011`，也。
   - `らしい`：`G0065`，旁白推断/调侃。

### E10-0818 `00:38:58,794 --> 00:39:00,254`

1. `お話し中 すいません`
   - `お話し中`：`G0046`，敬语名词化；整体为打扰时的礼貌开场。

### E10-0819 `00:39:01,589 --> 00:39:03,591`

1. `レイクホテルの方ですよね？`
   - `レイクホテルの方`：`G0004`，名词修饰。
   - `ですよね`：`G0012` 加 `G0083` 加 `G0034`，礼貌确认。

### E10-0820 `00:39:04,759 --> 00:39:05,968`

- `はい`：应答，跳过统计。

### E10-0821 `00:39:06,802 --> 00:39:11,432`

1. `はじめまして 私 古田という者ですが`
   - `という者`：`G0060`，自我介绍中“叫作……的人”。
   - `ですが`：`G0012` 加 `G0019`，礼貌铺垫。

### E10-0822 `00:39:11,515 --> 00:39:15,269`

1. `実は お２人に お願いがありまして`
   - `お２人に`：`G0017`，请求对象。
   - `お願いが`：`G0010`，主语。
   - `ありまして`：`G0075` 加 `G0012` 加 `G0035`，礼貌铺垫“有个请求”。

### E10-0823 `00:39:15,353 --> 00:39:16,187`

- `お願い？`：复述确认，跳过统计。

### E10-0824 `00:39:17,229 --> 00:39:21,609`

1. `あのホテルの売却を阻止していただきたいんです`
   - `ホテルの売却`：`G0004`，名词修饰。
   - `売却を`：`G0031`，宾语。
   - `阻止していただきたい`：`G0137` 加 `G0134`，希望对方帮忙阻止。
   - `んです`：`G0066` 加 `G0012`，说明请求内容。

### E10-0825 `00:39:23,110 --> 00:39:24,028`

- `えっ？`：反应语，跳过统计。

### E10-0826 `00:39:24,111 --> 00:39:27,490`

1. `あの辺一帯の土地はまもなく市に売却され`
   - `あの辺一帯の土地`：`G0004`，名词修饰。
   - `土地は`：`G0013`，话题。
   - `市に`：`G0017`，卖给的对象。
   - `売却され`：`G0006` 加 `G0117`，被出售并连接后文。

### E10-0827 `00:39:27,573 --> 00:39:30,785`

1. `次々とムダな建物が建設されます`
   - `次々と`：`G0308`，接连不断地。
   - `建物が`：`G0010`，主语。
   - `建設されます`：`G0006` 加 `G0012`，被建设。

### E10-0828 `00:39:31,077 --> 00:39:35,623`

1. `それをきっかけに自然は破壊され 町は荒廃し`
   - `それをきっかけに`：`G0223`，以那件事为契机。
   - `自然は`：`G0013`，话题。
   - `破壊され`：`G0006` 加 `G0117`，被破坏并连接。
   - `町は`：`G0013`，话题。
   - `荒廃し`：`G0117`，连用形中止。

### E10-0829 `00:39:35,706 --> 00:39:39,001`

1. `その結果 2040年には`
   - `2040年には`：`G0017` 加 `G0013`，时间点话题化；句子延续。

### E10-0830 `00:39:39,460 --> 00:39:43,839`

1. `富士山が世界遺産の登録を抹消されます`
   - `富士山が`：`G0010`，主语。
   - `世界遺産の登録`：`G0004`，名词修饰。
   - `登録を`：`G0031`，宾语。
   - `抹消されます`：`G0006` 加 `G0012`，被取消登记。

### E10-0831 `00:39:45,591 --> 00:39:47,134`

- `あの… / なぜ…`：话语填充和句子开头，跳过统计。

### E10-0832 `00:39:47,760 --> 00:39:50,221`

1. `そんなことが言い切れるか`
   - `そんなこと`：`G0167` 加 `G0097`，那样的事。
   - `ことが`：`G0010`，主语。
   - `言い切れる`：`G0233` 加 `G0069`，能够断言。
   - `か`：`G0002`，嵌入疑问/自问。

### E10-0833 `00:39:51,430 --> 00:39:53,140`

- `実は 私`：句子未完，跳过统计。

### E10-0834 `00:39:53,933 --> 00:39:55,726`

1. `タイムリーパーなんです`
   - `なんです`：`G0066` 加 `G0012`，说明身份。

### E10-0835 `00:39:57,394 --> 00:39:59,105`

- `タイムリーパー？`：复述确认，跳过统计。

### E10-0836 `00:39:59,188 --> 00:40:00,398`

1. `信じ難いかもしれませんが`
   - `信じ難い`：`G0286`，难以相信。
   - `かもしれません`：`G0095` 加 `G0012`，也许。
   - `が`：逆接铺垫。

### E10-0837 `00:40:00,481 --> 00:40:02,108`

1. `事実なんです`
   - `なんです`：`G0066` 加 `G0012`，说明事实。

### E10-0838 `00:40:03,275 --> 00:40:05,319`

1. `このままでは 浅田は`
   - `このまま`：`G0258`，保持现状。
   - `では`：`G0056`，以当前状态作前提。
   - `浅田は`：`G0013`，话题。

### E10-0839 `00:40:05,820 --> 00:40:07,988`

1. `ディストピアと化してしまいます`
   - `ディストピアと化して`：`G0415`，变成反乌托邦。
   - `しまいます`：`G0133` 加 `G0012`，不希望的结果。

### E10-0840 `00:40:08,072 --> 00:40:09,782`

1. `これを救えるのは`
   - `これを`：`G0031`，宾语。
   - `救える`：`G0069`，可能。
   - `のは`：`G0097` 加 `G0013`，把“能拯救的人/事”名词化并提题。

### E10-0841 `00:40:10,157 --> 00:40:12,201`

1. `ホテルの従業員であるあなたたち…`
   - `ホテルの従業員`：`G0004`，名词修饰。
   - `である`：`G0140`，书面/正式断定作连体修饰。

### E10-0842 `00:40:12,284 --> 00:40:13,619`

- `あの… すいません`：插话道歉，跳过统计。

### E10-0843 `00:40:14,078 --> 00:40:14,995`

- `はい`：应答，跳过统计。

### E10-0844 `00:40:15,079 --> 00:40:17,665`

1. `それ もう 解決したんで`
   - `解決した`：`G0001`，已经解决。
   - `んで`：`G0052`，说明理由/前提。

### E10-0845 `00:40:20,501 --> 00:40:21,460`

- `うん？`：反应语，跳过统计。

### E10-0846 `00:40:22,211 --> 00:40:23,838`

1. `いや ちょっと前に`
   - `前に`：`G0252`，在不久之前；句子延续。

### E10-0847 `00:40:23,921 --> 00:40:26,715`

1. `市長とホテルのオーナーが逮捕されて`
   - `市長とホテルのオーナー`：`G0016` 加 `G0004`，并列和名词修饰。
   - `オーナーが`：`G0010`，主语。
   - `逮捕されて`：`G0006` 加 `G0035`，被逮捕并连接。

### E10-0848 `00:40:26,799 --> 00:40:29,343`

1. `ホテルは残ることになったんで…`
   - `ホテルは`：`G0013`，话题。
   - `残ることになった`：`G0142` 加 `G0001`，结果变成酒店会保留。
   - `んで`：`G0052`，说明理由。

### E10-0849 `00:40:29,427 --> 00:40:32,221`

1. `ニュースとか見てないですか？`
   - `ニュースとか`：`G0063`，列举/模糊化。
   - `見てない`：`G0030` 加 `G0021`，没有看。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0850 `00:40:36,016 --> 00:40:37,601`

1. `ちょっと ニュースは`
   - `ニュースは`：`G0013`，话题；句子延续。

### E10-0851 `00:40:38,936 --> 00:40:40,479`

1. `見れてないですけど`
   - `見れてない`：`G0069` 加 `G0030` 加 `G0021`，没能看到。
   - `ですけど`：`G0012` 加 `G0019`，礼貌转折。

### E10-0852 `00:40:42,481 --> 00:40:43,774`

- `逮捕？`：复述确认，跳过统计。

### E10-0853 `00:40:43,858 --> 00:40:46,610`

1. `ネットで検索したら出ると思いますよ`
   - `ネットで`：`G0008`，场所/手段。
   - `検索したら`：`G0029` 加 `G0054`，如果搜索。
   - `出ると思います`：`G0023` 加 `G0012`，认为会出来。
   - `よ`：`G0083`，告知。

### E10-0854 `00:40:49,572 --> 00:40:51,115`

1. `そうですか`
   - `そうですか`：`G0003` 加 `G0012` 加 `G0002`，礼貌接受。

### E10-0855 `00:40:53,909 --> 00:40:57,413`

1. `何か 私にできることは？`
   - `何か`：`G0096`，不定。
   - `私に`：`G0017`，能力主体/对象。
   - `できる`：`G0069`，可能。
   - `ことは`：`G0097` 加 `G0013`，能做的事作话题。

### E10-0856 `00:41:00,499 --> 00:41:03,878`

1. `特には ないですね…`
   - `特には`：`G0013`，限定话题。
   - `ない`：`G0021`，否定。
   - `ですね`：`G0012` 加 `G0034`，礼貌缓和。

### E10-0857 `00:41:07,381 --> 00:41:08,549`

- `なるほど`：理解回应，跳过统计。

### E10-0858 `00:41:11,260 --> 00:41:12,344`

1. `じゃ…`
   - `じゃ`：`G0056`，承接告辞。

### E10-0859 `00:41:15,014 --> 00:41:16,640`

- `ありがとうございました`：感谢语，跳过统计。

### E10-0860 `00:41:17,933 --> 00:41:20,060`

- `ありがとうございました`：感谢语，跳过统计。

### E10-0861 `00:41:23,105 --> 00:41:27,985`

1. `こういった急なＳＦにも もはや動じなくなった`
   - `こういった`：`G0060`，`こういう/こういった` 指示性连体表达。
   - `にも`：`G0017` 加 `G0011`，对象加“也”。
   - `もはや`：`G0339`，如今已经。
   - `動じなくなった`：`G0022` 加 `G0001`，变得不再动摇。

### E10-0862 `00:41:28,068 --> 00:41:30,529`

1. `お会計お願いします`
   - `お会計`、`お願いします`：`G0046` 加 `G0012`，礼貌请求结账。
2. `あっ は～い`
   - 应答，跳过统计。

### E10-0863 `00:41:32,156 --> 00:41:33,449`

- `あっ そうだ`：想起某事，跳过统计。

### E10-0864 `00:41:33,532 --> 00:41:34,825`

- `あっ そうだ そうだ`：想起某事，跳过统计。

### E10-0865 `00:41:34,909 --> 00:41:36,744`

- `高橋さん お誕生日…`：祝贺开头，跳过统计。

### E10-0866 `00:41:36,827 --> 00:41:39,455`

- `おめでとうございま～す`：祝贺固定语，跳过统计。

### E10-0867 `00:41:39,538 --> 00:41:42,082`

- `えっ？ あっ… ありがとう`：反应和感谢，跳过统计。

### E10-0868 `00:41:42,166 --> 00:41:43,250`

1. `何でしょうか？`
   - `何`：`G0096`，疑问词。
   - `でしょうか`：`G0094` 加 `G0002`，礼貌推量疑问。

### E10-0869 `00:41:43,334 --> 00:41:44,376`

1. `いや 何だろう？`
   - `何`：`G0096`，疑问词。
   - `だろう`：`G0094`，自问推量。

### E10-0870 `00:41:44,460 --> 00:41:46,545`

1. `何でしょうか？`
   - `何`：`G0096`，疑问词。
   - `でしょうか`：`G0094` 加 `G0002`，礼貌推量疑问。

### E10-0871 `00:41:46,629 --> 00:41:48,255`

1. `何だろうなぁ`
   - `何`：`G0096`，疑问词。
   - `だろう`：`G0094`，自问推量。
   - `なぁ`：`G0131`，感叹/自言语气。

### E10-0872 `00:41:49,798 --> 00:41:50,633`

- `ガンプラ`：名词回答，跳过统计。

### E10-0873 `00:41:50,716 --> 00:41:51,550`

- `（拍手）`：音效，跳过统计。

### E10-0874 `00:41:51,634 --> 00:41:55,512`

1. `いちばんカッコよさそうなやつ選びました`
   - `カッコよさそう`：`G0032`，样态。
   - `やつ`：`G0097`，口语名词化。
   - `選びました`：`G0001` 加 `G0012`，礼貌过去。

### E10-0875 `00:41:56,096 --> 00:41:57,097`

- `ありがとう`：感谢语，跳过统计。

### E10-0876 `00:41:57,181 --> 00:41:58,766`

1. `うれしいですか？`
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0877 `00:41:58,849 --> 00:42:00,017`

- `まあまあまあ…`：含糊回应，跳过统计。

### E10-0878 `00:42:00,100 --> 00:42:01,894`

- `（拍手）`：音效，跳过统计。

### E10-0879 `00:42:01,977 --> 00:42:02,895`

- `♪ 55歳`：唱和，跳过统计。

### E10-0880 `00:42:02,978 --> 00:42:04,146`

- `そして…`：承接词，跳过统计。

### E10-0881 `00:42:04,230 --> 00:42:06,065`

1. `いや お客さんいるから ちょ… ちょっと…`
   - `お客さんいる`：`G0045`，有人在。
   - `から`：`G0088`，理由；后续制止延续到下一块。

### E10-0882 `00:42:06,148 --> 00:42:07,233`

1. `静かに… やめよう`
   - `静かに`：`G0017`，副词化。
   - `やめよう`：`G0201`，意向形提议。

### E10-0883 `00:42:10,653 --> 00:42:13,364`

1. `30年の月日がたった`
   - `30年の月日`：`G0004`，名词修饰。
   - `月日が`：`G0010`，主语。
   - `たった`：`G0001`，过去。

### E10-0884 `00:42:20,829 --> 00:42:21,830`

- `若葉ちゃん ありがとね`：感谢语，跳过统计。

### E10-0885 `00:42:21,914 --> 00:42:24,667`

1. `じゃ また連絡するね`
   - `じゃ`：`G0056`，告别承接。
   - `連絡する`：`G0029`，名词＋する。
   - `ね`：`G0034`，柔和语气。

### E10-0886 `00:42:24,750 --> 00:42:26,085`

1. `楽しんできてね`
   - `楽しんできて`：`G0024`，去享受后再回来/去玩一趟。
   - `ね`：`G0034`，叮嘱。
2. `うん`
   - 应答，跳过统计。

### E10-0887 `00:42:27,836 --> 00:42:28,921`

- `よいしょ`：动作声，跳过统计。

### E10-0888 `00:42:29,004 --> 00:42:30,506`

1. `気をつけてね`
   - `気をつけて`：`G0228`，句末て形轻请求/叮嘱。
   - `ね`：`G0034`，柔和。
2. `は～い`
   - 应答，跳过统计。

### E10-0889 `00:42:30,589 --> 00:42:32,174`

- `またね～`：告别语，跳过统计。

### E10-0890 `00:42:32,925 --> 00:42:35,177`

- `うわぁ ちょっと 久しぶり`：寒暄反应，跳过统计。

### E10-0891 `00:42:35,261 --> 00:42:37,429`

- `あ～ ホント`：反应语，跳过统计。

### E10-0892 `00:42:37,513 --> 00:42:38,931`

- `こんにちは`：寒暄，跳过统计。

### E10-0893 `00:42:39,014 --> 00:42:40,224`

- `お～ 来た来た`：寒暄/反应，跳过统计。

### E10-0894 `00:42:40,307 --> 00:42:41,892`

- `お久しぶりです / 久しぶり`：寒暄，跳过统计。

### E10-0895 `00:42:41,976 --> 00:42:44,103`

- `あっ これ 差し入れ`：名词省略句，跳过统计。

### E10-0896 `00:42:44,186 --> 00:42:45,646`

- `ありがとう 悪いね`：感谢和客气回应，跳过统计。

### E10-0897 `00:42:45,980 --> 00:42:46,814`

- `あっ…`：反应语，跳过统计。

### E10-0898 `00:42:46,897 --> 00:42:49,858`

- `遠藤さんに 日比野さんに 中村さん`：人名列举，跳过统计。

### E10-0899 `00:42:50,484 --> 00:42:52,570`

1. `そうしましたら こちらにタッチをお願いします`
   - `そうしましたら`：`G0054`，`そうしたら` 的礼貌形式。
   - `こちらに`：`G0017`，触碰位置。
   - `タッチを`：`G0031`，宾语。
   - `お願いします`：`G0046` 加 `G0012`，礼貌请求。

### E10-0900 `00:42:52,653 --> 00:42:53,487`

- `はい`：应答，跳过统计。

### E10-0901 `00:42:54,488 --> 00:42:55,322`

- `（認証音）`：音效，跳过统计。

### E10-0902 `00:42:55,406 --> 00:42:56,740`

- `ありがとうございます`：感谢语，跳过统计。

### E10-0903 `00:42:56,824 --> 00:43:00,244`

1. `待って 高橋さん 全然変わってないんだけど`
   - `待って`：`G0228`，句末て形制止/引起注意。
   - `全然変わってない`：`G0171` 加 `G0030` 加 `G0021`，完全没变。
   - `んだけど`：`G0066` 加 `G0019`，说明并带惊讶转折。

### E10-0904 `00:43:00,327 --> 00:43:01,745`

1. `今 おいくつでしたっけ？`
   - `でしたっけ`：`G0012` 加 `G0001` 加 `G0071`，礼貌回想确认。

### E10-0905 `00:43:01,829 --> 00:43:02,830`

- `84`：数字回答，跳过统计。

### E10-0906 `00:43:02,913 --> 00:43:03,831`

- `えっ！？`：反应语，跳过统计。

### E10-0907 `00:43:03,914 --> 00:43:04,873`

1. `人間でいうと？`
   - `でいうと`：`G0153`，用人类标准来说。

### E10-0908 `00:43:04,957 --> 00:43:06,250`

- `84だよ`：数字回答，跳过统计。

### E10-0909 `00:43:06,333 --> 00:43:08,335`

1. `てか 髪も全然 減ってませんね`
   - `てか`：`G0074`，话题推进。
   - `髪も`：`G0011`，头发也。
   - `全然減ってません`：`G0171` 加 `G0030` 加 `G0021` 加 `G0012`，完全没有减少。
   - `ね`：`G0034`，确认。

### E10-0910 `00:43:08,419 --> 00:43:10,671`

1. `言ったじゃん 自前のハゲじゃないって`
   - `言った`：`G0001`，过去。
   - `じゃん`：`G0092`，提醒对方。
   - `自前のハゲ`：`G0004`，名词修饰。
   - `じゃないって`：`G0043` 加 `G0064`，引用“不是天生秃”。

### E10-0911 `00:43:10,754 --> 00:43:12,840`

1. `ず～っと自前だと思ってたけど`
   - `自前だと思ってた`：`G0023` 加 `G0030` 加 `G0001`，一直以为是天生的。
   - `けど`：`G0019`，转折。

### E10-0912 `00:43:12,923 --> 00:43:13,882`

1. `違うから`
   - `から`：`G0088`，说明理由/纠正。

### E10-0913 `00:43:13,966 --> 00:43:15,259`

1. `あっ そうだ ズルッパゲだ`
   - `そうだ`：`G0003` 加 `G0012`，想起/确认。
   - `だ`：`G0012`，断定。

### E10-0914 `00:43:15,342 --> 00:43:16,385`

1. `ズルッパゲは やめてよ`
   - `は`：`G0013`，把该说法作为话题。
   - `やめて`：`G0228`，て形请求。
   - `よ`：`G0083`，提醒。

### E10-0915 `00:43:16,468 --> 00:43:18,512`

- `ヤダヤダ… / （笑い声）`：反应和笑声，跳过统计。

### E10-0916 `00:43:18,596 --> 00:43:19,763`

1. `お待たせいたしました`
   - `お待たせ`：`G0046`，敬语框架。
   - `いたしました`：`G0051` 加 `G0012` 加 `G0001`，谦让礼貌过去。

### E10-0917 `00:43:19,847 --> 00:43:22,182`

1. `生体情報の登録が完了いたしました`
   - `生体情報の登録`：`G0004`，名词修饰。
   - `登録が`：`G0010`，主语。
   - `完了いたしました`：`G0051` 加 `G0012` 加 `G0001`，谦让礼貌报告。

### E10-0918 `00:43:22,266 --> 00:43:24,560`

1. `お部屋は ５０１から５０３になります`
   - `お部屋は`：`G0046` 加 `G0013`，敬语名词作话题。
   - `５０１から５０３`：`G0026` 加 `G0014`，房号范围。
   - `になります`：`G0048` 加 `G0012`，服务业礼貌提示。

### E10-0919 `00:43:24,643 --> 00:43:26,228`

- `（笑い声）`：笑声，跳过统计。

### E10-0920 `00:43:26,312 --> 00:43:27,771`

1. `笑い過ぎでしょう`
   - `笑い過ぎ`：`G0179`，笑过头。
   - `でしょう`：`G0094`，确认推量/吐槽。

### E10-0921 `00:43:27,938 --> 00:43:29,773`

1. `ごゆっくりお過ごしくださいませ`
   - `ごゆっくりお過ごし`：`G0046`，敬语框架。
   - `くださいませ`：`G0053` 加 `G0049`，郑重请求。

### E10-0922 `00:43:33,277 --> 00:43:35,612`

1. `メチャクチャ買い込んでましたね`
   - `メチャクチャ`：`G0310`，强口语程度副词。
   - `買い込んでました`：`G0030` 加 `G0012` 加 `G0001`，过去结果状态。
   - `ね`：`G0034`，确认。

### E10-0923 `00:43:35,696 --> 00:43:36,655`

1. `そうだね`
   - `そうだね`：`G0003` 加 `G0012` 加 `G0034`，认同。

### E10-0924 `00:43:36,739 --> 00:43:38,198`

1. `絶対 余りますよね`
   - `余ります`：`G0012`，礼貌体。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0925 `00:43:38,282 --> 00:43:39,283`

- `いや…`：话语填充，跳过统计。

### E10-0926 `00:43:39,366 --> 00:43:41,285`

1. `あの３人は全部 食べるのよ`
   - `３人は`：`G0013`，话题。
   - `食べるの`：`G0097`，说明性名词化。
   - `よ`：`G0083`，告知。

### E10-0927 `00:43:41,368 --> 00:43:42,828`

1. `そうなんスか？`
   - `そうなんスか`：`G0003` 加 `G0066` 加 `G0012` 加 `G0002`，口语丁寧疑问。

### E10-0928 `00:43:44,496 --> 00:43:45,789`

1. `ちょっと裏行くから 頼むね`
   - `行くから`：`G0088`，说明理由/前提。
   - `ね`：`G0034`，请求缓和。

### E10-0929 `00:43:45,873 --> 00:43:46,832`

- `あっ はい`：应答，跳过统计。

### E10-0930 `00:43:59,428 --> 00:44:01,180`

- `あっ 高橋さん`：呼唤，跳过统计。

### E10-0931 `00:44:01,555 --> 00:44:02,473`

- `なに？`：短问句，跳过统计。

### E10-0932 `00:44:02,556 --> 00:44:03,766`

1. `あの… 高橋さんが`
   - `高橋さんが`：`G0010`，主语；句子延续到下一块。

### E10-0933 `00:44:03,849 --> 00:44:05,351`

1. `宇宙人だっていうこと 彼氏には言ってもいいですか？`
   - `だっていうこと`：`G0060` 加 `G0097`，引用并名词化“是外星人这件事”。
   - `彼氏には`：`G0017` 加 `G0013`，对象话题化。
   - `言ってもいい`：`G0217` 加 `G0109`，可以说吗。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0934 `00:44:05,434 --> 00:44:07,102`

1. `彼氏には言ってもいいですか？`
   - `彼氏には`：`G0017` 加 `G0013`，对象话题化。
   - `言ってもいい`：`G0217` 加 `G0109`，可以说吗。
   - `ですか`：`G0012` 加 `G0002`，礼貌疑问。

### E10-0935 `00:44:07,186 --> 00:44:08,437`

- `え～っ？`：反应语，跳过统计。

### E10-0936 `00:44:10,147 --> 00:44:11,690`

1. `結婚は考えてんの？`
   - `結婚は`：`G0013`，话题。
   - `考えてんの`：`G0030` 加 `G0066` 加 `G0097`，`考えているの` 的口语缩约。

### E10-0937 `00:44:11,774 --> 00:44:14,401`

1. `一応 来月 入籍予定です`
   - `です`：`G0012`，礼貌断定。

### E10-0938 `00:44:16,445 --> 00:44:17,363`

1. `特別ね`
   - `ね`：`G0034`，提醒/缓和。

### E10-0939 `00:44:18,030 --> 00:44:18,947`

- `ありがとうございます`：感谢语，跳过统计。

### E10-0940 `00:44:19,031 --> 00:44:19,865`

1. `じゃ その代わり`
   - `じゃ`：`G0056`，承接。
   - `その代わり`：`G0180`，作为交换。

### E10-0941 `00:44:19,948 --> 00:44:21,325`

1. `会って 能力 見せてほしいとかは`
   - `会って`：`G0035`，て形连接。
   - `見せてほしい`：`G0073`，希望对方展示。
   - `とかは`：`G0063` 加 `G0013`，把这类请求作为话题。

### E10-0942 `00:44:21,408 --> 00:44:22,618`

1. `なしだからね`
   - `だから`：`G0088`，说明/强调。
   - `ね`：`G0034`，提醒。

### E10-0943 `00:44:25,120 --> 00:44:26,163`

- `あの…`：话语填充，跳过统计。

### E10-0944 `00:44:26,246 --> 00:44:27,623`

1. `できれば結婚式の余興で十円玉…`
   - `できれば`：`G0081`，如果可以的话。
   - `結婚式の余興`：`G0004`，名词修饰。
   - `余興で`：`G0008`，场景。

### E10-0945 `00:44:27,706 --> 00:44:28,540`

1. `もう言ってるよね？`
   - `言ってる`：`G0030`，已经说了。
   - `よね`：`G0083` 加 `G0034`，确认。

### E10-0946 `00:44:28,624 --> 00:44:29,458`

1. `言ってないです`
   - `言ってない`：`G0030` 加 `G0021`，没说。
   - `です`：`G0012`，礼貌断定。

### E10-0947 `00:44:29,541 --> 00:44:30,793`

1. `言ってるでしょう`
   - `言ってる`：`G0030`，已经说着/刚说了。
   - `でしょう`：`G0094`，确认推量。

### E10-0948 `00:44:31,043 --> 00:44:33,337`

1. `なに？ 結婚式の余興って…`
   - `なに`：`G0096`，疑问词。
   - `結婚式の余興`：`G0004`，名词修饰。
   - `って`：`G0064`，口语提题。

### E10-0949 `00:44:34,088 --> 00:44:35,506`

1. `はい フロントです`
   - `です`：`G0012`，礼貌断定。

### E10-0950 `00:44:36,423 --> 00:44:37,591`

- `うん？`：反应语，跳过统计。

### E10-0951 `00:44:37,883 --> 00:44:39,885`

1. `なに？ どこで落としたのよ`
   - `どこで`：`G0096` 加 `G0008`，询问地点。
   - `落とした`：`G0001`，过去。
   - `のよ`：`G0097` 加 `G0083`，说明式追问。

### E10-0952 `00:44:40,260 --> 00:44:42,304`

1. `最後に出したの どこ？`
   - `最後に`：`G0017`，时间副词化。
   - `出した`：`G0001`，过去。
   - `の`：`G0097`，名词化“拿出来的地方/动作”。
   - `どこ`：`G0096`，疑问词。

### E10-0953 `00:44:42,721 --> 00:44:44,306`

1. `じゃ コンビニじゃない？`
   - `じゃ`：`G0056`，承接推论。
   - `じゃない`：`G0098`，确认反问。

### E10-0954 `00:44:46,975 --> 00:44:48,268`

1. `特別ね`
   - `ね`：`G0034`，提醒/宽许语气。

### E10-0955 `00:44:50,437 --> 00:44:52,856`

1. `ちょっと出てきます`
   - `出てきます`：`G0024` 加 `G0012`，出去一下再回来。

### E10-0956 `00:44:52,940 --> 00:44:53,774`

- `あの…`：话语填充，跳过统计。

### E10-0957 `00:44:53,857 --> 00:44:54,858`

1. `結婚式の余興なんですけど…`
   - `結婚式の余興`：`G0004`，名词修饰。
   - `なんですけど`：`G0066` 加 `G0012` 加 `G0019`，说明并铺垫请求。

### E10-0958 `00:44:54,942 --> 00:44:56,151`

1. `特別ね`
   - `ね`：`G0034`，宽许/提醒。

### E10-0959 `00:44:56,235 --> 00:44:57,236`

- `ありがとうございます`：感谢语，跳过统计。

### E10-0960 `00:44:57,319 --> 00:44:59,405`

1. `高橋さんの秘密も`
   - `高橋さんの秘密`：`G0004`，名词修饰。
   - `も`：`G0011`，秘密也；句子延续。

### E10-0961 `00:44:59,488 --> 00:45:04,159`

1. `今では 富士浅田市民の過半数が知っていると言っても`
   - `今では`：`G0056`，时间话题提示。
   - `富士浅田市民の過半数`：`G0004`，名词修饰。
   - `過半数が`：`G0010`，主语。
   - `知っている`：`G0030`，知道的状态。
   - `と言っても`：`G0416`，接入“说……也不为过”的前半。

### E10-0962 `00:45:04,243 --> 00:45:05,828`

1. `過言ではない`
   - `過言ではない`：承接上一块 `と言っても`，构成“说……也不为过”的固定结构。

### E10-0963 `00:45:06,662 --> 00:45:07,621`

- `ただ…`：转折铺垫，跳过统计。

### E10-0964 `00:45:08,997 --> 00:45:13,544`

1. `不思議と その情報が県境を越えたことは`
   - `情報が`：`G0010`，主语。
   - `県境を`：`G0031`，宾语。
   - `越えたこと`：`G0001` 加 `G0097`，把“越过县境”名词化。
   - `ことは`：`G0013`，话题。

### E10-0965 `00:45:16,171 --> 00:45:18,006`

1. `一度もない`
   - `一度もない`：`G0368` 加 `G0021`，一次也没有。

## 第五批复盘（0801-0965）

- 本批完成 E10 与全剧收尾，从高桥的“谦逊被照单全收”玩笑，接到未来时间旅行者再次登场、30年后酒店日常，以及最后“秘密只在本市流传”的总结。
- 新增 `G0414 Nがあってこそ`、`G0415 Nと化す`、`G0416 と言っても過言ではない`。
- 高频复习项包括 `じゃん`、`っていうか`、`じゃないですか`、`ていただきたい`、`ことになる`、`このまま`、`もはや`、`でしょうか`、`てほしい`、`と言っても`。
- E10 已完成，整部 `E01-E10` 的逐句解析已收尾。
